# Fixes log

Short list of **resolved** issues. Active problems stay in [bugs.md](bugs.md).

| When | Area | What was fixed |
|------|------|----------------|
| 2026-09-23 | Marketing | **Meta ad preview links.** The Meta sync now reads each ad's `preview_shareable_link`, Meta's shareable fb.me link to the rendered ad, which opens with no Ads Manager login, into `meta_ad_creatives.preview_shareable_link` (`20260923150000`). The ad name links to it on the Marketing Report, Overview and Explorer; the destination stays a separate line. Probed first on the live account: 10 of 10 ads returned one, nothing refused. The link is written only when Meta returns one, so a refused or omitted field on a later run never erases it. `wow_creatives` is edited in place from its deployed definition. A batch in which EVERY ad is refused on PERMISSION (a scope refusal is not the unknown-field shape, so nothing named it) retries once without the preview; refused again, the ad-level sync errors instead of logging a successful zero creatives. The creative backfill's default mode now also re-asks about ads missing a preview, so one run fills the 4,093 creatives the 30-day nightly never reaches |
| 2026-09-23 | Accounting | **Prepared coding suggestions are stored, not held in the page** (`20260923120000`, `card-categorize` split into `prepare.ts` + `index.ts`, `v2/coding-suggestions.js`). A Claude suggestion lived in a browser `Map` until someone accepted it and pressed Save: a reload, a second tab or a colleague's screen lost the prepared work, and it had to be paid for again. Suggestions now live in `card_coding_suggestions`, apart from accepted coding, with the fingerprint of the facts they were made from (`card_coding_input_hash`), their evidence, confidence, model and prompt version. The preparer reads that fingerprint BEFORE the facts and the writer refuses any answer about facts that moved in between. A dismissal persists until the facts change or someone asks again; a failure never replaces a prepared answer; duplicate delivery leaves one live row. **Accepting now saves**: `accept_card_coding_suggestions` re-checks company, finance access, the batch, the row, the facts, the connection and the account, then writes through `apply_card_coding` as the person clicking — no separate Save — and approval and posting are unchanged. Each model call's answers are saved as it returns, so one slow or failed call no longer discards the rest, and every run records per-phase timings and the model's own token counts in `card_coding_preparation_runs`. The page shows feed-synced and coding-prepared times separately, ready / needs-a-person / failed counts, a Retry on failures and Ask again on declines. Preparation is still started by a person in this change; background preparation after sync is the next one. Review fixes: a suggestion whose account was deactivated no longer blocks a fresh one (one `card_coding_suggestion_stale_reason()` now serves the view and the writer), a slice's rows are saved in chunks of 500 so a merchant with thousands of transactions cannot exceed the writer's limit, and bulk Use suggestions sends at most 500 ids per call and keeps earlier batches if a later one fails. |
| 2026-09-23 | Accounting | **Payment requests in the cash forecast, a Books ledger roll-forward, and Books laid out for everyday work** (UI only, no migration). *Cash forecast*: open Request Manager requests can be added as a "Payment requests" planning line, switched off by default. It carries anything not paid, completed, rejected or cancelled, because Baseballism's open requests are all `new` (0 of 102 were `approved` on 2026-09-23), on its due date, or the first forecast day if due today. It is never an actual, never part of the bank trend and never matched to bank rows: a request leaves the forecast when it is marked paid. **Past-due requests are listed, not forecast** (68 requests / $1.15M on 2026-09-23, mostly paid but never marked), so a stale queue cannot put a cash cliff on tomorrow. Undated ones are listed too. Overrides, planned movements and payment requests moved from below the grid into a **Planned activity** side panel, and forecast cells that include a plan or request carry a dot. RLS note: `payment_requests_active_select` shows a finance user who is not an AP manager or admin only their own requests, and the panel says how many it can see. *Books & setup*: a new **Ledger** tab rolls each account forward (opening balance + posted by Silo + other activity in QuickBooks = closing per a saved QuickBooks trial balance). The opening balance comes from the accepted snapshot. "Posted by Silo" is the `Line` array of POSTED `quickbooks_journal_postings` payloads (what Intuit received), and "other" is everything entered directly in QuickBooks, kept as its own column rather than folded in. A missing trial balance reads unknown, never zero. Postings and trial balances are read for the connection the books were seeded from only (account ids are local to a connection), every posted journal is read in ordered pages until an empty one (never one capped page), and a trial balance is used only when its params and header match the opening snapshot: same basis, currency, unfiltered, Debit/Credit columns and end period, mirroring `seed_accounting_from_qbo`. An incompatible one is excluded or named, never compared. A read-only "Fetch trial balance through today" uses the same `quickbooks-report` fetch Setup seeds with. Accepted books now open on Ledger, and tabs lead with Ledger / Journals / Chart of accounts / Locations. Chart of accounts shows status at seeding AND in QuickBooks now (inactive / not in last sync flagged) and what each line needs (customer on AR, vendor on AP). Locations lists QuickBooks locations with the Silo sales locations mapped to each, and names unmapped ones. The accepted opening trial balance folds behind a summary line, and the migration steps can be hidden (remembered per company) while the headline stays |
| 2026-09-23 | Accounting | **Trust pass on Schedules, Cash forecast, Reports and Journals** (UI only, no migration). *Schedules* showed `recognized_to_date` as "Recognised", but that column is a date calculation (months whose service has ended) and says nothing about posting — so a schedule read as done when nothing had reached QuickBooks. It is now "Due to date", beside "Posted to QuickBooks" (net credits to the prepaid account on posted `schedules:prepaid:*` entries) and "Due, not posted", all scoped to prepaid items because only prepaid amortization posts from that page (deferred and other types are named separately with their due amount, never counted as a missing prepaid month); an unreadable posted figure shows as unknown, never zero. "Stamp selected" is "Create schedule". *Cash forecast*: "Go to forecast" measured `offsetLeft` from the page body, so it overshot by the sidebar width and hid the actual/forecast boundary under the sticky label column; it now also scrolls the grid into view and lands the boundary a quarter of the way in. COA rows open collapsed as one total per QuickBooks account type and expand to their accounts; an actual column containing today or pending Plaid rows says "in progress" / "N pending" in its header (pending rows are still never counted); the status line names the oldest bank sync; charts collapse; account cards and stats are shorter. *Reports* picks a statement by tile, and the account filter appears only for the three reports QuickBooks applies it to. The staged/posted Adjustments list left Reports: Discard (draft adjustments) and Void (posted adjustments and card batches, same RPCs) are now on Books & setup → Journal register, which already lists every source, and Reports links to it as "Journals". An adjustment can still start from an account's drill-down |
| 2026-09-22 | Reporting (v3) | **Reports library: SILO Reports, My Reports, Dashboards.** `/v3/dashboards.html` mixed the canned SILO definitions and custom saved reports in one "Saved reports" list under a page and sidebar that said Dashboards. It is now titled Reports with three URL-backed tabs (`report-library.js` owns the classification: SILO = system AND global; My Reports = every other readable report, company-shared included), lighter cards with a one-sentence summary and a Details disclosure, no redundant badges on SILO cards, contextual New report / New dashboard, and per-tab empty and no-result states. The sidebar's separate Saved reports row is gone. `report-catalog.js` now files by title, since the shortened titles misfiled six reports. `20260922170000` records the direct production cleanup (21 → 17 SILO reports, shortened titles, reconnected widgets) and runs last in `apply_all_post_merge.sql`, because the seed migrations would otherwise re-create the retired reports and restore the long titles on any re-run; it is a no-op against production |
| 2026-09-22 | Launches | **The launch form now asks how a launch will be measured before it saves.** A launch is measured only through a linked PO or attached products, and 43 of 61 were unmeasurable because nobody attached either — unrecoverable later, since overlapping launches cannot be separated by date. A MEASUREMENT LINK section states the link; with none, Save is refused until the person links a PO, commits to attaching products right after saving, or marks products "not known yet". That last choice is stored (`20260922150000`: `launch_calendar.products_unknown_at`/`_note`, `_by` stamped by trigger; attaching a product or linking a PO clears it by trigger from any writer, and a later removal does not resurrect it) and the launch keeps a "Products not known yet" marker, alongside "No products or PO" for unlinked ones, with a MEASUREMENT filter, until it is linked. Measurement views and rules unchanged. Also fixed the post-save hint ("add product lines…") being overwritten by the generic Saved message a line later. Rules in `v2/launch-product-link.js`; unit + browser suites in `v2/tests/` |
| 2026-09-22 | Purchasing / Multi-tenant | **The PO Builder vendor PDF printed Baseballism's identity for every tenant** — `Baseballism Inc.`, the Beaverton ship-to and a named buyer's phone were literals in `/v2/po-builder.html`. `20260922130000` adds the document identity to `company_settings` (legal name, ship-to, phone, purchasing contact), editable on Workspace Settings → Company; the PDF reads it through `v2/po-document-identity.js` with honest fallbacks (title, the generating user) and names a missing address rather than inventing one. Baseballism is seeded with its previous values only where null, so its PDF is unchanged. Also adds the #750 Slack-pause migration to `apply_all_post_merge.sql`, which had missed it |
| 2026-09-22 | Payment Requests | **Payment Request 2 promoted.** The assisted intake (`/v2/purchase_request2.html`) is now the `Payment Request` nav row, breadcrumb and Finance/Request Manager/Help link; the original form (`/v2/purchase_request.html`) is renamed `Payment Request 2` and leaves the menu, reachable only from the promoted page's header link. Names and links swapped; URLs and device-local drafts untouched, so nothing bookmarked or in progress moves. The `· Beta` pill is gone |
| 2026-09-22 | Payment Request 2 | Block NUL and unpaired Unicode surrogates before submission. A reviewed text-repair dialog recovers rejected device-local drafts with their original reference and attachments, only after an authenticated same-company absence check. Revision conflicts and unknown/existing requests refuse recovery. Valid Unicode is preserved; no database or Edge deployment changes. |
| 2026-09-22 | Notifications / Payment Requests | **Deleting Slack secrets did not pause the legacy Baseballism channel.** Seven database triggers and an active daily cron called a separate unauthenticated `notify-slack` Edge Function whose webhook was embedded in source. The migration removes every legacy caller and retires their database functions; a checked-in disabled endpoint replaces the previously untracked production function. Tenant-aware email and the secret-gated `sample-notify` email path remain. Payment Request 2 also preserves and displays the bounded PostgREST error code/message when an insert is rejected, instead of discarding the cause behind “Submission could not be confirmed.” **Cycle-1 review corrections:** the stored reason is also rendered when the draft is reopened from the shelf after a reload (`resumeMessage`), which is the recovery path an IndexedDB-backed retry actually takes, and `legacy-slack-pause.test.mjs` is now invoked by the Finance database job — it had landed with no step running it, so CI passed having executed none of its six checks. `workflow-env-contract.test.mjs` now fails on any suite under `scripts/tests/` that no workflow's `node` step names |
| 2026-09-20 | Marketing / Multi-tenant | **Every Marketing RPC was scoped to the online store by a hardcoded `location_tag = 'online'`** (`20260920190000`, `20260920180000`) — seven sites across five deployed functions (`wow_report` ×3, `wow_data_through`, `wow_kpi_compare`, `wow_online_shop_domains`, `wow_paid_media_reality`). It worked only because Baseballism named their online location "online". **Measured across the three live tenants:** Baseballism 27 locations (1 online / 26 retail / 1 wholesale); **Test Company 2 locations, both retail, no online store at all**; BlockayOps no `locations` rows. For tenants two and three the literal matches nothing, so every Marketing figure returns empty and the page draws it as **zero — indistinguishable from a bad week**. Now resolved through `silo_channel_location_tags()` over `locations.store_type`. **No new table, and the first draft's was wrong:** it assumed nothing maintained `store_type` — true of the Shopify sync, which only *reads* `locations`, but false of the app, since `/v2/integrations.html`'s location mapper has always written it from a constrained select. A second control for one fact is the drift being removed. `silo_location_slug()` also names the slugify join that was inlined by hand in three readers. **The array return is load-bearing:** a set-returning function in a `WHERE` clause carries the planner's 1000-row default — the trap `wow_window()` needed `ROWS 1` for — so a STABLE scalar keeps the `(company_entity_id, location_tag, day_date)` index usable; verified `= any(...)` returns $51,226,349.47 both ways. Not filtered on `is_active`: a closed store's history is still retail revenue. **An empty channel is NOT CONFIGURED, never zero** — `wow_channel_status()` reports it and both `/v2/wow-report.html` and the Integrations location panel render that instead of a `$0`. **Both migrations refuse rather than guess:** the first asserts per company that the derived online set equals the literal's (Baseballism `{online}`={online}, the other two `{}`={}), so no published number moves; the second rewrites the *deployed* bodies by assertion-guarded string replacement (the `20260901140000` method — several of these were edited in place and the repo text is not what runs, which is how `wow_creatives` nearly lost two headline metrics), checking each function's site count and that every literal is a `location_tag` predicate. `verify_v2_schema.sql` fails CRITICAL if the literal returns anywhere in `public`; confirmed the check fires — its regex matches exactly those 5 functions before the fix |
| 2026-09-20 | Notifications | **A notification now names the tenant it is about, and a reply reaches that tenant** (`20260920190000`). All ten mail edge functions held ONE global constant — `SILO_MAIL_FROM`, defaulting to `SILO <noreply@silo-baseballism.com>` — and **nine of the ten set no `Reply-To` at all** (only `payment-request-forward-melio` did), so a reply about an invoice, a purchase request or a review went to whatever the From address was: SILO. A single env string also cannot carry one company's name on one email and another's on the next, so the sender identity is now resolved at send time by `resolve_notification_sender(company, purpose, actor_email)` — From built from the company's own title (`Baseballism - SILO <notifications@get-silo.com>`), Reply-To from `company_notification_contacts`. **The fallback chain is the whole point**: purpose → `general_ops` → whoever triggered it → an owner-admin → nothing, and **never a SILO address** — omitting Reply-To looks harmless and would route every unconfigured tenant's replies to `notifications@get-silo.com`, which is every tenant on day one. `reply_to_source` records which rung answered, so a blank row can still say who it reaches. A **database** function rather than a `_shared/` TS module, because `_shared` sits outside each function directory and `deployment-drift-check` diffs deployed functions against the repo — a cross-directory import risks showing all ten as permanently drifted. The `sender` is passed explicitly and is deliberately NOT module-level: a Deno isolate serves concurrent requests, so a shared mutable would put one tenant's name on another tenant's email. The From display name is sanitised, since a title carrying a quote or CRLF would split the header. Workspace Settings → Notifications becomes editable for the five purposes and renders the RESOLVER's answer rather than the stored row, so an unset purpose shows which rung is covering it. Also: **the ten functions now typecheck in CI** — nothing checked them before, which is how four carried a `err?.message`-on-`{}` error for months (Supabase does not typecheck on deploy, and these are only exercised when an email actually sends); all four fixed. Regression: `scripts/tests/notification-sender.test.mjs`, 12 assertions, **5 mutations all caught**, including `fallback-to-silo` and `cross-tenant-open`. **The independent review caught two P1s, both real:** the resolver is DEFINER, takes a company id and is granted to `authenticated`, so it was an RLS bypass until it re-checked the caller's own memberships (a member of one tenant could read another's reply address, and its owner-admin email out of the fallback); and the `sample-notify` edit had put a `const` inside a `Promise.all([` array literal, a syntax error that would have stopped every sample notification. The second was missed locally because the check loop grepped stdout for `ERROR` and a Deno *syntax* error prints lowercase `error:` — it now goes by exit code. `verify_v2_schema.sql` gains **Notification sender resolves per tenant**, which fails if the fallback can ever name a SILO domain. **Requires `SILO_MAIL_FROM` to be set and the ten functions redeployed** — merging does not deploy. |
| 2026-09-20 | Multi-tenant | **The entity admin gate answered about the wrong company** (`20260920160000`; found by impersonating the newly invited BlockayOps admin while finishing the Workspace Settings acceptance walkthrough). `is_owner_admin()` was `exists (select 1 from entity_memberships where user_id = auth.uid() and role in ('owner','admin'))` — **no `entity_id` predicate at all**, so it asked "am I an admin *anywhere*" and answered true for *every* company. `20260913054723` diagnosed this same root cause and fixed it for `profiles`; the function stayed in place for everything else. Measured live: operational isolation held (0 Baseballism payment requests, POs or launches; `profiles` correctly returned only the 2 BlockayOps members) but `entities` returned **all three tenants** with their `entity_key`, through `entities_select_access` → `can_access_entity()`. The same unscoped gate was the **entire qual** of `entities_delete_admin_only`, and DELETE was still granted to `authenticated` — `20260920130000` revoked UPDATE only. **On severity, precisely:** deleting Baseballism would fail, because ~70 child FKs are `NO ACTION` and populated, so the statement aborts atomically — accidental protection, not design. A tenant founded last week has no rows in those tables, and ~70 *other* FKs are `CASCADE` (`company_settings`, `entity_memberships`, `shopify_connections`, `billing_subscriptions`, `stripe_connect_accounts`, every `customer_account*`). Not tested by deleting anything: entity triggers reach `notify-slack` via `pg_net`, which is not transactional. **The same stale vocabulary fails the other way too** — per-company roles are `owner_admin|admin|member|viewer` and `'owner'` matches no production row (33 `admin`, 3 `owner_admin`, 2 `member`), so `is_entity_admin`, which *is* correctly scoped, excluded **every owner_admin from their own company**. Fixed: both gates test `('owner_admin','admin')`; `can_access_entity()` loses the wide branch; the four policies built on it are retired (each table already carried a correctly scoped `is_entity_admin(...)` sibling — they were pure widening duplicates, and policies are OR'd, so fixing the function alone would not have closed it), with the author/uploader clauses on `entity_comments`/`files` preserved separately; `is_owner_admin()` is **dropped** rather than corrected, since a zero-argument "am I an admin" helper cannot be scoped to a row. INSERT and DELETE on `entities` are revoked — founding and destroying a company are definer-function acts, and every legitimate writer already is one. Regression: `scripts/tests/entity-admin-gate-scope.test.mjs`, 12 assertions, **6 mutations all caught**; the fixture builds the hole first, and its delete assertions use a *young* tenant because asserting against Baseballism would pass with or without the fix. `verify_v2_schema.sql` gains **Entity admin gate is company-scoped**, confirmed CRITICAL against production before applying. |
| 2026-09-20 | Multi-tenant | **The stamp helper was not a no-op, and check 6 could not fail for fewer than six tables** (`20260920150000`; both found by the independent review on #736). Putting the helper into CLAUDE.md's new-table checklist turned a quarterly repair into a per-migration step, and that exposed two things. **The helper** looped every eligible table issuing `DROP` + `CREATE TRIGGER` unconditionally — 164 on production — and inside a migration transaction those `ACCESS EXCLUSIVE` locks are held to COMMIT, so a migration adding one table would lock 164 operational tables and block behind the first long transaction it met, with writers queued behind it. It now enumerates only tables whose trigger is missing or **wrongly bound** (right function, BEFORE INSERT, FOR EACH ROW, enabled — a trigger with the right name and the wrong body is what a name-only test waves through), so a no-op run takes no locks. **Check 6** compared a COUNT of triggers against a COUNT of required tables over *different* sets: the denominator excluded seven tables, the numerator counted triggers on all of them, and five of those seven carry one — measured 169 vs 164, **slack 5**. One through five required tables could lose their trigger with the check still green; six is what made it red, which is the only reason the `customer_account*` gap was ever seen. Replaced with an anti-join that tests each required table and **names** the ones missing. Regression: `scripts/tests/stamp-trigger-helper.test.mjs` proves the no-op by **trigger oid stability** (an oid cannot survive a drop-and-recreate) and requires red for exactly one missing table; 3 mutations, all caught. **A second review cycle then caught both predicates accepting a trigger that does not fire**: `tgenabled <> 'D'` also admits `'R'`, a replica-only trigger that never runs in the origin session mode application inserts use — one `alter table ... enable replica trigger` and the backstop is off while the helper skips the table and check 6 reads `ok`, the two safeguards agreeing on the wrong answer. And a bitwise `(tgtype & 4) = 4` requires INSERT without *rejecting* the other event bits, so `BEFORE INSERT OR UPDATE` passed as equivalent — a trigger that re-stamps a deliberately cleared `company_entity_id` on UPDATE instead of letting the write fail. Both predicates now require `tgenabled in ('O','A')` and exactly `tgtype = 7`; measured first, all 169 production triggers are already 7/'O', so the tightening is a no-op there. |
| 2026-09-20 | Multi-tenant | **Six `customer_account*` tables had no company stamp trigger** (`20260920140000`). `20260919140000` created them with `company_entity_id` and never called `attach_stamp_company_entity_id_triggers()`, so `verify_v2_schema.sql` check 6 read MISSING on every deployment-drift run from that merge onward — the row was there, red, and unread. Stated narrowly: the trigger is a BACKSTOP, not the mechanism. `submit_customer_account()` and friends set the company explicitly, so no row was mis-stamped; what was missing is the layer that catches an insert which forgets, on a table that carries client-side column privileges. Measured when re-run against production: 163 tables carried the trigger before, **169 after — exactly the six**, with `inventory_on_hand` and `sales_by_day` still excluded (the function skips them, so the 1.1M-row sales path never gains a per-row trigger). The five tables where the function's exclusion list is narrower than check 6's — the four `plaid_*` and `finance_audit_events` — already carried it, so for them this was a drop and recreate of an identical trigger, not new behaviour on finance records. `CLAUDE.md`'s "Adding a new DB table" checklist gained the step, so the next table does not repeat it. |
| 2026-09-20 | Security / Multi-tenant | **`entities` took client UPDATE from any member of a company, a viewer included** (`20260920130000`). Two permissive UPDATE policies, `entities_update_member` and `entities_update_access`, both resolved to `is_entity_member(id)`, which has no role filter — and RLS cannot scope to columns, so a member could rewrite its own company row's `title`, `entity_key`, `meta` and `entity_type` straight from the browser. `entity_key` and `meta.nav_profile` are exactly what `resolveNavProfile()` reads to decide **whose menu** a company is served, and `title` had just been made owner-only through `set_workspace_company_name()` — which was the right path and, until now, not the only one. **Bounded to the caller's own tenant** (`is_entity_member` is false for every other company's row, and isolation elsewhere keys on `company_entity_id`), so this is escalation *inside* a company rather than the cross-tenant hole `20260917220000` closed. Found by the verify check that shipped with `20260920120000`, the first time it ran against production — not caused by it. Nothing legitimate used the policies: every write is a SECURITY DEFINER function or the service role, neither affected by a policy or a grant on `authenticated`, and the only client references are reads. Grant revoked as well as policies dropped, so a future permissive policy cannot reopen it alone. `entities_insert_active_user` deliberately untouched: a row with no membership can never become anyone's active company. |
| 2026-09-20 | Billing / Payments | **A 1px screen-reader span was scrolling the whole Billing page sideways on a phone.** `v2/tests/browser/payments-ui.test.js` had been **red on `main`** on its "no page overflow on phone" check: at 390px, `documentElement.scrollWidth` measured 587. The obvious suspect was `table.bil-table`, whose min-content width is 650 — and it was innocent: `.pay-scroll` clips it correctly and every ancestor down to `body` measured exactly 390. The culprit was `.pay-sr-only`, absolutely positioned with **no offsets**, so it sat at its *static* position — inside the horizontally scrolled table, at x=586 — with the initial containing block as its containing block. `body`'s `overflow-x: clip` hid the paint but does not stop `html` counting it in its scrollable overflow. Pinned with `left: 0; top: 0`: 587 → 390. Two stale test counts fixed alongside, both red on `main` since Invoicing joined `ACCOUNTING_PAGES` without them moving: the destination count in `scripts/tests/accounting-suite.test.mjs` (7 → 8) and the Tab-order assertion in `transactions-filters` (`Sales & journals` → `Invoicing`). |
| 2026-09-20 | Settings / Platform | **Workspace Settings, and Silo Admin split out of it** (`20260920120000`, `v2/workspace-settings.{js,css}`, three new tabs, `/v2/platform-admin.html`). Settings was three unrelated places — Integrations in the sidebar, Billing commented out beside it, and `backend.html`, which mixed *this company's* people with the platform's "found a new company" control. Now one tabbed area (Company · Team · Integrations · Billing · Notifications) drawn by the same strip the Accounting Suite uses, over the **pages that already existed**: `/v2/integrations.html` and `/v2/billing.html` are the tab URLs, so no bookmark moved and neither page was rebuilt. One sidebar row replaces the per-page rows, because a row per tab beside a tab strip is two navigations for one place. **Silo Admin is a separate area with a separate gate**: its row is `requiresGrant`, visible only once a `platform_admins` row is read back — unlike `roles`, which fails *open* while the profile fetch is in flight and would have flashed a platform link at every user on every deep link — and `platform_list_companies()` is `is_platform_admin()`, deliberately not `is_admin_user()` (which passes for any membership admin) and not "any owner_admin". A company owner is not a platform admin. **Two things the Team tab needed that did not exist.** Changing a role meant `admin_update_profile()`, which writes the **global** `profiles.role`; that column is not per-company and gates read it alone (`can_manage_journal_entries()` admits on `p.role`/`p.department` with no reference to the active company), so an admin of company B could change what somebody may do inside company A — `set_workspace_member_role()` writes the membership always and the global profile only when the target belongs to no other org, the rule `accept_org_invite` already follows. And removing somebody from *one* workspace had no implementation: the nearest thing set the global `is_active` flag and locked them out of every company they belong to. Both refuse to leave a workspace without an owner, which nothing in the product could put back. Renaming goes through `set_workspace_company_name()` rather than an UPDATE policy on `entities`, because RLS cannot scope to columns and the policy would also hand the browser `entity_key` and `meta`. Email senders, Stripe, onboarding and notification behaviour are untouched. **Two findings from the independent review, both fixed and both measured.** The last-owner check was check-then-act across two DIFFERENT rows — the target membership is locked, the owner count is not — and self-demotion is allowed on purpose, so two owners each stepping back shared no row lock at all: two real connections left the workspace with **zero** owners, a state nothing in the product can undo. Both functions now take one shared per-company `pg_advisory_xact_lock`, with `scripts/tests/workspace-settings-concurrency.test.mjs` driving the interleave and a mutation removing both locks required to reproduce the damage. And the Notifications tab printed the fallback sender as if it were the configured one — ten functions read `SILO_MAIL_FROM`, a secret no browser can read, so the one screen for checking notification configuration would have been confidently wrong on any deployment that set it; it now names itself as the fallback and says the live value is not readable there. |
| 2026-09-19 | Billing / Payments | **Stripe, twice: SILO's own subscription revenue, and Connect so a client can invoice their own customers** (`20260919120000`, four edge functions, `/v2/billing.html` and `/v2/invoicing.html`). The two share a vendor and nothing else — SILO is the merchant on one and the tenant is the merchant on the other — and almost every way this goes wrong is a confusion between them, so the separation is structural rather than conventional. **Connect is Standard**: the client owns the Stripe account, keeps their own dashboard and carries their own dispute liability, and SILO stores **no per-tenant credential at all** (platform key plus a `Stripe-Account` header). **Every table is a read-only mirror** — not one INSERT/UPDATE/DELETE policy on any of them, and no write grant — because a client-writable row would be an invoice SILO claims to have sent and Stripe has never heard of; the same stance Card Coding takes toward QuickBooks. **One webhook function, not two**: both endpoints deliver the same event types (`invoice.paid` is a tenant paying SILO on one and a tenant's customer paying them on the other), the endpoint is decided by *which signing secret verified the signature*, and a platform event carrying a connected account **raises** rather than being attributed — that is what crossed endpoint secrets look like, and attributing it would file a client's own sales as SILO revenue. **Out-of-order deliveries cannot un-pay an invoice**: every handler re-fetches and stamps the fetch time, the sync functions return early on anything older, and a BEFORE UPDATE trigger repeats the test for a writer going round them (the service role bypasses RLS, so a policy could not be that backstop). **A lost response does not invoice a customer twice**: a caller-minted `request_id` is recorded before Stripe is called and a repeat returns the first attempt's invoice — Stripe's own `Idempotency-Key` only covers a retry that reuses the key, not the reload that mints a fresh uuid, which is the retry that actually happens. Amounts are integer **minor units** with the currency beside them (`numeric(14,2)` is a rounding step at every boundary and is simply wrong for JPY), and the one conversion from what somebody typed lives server-side in one module that refuses three-decimal currencies by name rather than mis-scaling them by ten. `can_manage_client_invoices()` is deliberately narrower than `is_admin_user()` (28 of 29 profiles are membership `admin`). Found while writing the schema check: Supabase's default privileges **GRANT ALL on a newly created view**, and a simple view is auto-updatable — so the SECURITY DEFINER `stripe_connect_status_v` was writable by any authenticated user with RLS bypassed until the explicit revoke was added. 63 assertions across two suites, eight mutations, all in `sync-tests.yml`. **Nothing has run against live Stripe yet** — see the unverified list in [stripe.md](stripe.md). |
| 2026-09-18 | Multi-tenant | **Company creation was open to anyone with the public anon key, and is now invite-gated at the trigger.** `handle_new_user` provisioned an entire tenant — `entities` row, `owner` profile, `owner_admin` membership — from an `org_name` key in the signup metadata (20260714190000). `signUp` is a public Supabase Auth endpoint and the anon key ships in `pages/config.js` by design, so that key was caller-controlled input: anyone could found a SILO tenant on this project's Supabase and Anthropic quota. Removing the organization field from `pages/login.html` would have been theatre — the form was never the boundary. The founding branch is deleted; founding now runs through `redeem_platform_invite`, gated by a `platform_admins` table seeded with Blake alone (deliberately not "any owner_admin": 28 of 29 Baseballism profiles are membership `admin`, the same blast radius that made the comp-request gate diverge from the AP gate). **The redeem is idempotent, not merely atomic** — atomicity is free in a definer function, but it does not cover a committed row with a lost response and a user who clicks again, so the invite stores `created_company_id` and a repeat returns that company with `repeated = true`. **Business timezone now reads from the company record** (`silo_business_today()`/`_yesterday()`, the change 20260904280000 said was needed) — but ten further public functions and seven files under `scripts/`/`v2/` still hardcode `America/Los_Angeles`, so onboarding **refuses** a non-Pacific timezone and names what does not honour it, rather than storing a setting that reads as configured and is not. **Declared currency is reconciled, not merged**: `accounting_settings.base_currency` is derived from QuickBooks' own trial balance and its row cannot exist before a realm is connected (`qbo_connection_id` is NOT NULL), so `company_settings.default_currency` is a separate fact with a trigger that raises on divergence. Also dropped `create_entity_with_owner`, which **had never worked**: it inserts membership role `'owner'`, forbidden by the CHECK since the multi-tenant work, so every call failed `23514` and rolled back its own entity insert (measured on production before removal). New: `/v2/company-onboarding.html`, `/v2/setup-checklist.html`, and `SILO_MAIL_FROM` so a prospect's invite does not arrive from a Baseballism address. `20260918120000`; `scripts/tests/company-onboarding-database.test.mjs`, 46 assertions and twelve mutations, which also runs the four new verify checks and then breaks each guard to confirm they can go red. **The three claims that suite structurally cannot make are now measured too** — PGlite is a single connection, so it can prove a guard refuses a bad state but never that two sessions racing cannot assemble that state between them. `scripts/tests/onboarding-concurrency.test.mjs` interleaves two real PostgreSQL connections and observes the block in `pg_stat_activity` rather than inferring it from timing. Removing each lock produces the damage it prevents: without the per-company advisory lock a company commits with its books seeded in USD under a CAD declaration; without the profiles `FOR UPDATE` an administrator's deactivation is undone by the redeem it raced; without the invite-row `FOR UPDATE` one invite founds **two** companies — a tenant the founder owns and never asked for. All three were argued from the source across four review cycles and none had been observed. CI runs the suite against a `postgres:16` service and requires each of the three mutations to go red, so a lock cannot be dropped later and leave a green suite behind. **One more recovery bug, found by the cycle-3 review:** pausing company CREATION also blocked RECOVERING a company that already existed. The zero-supported-timezone gate on `/v2/company-onboarding.html` was an unconditional return sitting above the accepted-invite handling, so the exact sequence the whole idempotent-retry path exists for — the company commits, the response is lost, the founder reopens the same link — met "Company creation is paused" for a company already sitting there, with no way through. A company that already exists already has a timezone; it chose one when it was founded. Recovery now runs before that gate, offers a Continue action, and redeems through the server rather than trusting the peek, which re-runs the account, email and membership checks. The page sends NULL for the three creation inputs because the retry branch returns before validating any of them — an ordering nothing had stated out loud, now pinned by a test that also proves a FIRST-time redeem still rejects the same nulls. The page regression is DRIVEN, not grepped: the broken and fixed pages differ only in the order of two blocks, which a source-pattern assertion cannot tell apart, so the page's own IIFE runs against a synthetic DOM and the mutation reproduces the reported symptom verbatim. |
| 2026-09-16 | Ask SILO | **A traced Sonic investigation ended without the measure it was built around, and the record says guidance was not the missing piece.** Replayed from `silo_chat_audit_log.diagnostics` (`b44e03ab…`) rather than re-derived — which is what that column was added for. 13 queries, **18.3s of database time inside a 138s request**: the budget went on model round-trips, not SQL. (1) The statement that would have isolated Sonic ad spend ran last as `min(date)` against `meta_ad_performance_daily`, whose column is `day_date`, failed in 110ms, and the loop was already past the wall-clock guard — so no round remained. That same request had called `describe_relations` **on that table** and been handed every column with its type, the schema map says "never guess a column that isn't listed", and the 45s investigation checkpoint had fired: three interventions, all upstream, all ineffective. `annotateColumnError` was five hand-written regexes with no entry for this and no way to have one; it now answers from the catalog (real columns of the relations the statement reads, closest-by-name first), and ONE round is held back to use it — at most one per request, never after a statement timeout (re-running the same heavy statement is not a correction), only inside the gateway margin. Granting it consumes the investigation checkpoint, because the two say opposite things and a handler test caught them landing in the same round with the correction *not* read last. (2) The sales result's envelope said `pooled_across: location_tag`, no statement in the request restricted channel, and the answer published "14 Sonic products, online sales_by_day" — wrong channel *and* wrong source, with the prompt rule against exactly that already in place. `auditAnswerClaims` now checks the finished answer against the envelopes it was written from and appends a visible scope note; it **annotates and never rewrites**, because it is a word search over prose that is wrong in both directions. (3) The planning record answered the launch date and showed `preview_start_date` NULL on every Sonic row — it does not say when prelaunch began — and the sales query then ran `between '2026-08-01' and …`, so every "before the launch" figure rested on an 1 August that appears in no earlier result. Date literals now carry `boundary_provenance` (`from_results` / `from_question` / `unsourced`). Deliberately out: `campaign_name` as a claim dimension ("the campaign" is too common to flag without crying wolf) and `totals_only` as pooled (`meta_ad_performance_daily` and `marketing_daily_totals_v` both lack a platform column; every row in one is Meta's and every row in the other spans three, and the envelope cannot tell them apart). 53 new deterministic assertions across three suites (53 + 97 + 67 = 217 total); **18 mutations, every one caught** — three earlier versions of these tests passed vacuously and were rewritten after mutation testing exposed them, and two dead branches of my own (a `!timedOut` guard and a second regex alternative) were deleted rather than left credited as coverage. **Cycle-1 review corrected two P1s, one of them a test of mine pinning the wrong behaviour:** a dimension another query resolved is still unresolved for the POOLED one (ask-a-total-then-split silently disabled the whole check), and the correction round's fixed 115s cutoff overshot the 150s gateway by 7s at the measured 17.1s/model-call while never firing on Sonic anyway (~121s elapsed at its boundary). The reservation is now measured per request in `budget-lib.mjs` -- extracted because two handler mutations survived within a millisecond of the boundary, and driving real elapsed time cannot pin a formula. Honest consequence: the correction round fires for a request that reached the budget through cheap rounds and not for one that crawled there, Sonic included; F1's catalog hint is the half that works regardless of budget. **Filed, not fixed (bugs.md):** `chat_run_readonly_query`'s declared 30s statement timeout has never governed — `SET LOCAL` cannot re-arm the running statement, so `authenticated`'s 8s wins. Measured through the RPC (a 10s query passes under a 20s caller timeout and is cancelled under a 4s one) and confirmed across all 54 logged production queries: every timeout at 8.1–8.3s, longest success 7.8s. **Cycle-2 review found two more, both fixed:** the reservation is an *admission* check and nothing made it binding on the two calls it reserves for -- `callAnthropic`'s fetch had no abort signal, so a grant admitted on 15s samples whose correction and forced final each took 25s reached ~155s and was killed by the gateway with no answer and no audit row (ordinary latency variation, and the fixed-latency tests could not show it, because a fixed latency makes the estimate right by construction). `modelCallTimeoutMs` now puts an `AbortSignal.timeout` derived from the 140s safe line on every post-budget call: a correction reserves the query plus the forced final, the final reserves nothing, a cut-off correction loses the round rather than the request, and a remaining budget of 0 raises instead of going out unbounded -- reachable with no correction at all, since one ordinary round starting at 94s and running 50s arrives there with the line already spent. And the mixed-scope note contradicted its own cycle-1 correction: the per-flag clause was fixed, the shared closing sentence still said the label "was not established by anything that ran", so an answer correctly citing the online-only figure as "online" was told the results were mixed and then that no query established the label. The closing branches now. A guard for a state the constants cannot produce was deleted rather than left as uncovered defensive code, with the invariant asserted in `budget-lib`'s own suite instead. Because both fixes landed after the second review, the PR's final status is **needs additional independent review**. |
| 2026-09-16 | Ask SILO | **Correct SQL, wrong labels: a figure arrived at the answer with no record of what it was scoped to.** Two traced answers (`c0b642ca…`, `7c90b2cd…`). (1) A week of ad spend summed from `marketing_daily_totals_v` — a view with **no platform, campaign or ad column at all** — was published as *"Meta ad spend … $118,946"* and paired with Meta's own attributed value and a 1.01 ROAS. Re-measured: Meta $114,334.99, Google $4,610.92, combined $118,945.91. The per-platform split was returned by the VERY NEXT query in the same request and was not used. (2) A weekly bucket running 31 Aug – 6 Sep was called *"the following week … post-launch"* against a 1 Sep launch; all $17,500 of its spend went out on 31 August, pre-launch, while all $85,120 of value landed after — and the 4.86 that produced became the first recommendation. (3) Ads selected by `meta_ad_creatives.body ilike '%sonic%'`, with no campaign predicate, held $25,488.06 in Purchase Campaigns and **$0.00** in Subscribers over 1–7 Sep and were described as one campaign moving off lead-gen. (4) Cutting subscriber-acquisition spend was recommended on immediate purchase ROAS, in an answer that elsewhere correctly says no subscriber-to-order linkage exists. None is an arithmetic bug and none is fixed by prompt text alone: after nineteen rounds the model is reading a wall of anonymous JSON arrays and has to REMEMBER which was all-platform. **So it no longer has to.** `run_sql` now returns `{ evidence_scope, rows }` — derived by `evidence-scope.mjs` from the statement plus the catalog's `pg_catalog`-generated column lists: which scope dimensions are narrowed (with their literals), broken out, or POOLED; which relations carry none of them and are therefore already totals; the date window and every bucket edge; and whether the 1000-row page cap was hit. It describes the QUERY, never the values, and reports a dimension as pooled when it cannot tell, so pooled is the safe reading. A sixth tool, `describe_relations` (6 relations/call, 3 calls/request), fetches full cards mid-investigation — the detail slice is ranked on the OPENING question's words and frozen for the request because it IS the cached prompt prefix, and an investigation moves. **Coverage is measured, not remembered**: the Meta ad-level card said "about 7 weeks of history (from 2026-07-08) … do not use it for launch comps" over a table holding **415 days back to 2025-07-28**, so the sentence is gone and NOT replaced with a fresher range — `verify_v2_schema.sql` goes CRITICAL if any hardcoded range returns. The budget-exhausted instruction no longer says "instead of refusing to answer": it asks for supported findings AND named unfinished checks, and forbids promoting an observation to a recommendation to fill the shape of the question (`partial`/`partial_reason` on the response). `silo_chat_audit_log.diagnostics` records per-query outcomes and context selection — **never result rows**, size-capped before the insert, and the insert retries without the column so function and migration apply in either order. 74 new deterministic assertions across three suites (36 + 24 + 14; 157 total), every fix mutation-tested; a model evaluation with a baseline control arm lives in `evals/` and is deliberately NOT in CI. `docs/ops/ask-silo-evidence-scope.md`. |
| 2026-09-16 | Marketing | **Destination coverage was bounded by the nightly's window, not by Meta — a backfill reaches the rest.** The nightly asks Meta about creatives only for ad ids with insights rows in its trailing window (`days_back ?? 30`), so measured 2026-09-16 **126 of 4,079 stored creatives had ever been asked about**, 82 resolved, and **$5,304,686 of SHARE spend sat on ads never requested at all** — not refused, never asked. `scripts/meta-creative-backfill.mjs` + `meta-creative-backfill.yml` (manual, job_type `meta_creative_backfill`, migration `20260916150000`) walk the stored creatives and can also enumerate the account's own ad ids to reach ads that never landed here. It **only ever adds**: a resolved link, recovered page-post copy, or a full row for an ad with none — re-asking about an old ad can never blank a destination the nightly resolved or copy the page-post pass recovered, because the writes are partial column sets and `ON CONFLICT DO UPDATE` touches only what it names (proven against a real Postgres, not assumed). Each chunk is written before the next is fetched, so a rate-limit at ad 3,900 keeps 3,899 ads of work and the default `missing` mode makes a re-run a resume. 22 new assertions across a core suite and a suite that executes the DRIVER itself — the core passing alone is the exact state in which a dead-zone reference shipped in an orchestrator callback here on 2026-09-09. |
| 2026-09-16 | Marketing | **Ad destinations resolve — 82 of 126 — and the measurement inverted two rounds of guessing.** First clean run: `[meta] creative links: 82/126 resolved (asset_feed=82) [asked: object_url,url_tags,asset_feed_spec, refused: effective_object_url]`. Every resolved link is a real `baseballism.com` collection or blog URL; **zero** are Meta-hosted. Two beliefs this repo had written down were wrong: `effective_object_url` was called "the only source a page-post (SHARE) ad has" and is in fact **refused by this account as an unknown field**, while `asset_feed_spec` was called "the riskiest" and supplies **every** resolved destination — all 82 resolved ads ARE `SHARE`. Acting on that: `effective_object_url` is no longer requested or read (it cost a retry and a `[warn]` per run and could never resolve anything), and `CREATIVE_DROP_ORDER` is inverted so an unnamed refusal drops `url_tags` first and `asset_feed_spec` last — under the old order it would have discarded every destination the account has. No behaviour change on a NAMED refusal, which is the path production actually takes; this is the fallback being corrected against evidence instead of a guess. The 44 unresolved (24 VIDEO, 19 SHARE, 1 PHOTO) render "destination not resolved" with per-group coverage, never a blank. Separate finding for marketing: **`url_tags` came back null on all 82 — no UTMs on any ad.** |
| 2026-09-16 | Ask SILO | **A SELECT is not a read, and `chat_run_readonly_query` only ever checked that a statement LOOKED like one.** `select public.set_active_company('<an entity the caller belongs to>')` is a single semicolon-free SELECT that passes every guard the function had and UPDATEs `profiles.active_company_id` — the column every RLS policy in SILO reads to decide which company's rows the caller sees. `set_active_company` is granted to `authenticated`, so it was reachable from Ask SILO's query tool, the report builder preview and every `/v3/` widget. The function now enters a read-only transaction, so the refusal is the EXECUTOR's and holds however the write is reached. **Two plausible fixes were tried first and both fail in ways that read as correct in review**, which is why there is now a database test pinning them: a function-level `set transaction_read_only to 'on'` is rejected outright by Postgres (`cannot be set locally in functions`), and declaring the function STABLE — the obvious move, since PostgREST runs a STABLE function read-only — was measured letting a nested VOLATILE function write anyway (SPI's non-volatile guard is per function) while also making `set local statement_timeout` illegal, silently dropping the 30s budget |
| 2026-09-16 | Ask SILO | **An answer cut off by the output-token limit lost its opening.** The handler correctly refused to ship a truncated fragment as finished and asked the model to continue — then returned ONLY the continuation, in both the main loop and the forced-answer path. The user got an answer starting mid-thought with its figures missing, and the audit row recorded that same headless text as the answer given. It bit hardest on the longest answers, which are the ones carrying their evidence up front. Segments now accumulate, with raw whitespace preserved across the seam (trimming each half glues `The` to `biggest`), and prose the model abandons to run another query is reset rather than pasted onto the front of the eventual answer |
| 2026-09-16 | Ask SILO | **A rejected audit-log INSERT was indistinguishable from a successful one.** supabase-js resolves with a RETURNED `error` object rather than throwing, and the only handling was a try/catch — so an RLS or constraint rejection wrote nothing, said nothing, and left `silo_chat_health_v` computing a reliability score over a log with holes in it. Insert errors are now checked, logged with their code, and reported to the page as `audit_logged: false` so it does not promise a crash-recovery it cannot perform |
| 2026-09-16 | Ask SILO | **Crash recovery matched on question TEXT, which is not an identity.** When a fetch died but the edge function finished, the page looked up the newest `status='ok'` row whose `question` equalled the last user message — and a conversation is mostly short replies ("yes", "keep going", "now by month"), so a dropped request could recover an EARLIER turn's answer and present it as the reply to this one. Worse, the select policy on that table is `created_by = auth.uid() OR is_exec_or_owner()`, so for an exec the match was not scoped to their own rows at all. A request now carries a client-minted `request_id` (`silo_chat_audit_log.request_id`) and recovery asks for that row and that reader. The old six-minute recency window went with it — it was cutting off exactly the long drafts recovery exists for |
| 2026-09-16 | Ask SILO | **A tab's conversation was not bound to who was in it.** History lived under one fixed `sessionStorage` key with no identity check, and a tab outlives both a company switch and a sign-out/sign-in — so the previous identity's conversation was replayed into the log AND posted back to the edge function as context. Storage is now keyed by user and company, the stored blob repeats both so a mismatch is refused rather than rendered, and foreign conversations are purged on boot. Separately, the company a question is asked from is now verified server-side at the start of a request and again before the answer goes out: RLS scopes every read through one mutable per-user field while a single request makes many calls over a minute, so a switch in another tab used to land between two of them and silently mix two tenants' rows. **A WRITE is bound to the starting company by the DATABASE, not by a check.** The delivery check runs after the tool loop, and a note or concept saved mid-request is stamped with the NEW company and committed long before the answer is discarded — refusing to deliver does nothing about a row already sitting in another company's data. A pre-write check is not enough either, and the first version of this fix was exactly that: the check and the write are separate round trips (`create_product_concept` even runs a duplicate-title lookup in between), so it can always be raced. Every write now sends `company_entity_id` EXPLICITLY — `stamp_company_entity_id` only fills the column when it is NULL, so the value survives and the insert policy's `company_entity_id = active_company_id()` refuses the row outright if the active company has moved. Updates cannot be refused by a WITH CHECK they still satisfy, so they are ADDRESSED by company instead and a write matching no row is an error, never a silent no-op. The pre-write check stays, demoted to what it is good at: turning the common case into a sentence a person can act on rather than a raw RLS rejection. Verification failing is a refusal, not a shrug: the lookup is retried once, and a company that still cannot be established refuses the request rather than skipping the comparison, which is the same outcome as not checking at all. On the page, a question that failed with no answer stays in the conversation so Try again can resend it — but typing a DIFFERENT question instead now removes it first, since two consecutive user turns in one request let Ask SILO answer the abandoned question or blend it into the new answer |
| 2026-09-16 | Ask SILO | **Answers read like a query log, and offered things the chat cannot do.** Measured over 30 days of `silo_chat_audit_log`: answers opening with the view and filter they came from instead of the figure; an answer saying a saved report was "updated" and offering to overwrite it by re-saving under the same name (which creates a SECOND report); answers offering downloadable files, which no part of this chat can produce; and a "conversion rate" dividing a product's total orders by that product's landing sessions, noting one row at 144.7% and treating the rest as valid. The shared prompt now carries a business-language contract, an explicit list of what the chat cannot do, and a rule that a query running is not evidence the metric means what its name says. Also removed one company's voice and protected tagline from the shared multi-tenant prompt — brand identity is `silo_chat_notes` data, never a constant in the edge function |
| 2026-09-16 | Ask SILO | **Web search citations were dropped on the way out.** Answer assembly maps content blocks to `b.text` and discards the rest, so a web-sourced claim arrived beside queried numbers with nothing to tell them apart — the exact blending the prompt's internal-vs-public rule exists to prevent. The pages the search returned are now collected from both places Anthropic puts them and rendered beneath the answer, separately from the query panel |
| 2026-09-15 | Products | **Four fixes on `/v2/products.html`.** (1) **Caps carry a youth snapback and an adult one**, where a single `Snap` chip could not tell them apart — the POs order them as separate products (`Beast Mode Cap - Youth` and an adult snapback both ship a variant literally called `Snap`), so a size request for a youth cap read identically to an adult one. Every other cap size is a fitted size and is adult-only, which is why only the snap row split. The 14 rows already storing a bare `Snap` are neither rewritten to one of the two nor dropped: which they meant is not recoverable, so the chip is still rendered on those rows, labelled `Snap (legacy)` and dimmed, and is never offered on a new sample — same treatment the pre-split `received` status gets. Dropping it would have deleted that data on the next save of the row. (2) **A PO / Incoming result can be clicked and saved.** `v_launch_po_product_lookup` has no `id` column at all — it is grouped by (PO, product title) and keys on `po_header_id` — but the picker read `r.id`, got undefined, and set `product_master_id=''`: the click appeared to do nothing, and nothing was stored. A PO now lands in `po_header_id` (already on `product_samples`; added to `product_tracker` by `20260915230000`), never in `product_master_id`, which is a different table's key. The pick also fills the title/type/factory/ETA it left blank, and a green PO chip sits beside the search so the link is visible where it was made. (3) **Expected Units is the whole PO, not one size.** It was labelled "(from the originating PO)" and had never been filled by anything, so it was typed by hand a line at a time. It now comes from `total_units`, which the view already sums over every size line for that product on that PO — a 6-size tee reads 325, not the 75 on whichever line was read first. The figure is shown in the picker with its size-line count *before* it is picked, so it is checkable against the PO rather than taken on trust; a typed title is left alone, but the PO-owned number does follow the PO. (4) **The long filters are searchable.** 141 factories, every PO, every launch and ~130 product types were native dropdowns. Each is now a type-ahead over a real `<select>` that stays in the DOM and stays the source of truth — so `fillSelect`, the `?photo=` deep link, `ctFiltered()` and `notifyAssignee`'s `selectedOptions` were untouched and a pick still fires `change`. Short fixed-choice filters (statuses, photo status, has-samples) stay native: a search box over four options is worse than the dropdown it replaces. A cut list says how many it is showing; abandoning a half-typed search puts the real selection back rather than leaving stray text where a value should be. The page **feature-detects** `product_tracker.po_header_id` and omits it from the write until the migration is applied — sending an unknown column fails the whole update, which would turn "the PO link is not stored yet" into "nothing on this page saves" — and the chip says so rather than claiming a link the save cannot make. |
| 2026-09-15 | Marketing | **The destination feature resolved NOTHING on its first real run, and the coverage log is what caught it.** Measured the moment it shipped: `[meta] creative links: 0/126 resolved (none) — link fields REFUSED by Meta this run`. The cause was a design error in the fetch, not in the account: `effective_object_url`, `url_tags` and `asset_feed_spec{link_urls}` were requested as ONE tier, with an all-or-nothing downgrade. `asset_feed_spec{link_urls}` is the risky member — nested subfield selection on a field not every ad account exposes — and when Meta refused it the code dropped all three, including `effective_object_url`, the single field 63% of this account depends on because a page-post (SHARE) ad carries no `object_story_spec` of its own. Every one of the 126 ads in the window fell back to a source it does not have. **The fetch now keeps an ACTIVE SET and removes exactly the field Meta NAMES**, so a bad field costs only itself; an ordered drop list is the fallback for a refusal that names nothing, and a refusal naming a subfield (`link_urls`) maps back to its parent. The first attempt at this fix was a fixed six-step ladder that always dropped `asset_feed_spec` first and used the named field only for logging — which had the SAME defect one level down: refusing `object_url` retried the same bad field, then dropped `asset_feed_spec` for nothing, emptying any Dynamic Creative ad whose only destination is `asset_feed_spec.link_urls`. Caught in review, not in production. `object_url` is requested alongside `effective_object_url` and dropped separately, since which of the two a given API version accepts is not knowable from here and asking for one is a coin flip on most of the account. The log now names the tier the run settled on and the field Meta named, so the next refusal is diagnosable instead of re-guessed — the first one was not, because the failing member of a three-field tier was never identified. Three new assertions, including one that replays the exact production failure, and a `tier-collapses` mutation that restores the shipped defect and must fail the suite. Two test-fidelity bugs found while writing the regression, both of which made assertions pass for the wrong reason: the fake omitted `object_url` from the fields it strips when unrequested (so it served a field the request never asked for), and both the fake and the assertions matched field names by SUBSTRING — `effective_object_url` contains `object_url`, so a fake asked to refuse `object_url` went on refusing after it had been dropped. Both now match whole tokens. Also fixed a DEAD mutation hook: `no-item-check` pointed at a string the refactor had removed, so it was failing the did-it-apply guard and reporting as "caught" — a test credited as coverage while guarding nothing. |
| 2026-09-15 | Marketing | **Where a Meta ad actually sends the customer.** Asked for directly (Sammie): see the landing page an ad takes the customer to. `meta_ad_creatives` carried the thumbnail, the copy, the format and the status and nothing about the destination, so the Marketing Report could show what an ad LOOKED like and never where it pointed, and no query could line an ad up against `shopify_landing_pages_daily`. The sync now resolves a destination from seven sources in priority order and records WHICH one answered (`link_url`, `link_url_source`, `link_url_tags`, plus a generated `link_path` for joining to `landing_page_path`). **The account's shape decided the design**: 2,565 of 4,079 stored creatives are `object_type = SHARE`, page-post ads that carry no `object_story_spec` of their own -- resolving from the spec alone would have covered a third of the ads and read as "most of our ads go nowhere" -- so `creative.effective_object_url` is requested too. That source is LAST and is never treated as authoritative: on a page-post ad Meta may resolve it to the Facebook post, which is why the source is stored beside the URL and the report shows the HOST, marking a Meta-hosted destination in amber. The new fields are a separable TIER with drop-and-retry, because these reads go through the Graph batch API where an unaccepted field returns 200 outside and fails every item inside -- silently, in a loop that skips non-200 items -- and would otherwise have written thousands of rows with no copy and no links, warning about nothing. A blank destination renders "not resolved" and each objective group says how many of its ads resolved one; absence is never a claim that an ad goes nowhere. **Coverage is unverified until the first nightly**: no probe stands behind it (a dispatchable workflow cannot run from an unmerged branch), so the sync logs resolved/total by source on every run and that line is the measurement. Also reconciles `wow_creatives`, which production was running AHEAD of this repo with thruplays/leads that no migration contained -- a rebuild from the repo file would have deleted the Thruplays and Subscribers headlines silently (bugs.md). 37 assertions over three suites, seven mutations. |
| 2026-09-15 | QBO history | **Archive a fiscal year with one button, because a fiscal year is the only window QuickBooks can actually check.** Measured 2026-09-15: **QBO's TrialBalance ignores `start_date`** — two runs against the same end date asking for `2025-08-01` and `2026-01-01` returned BYTE-IDENTICAL rows and differed only in the echoed `Header.StartPeriod`. Its P&L column is always fiscal-year-to-date, so a ledger window crossing the fiscal-year boundary archives every line correctly and then disagrees with the trial balance on every income and expense account. On the 2025-08-01 → 2026-07-31 window that read as 145 exceptions and $54.4m affected; applying each account's direction, **63 of the 64 differences are exactly that account's own August–December 2025 activity, residual $0.00**, and zero balance-sheet accounts moved. All 36,778 lines were correct. `/v2/accounting-books.html` now offers the archivable fiscal years as buttons, each resolving to its own window with the most recent clamped to the day before the accounting start date, showing what is already saved and what a partial window overlaps. The unreconcilable window is now unreachable rather than refused after the fact; manual dates remain, and both paths run one shared archive flow. **This corrects the causal story in the previous row**: the page's fiscal-year-start request was not the cause, so `20260915210000`'s `Header.StartPeriod` check cannot fire for a trial balance (logged P3 in bugs.md). |
| 2026-09-15 | QBO history | **A twelve-month archive was checked against a seven-month trial balance, and reported $54.4m of exceptions over history that was completely intact.** QBO's trial balance is period-scoped -- a balance-sheet account reports as-at, an income or expense account reports ACTIVITY for the range -- and `/v2/qbo-history.js` requested it from the FISCAL YEAR START rather than the ledger's start. Every window anyone had tried began on January 1, so the two coincided and every archive reconciled; the first window to cross a fiscal-year boundary (2025-08-01 → 2026-07-31) compared twelve months against seven. Result: 63 P&L accounts out by $33.3m (Income 35, COGS 5, Expense 17, Other Expense 6) and **zero** Bank/AR/AP/Credit Card accounts out -- the signature of a period mismatch, not lost data. All 36,686 lines were archived correctly; only the verdict was wrong. Both halves fixed: the page now requests the trial balance for the ledger's own window, and `20260915210000` makes `archive_qbo_ledger` REFUSE a pair whose stored periods differ or whose `Header.StartPeriod` disagrees with the ledger's start (the columns say what was asked, the header says what QBO answered). Verified read-only across all 22 stored report windows: every run carries `Header.StartPeriod` equal to its `start_date`, so nothing existing is refused. The UI test had been PINNING the defect (`assert.equal(fetch[1].body.params.start_date,'2026-01-01')`) and the trial-balance fixture carried no `StartPeriod` at all, which is why neither caught it; both corrected. Mutation `accepts-mismatched-tb-period`. Verify gains two rows. |
| 2026-09-15 | Books & setup | **Books & setup opens on a migration flow that is read from the books, not ticked by a person.** Five steps across the top of the page — banks and cards connected, opening trial balance stored, QuickBooks history retained, coding and rules in Silo, revenue journals posted — each recomputed on every load from the rows that would have to exist for it to be true. There is no table recording "we finished step 3", so the strip cannot drift away from the ledger. **Unknown is not Done, and that is the whole design.** Facts are gathered independently and a read that fails becomes `UNMEASURED`, which is a different value from measured-and-absent: a table that errors degrades its own step to "Unknown" (rendered as a word, not a colour), leaves the other four alone, and the flow points AT it rather than stepping over it to reach a later "Done". **Every step also says what it does not establish** — retained history proves QuickBooks' reports agree with each other, not that invoices and attachments were kept; connected feeds name the accounts that reach Silo, not how many the company holds; a month with posted entries is not a month with every revenue source in them. History coverage is judged against the accounting start date and nothing else: the saved windows are merged (overlapping and adjacent ones are one period of coverage), any gap up to the hand-off is named with its dates, and how far BACK history should reach is reported rather than graded, because that is a business decision Silo cannot read. **One real bug found by its own test:** exception and line counts were summed across snapshots, so Baseballism's two saves of the same January–July window read as "142 account exceptions" and 45,818 lines when there are 71 and 22,909. Figures now come from one snapshot per distinct window (newest wins), then from a non-overlapping selection (widest wins), and the panel says when a snapshot was set aside. `uncoded` is read from `coding_source`, never from a null `qbo_account_id`, or every split row would be reported as waiting to be coded. The month in progress is never graded as missing. **A second real bug, in CSS:** `.is-open` set `border-color`, a shorthand, which wiped the state rail off the one step a reader is actually looking at. `v2/migration-status.js` holds `assess()` as a pure function over plain facts; no migration, no new grant, and nothing on the page is written. Tests: 34 unit assertions over the decision table (8 mutations, each failing a test) and 17 browser assertions against the real page booting the real module (5 mutations), including the strip reflowing 5 → 2 → 1 columns without scrolling sideways and each step painting the rail its state declares. The harness gained `.is()`, `count`/`head`, a real `.limit()`, and a `broken` option so "what does the page show when a read fails" is testable at all. |
| 2026-09-15 | QBO history | **The archive refused the full-year import over a QuickBooks housekeeping section that holds no money; it now keeps those rows instead.** With the bounded archive applied, the real import stopped on `Unsupported or duplicate ledger account section`. Measured on the stored reports: exactly one leaf section in each window has no account id (209 sections / 36,778 rows in the full year, 193 / 23,002 in the half year) and there are ZERO duplicate ids, so `duplicate` was never involved. That section is QBO's `Not Specified` group, 24 rows, every one a Journal Entry for `.00` or a Payment with a blank amount reading `Created by QB Online to link credits to ...` -- records QuickBooks generates to link credits, which nobody can assign an account to, so the refusal made that window permanently unarchivable. `20260915200000` archives the section rather than skipping it: every row kept under `silo:unattributed` (no collision with a numeric QBO id), typed `Unattributed`, named in the reconciliation under its own `unattributed_ledger_section` issue with a null difference since it has no trial-balance counterpart. **An account-less section carrying an actual amount still refuses the whole import** and names how many rows have one -- that is a bookkeeping problem for a person to fix in QuickBooks, and filing it under a placeholder is the silent mis-attribution the archive exists to prevent. Nothing truncated, no row dropped, on either path. The zero section is deliberately not counted in `exception_count` (it is provably zero, the books tie, and an exception on every archive is one nobody reads); both guards are marked in the reconciliation block if that should change. **Cycle-1 review correction, same day.** The placeholder has no trial-balance counterpart, so the comparison is skipped for it and whatever admission admits is never checked again -- which made the amounts-only admission test the whole safety property, and it was too narrow. A `Beginning Balance` row carries a blank amount and a real running balance, so a section with a $250 closing balance passed, kept that balance under the placeholder and reported `matched`; and the exemption from `exception_count` covered EVERY problem on the section, not just the intentional notice, so a running balance gap or a disagreeing period total there also read as `matched`. Admission now requires amount cells, running balance cells, the period total AND the section's ending balance (`Summary.ColData[7]`, `rbal_nat_amount` -- cycle 2 found this one: a section can report zero movement in column 6 while reporting a balance carried out in column 7, which a real account catches as a `trial_balance_mismatch` and the placeholder never would) all present and all blank or zero, and only `unattributed_ledger_section` is exempt from the exception count. **A second defect found while verifying it against production:** of the seven stored windows, four carry exactly one row with BOTH cells blank and were failing on `Missing running balance` rather than on the account-less section -- so the original fix would have unblocked only the window that was reported. A blank running balance now reads as zero on the placeholder (where admission has already proved the section is all zero) and stays a hard refusal on a real account. Tests: the zero section archives with its rows and reconciliation entry, a section with one $250 row refuses and writes no evidence, a non-zero beginning balance refuses, zero movements against a moving balance refuse, a disagreeing and an absent period total refuse, a both-cells-blank row archives as zero, and a real problem on the placeholder counts as an exception; a non-zero and an absent section ending balance refuse; mutations `absorbs-unattributed-money`, `admits-unattributed-balance`, `admits-unattributed-ending-balance`, `exempts-unattributed-problems`. Verify gains six rows inside `QBO history bounded archive`. |
| 2026-09-15 | Accounting Suite | **Every dialog in the Accounting Suite was see-through, and one missing token was the whole cause.** 57 rules across the suite set a background, a sticky header or a popover from `var(--bcn-panel)` with no fallback -- and `--bcn-panel` is defined in no stylesheet in this repo. CSS does not warn: the declaration is invalid at computed-value time, `background-color` falls back to its initial value, and that value is `transparent`. So the row-detail drawer, the split editor, the confirm dialogs, the sticky page header, the "More actions" popover and the suite's own nav bar all painted nothing, and the register scrolled visibly through them. All 57 now use `--bcn-surface`, the real Beacon token, and `v2/tests/unit/beacon-tokens.test.js` fails if an undefined one returns (the two remaining offenders outside the suite are listed in bugs.md so the count cannot grow unnoticed). **Second shape, same symptom:** `schedules.html` and `fixed-assets.html` style `dialog` for border, radius and padding and set no background at all, so a native dialog took the UA's `Canvas` -- WHITE in both themes, because beacon.css declares no `color-scheme`. One baseline in `accounting-suite.css` (background, colour, a `max-height` so a tall dialog can be scrolled rather than clipped, a backdrop) covers all seven suite pages and leaves each page's own border and padding winning. `accounting-dialogs.test.js` opens every dialog on every suite page in both themes and checks opacity, text contrast, scrollability and that it fits a 390px screen; dropping the baseline fails it on exactly the two dark-theme dialogs. |
| 2026-09-15 | Transactions | **The register gained combinable filters, and the filter predicate stopped existing in two copies.** Date, transaction type, merchant-or-description, account/category and amount now combine with the existing status segments and the quick search, over the WHOLE loaded set rather than a page of it. Amounts are magnitudes with a separate money-in/money-out direction, compared in integer cents (`0.1 + 0.2` is not `0.3`, and an exact-amount filter that misses the row it was typed from is worse than none); a reversed range reads in order rather than matching nothing; a half-typed value narrows nothing rather than emptying the screen. **Filtering by account matches a split transaction on its split LINES and lists the parent once** -- the predicate runs over the parent and asks whether any line matches, so a split across four accounts cannot appear four times, and a split is never counted as "uncategorized" despite its null parent account. Active filters show as removable chips with `Showing X of Y` and Clear filters, and the position survives opening a transaction, reviewing a journal and reloading the date range (it is page state keyed per account, not DOM state). The pickers offer only values present in the loaded set, with counts, plus explicit "no merchant from the source" and "Uncategorized" choices; a stored choice whose rows are no longer loaded says so rather than silently resetting while still applying. All of it is `v2/transaction-filters.js`, which both the table and the AI-candidate list now call -- they had two separately-written copies of the predicate that were already drifting. |
| 2026-09-15 | Transactions | **Merchant is its own column, and it is blank when the source supplied none.** `card_transactions` carries both `clean_merchant` (Plaid's `merchant_name`, or the issuer's own cleaned name from a CSV) and `merchant` (`coalesce(merchant_name, name)`, which falls back to the raw descriptor). Only the first is a merchant: 1,234 of 2,274 production rows have none, and printing the descriptor there would turn "the bank told us nothing" into a claim about who was paid. The cell is empty for those rows, carrying a screen-reader-only note; the description stays in the Transaction column and in Source details. Clicking a merchant filters to exactly the value its picker offers. |
| 2026-09-15 | Transactions | **The Rules page was showing at most 500 of 813 rules, in one unpaged, unsearchable list.** `loadRules()` carried a hard `.limit(500)`, so 313 rules were invisible -- and a rules page that cannot show a rule cannot be trusted to explain why a row was coded. It now pages the fetch like `loadTxns()` does, and the list gained text search, an account filter, a rule-type filter, a match-type filter, and 25-per-page paging with a 10/25 choice. **Filtering happens before paging** and the count reports matches out of the total, so "1 of 61" is never computed over a single page; a filter that shortens the list cannot strand the reader on a page that no longer exists. Saved splits (`card_split_rules`) are listed alongside, showing their ordered accounts and saying in the row that they remember accounts and never amounts. They are read-only here on purpose: those tables take no client writes and there is no delete RPC, so a Delete button would be a control that silently does nothing (an RLS refusal on a delete is a success with zero rows, not an error). Recorded in bugs.md. |
| 2026-09-15 | Accounting Suite | **The suite nav has icons, and an icon-only mode that is still usable.** Seven destinations drawn in the same 24-unit, 1.6-stroke language as the sidebar's icons, with the icon map living in `accounting-suite.js` rather than in framework code. Compact mode clips the label rather than removing it, so the link's accessible name is identical in both modes, and the tooltip is drawn from `data-label` so it cannot drift from the link; it appears on hover AND on keyboard focus. A label behind hover is unreachable by touch, so compact is not offered at all on a coarse pointer or under 760px -- the toggle is hidden there rather than left as a control that appears to do nothing. `aria-current` marks the active destination in both modes. |
| 2026-09-15 | Card coding | **One transaction can now be coded across several accounts.** A coded row carried exactly one `qbo_account_id` and the approval snapshot built exactly one journal line per row, so a $25,187.68 loan payment that is part principal and part interest had three bad options: all to the liability (overstating principal paid, hiding the interest), all to interest (never reducing the loan), or excluding the row and hand-writing a journal adjustment every month with nothing linking the two. `20260915100000` adds `card_transaction_splits` (signed per-line amount, own account/location/entity/memo, composite FK to the parent so a line cannot cross tenants) plus `card_coding_effective_lines`, which yields a split row's lines and an unsplit row's single line and is what `approve_card_import_batch` now validates and aggregates through -- one definition of a posted line, so a split line cannot skip a check an ordinary line passes. **The lines must total the parent to the cent**, enforced in the RPC, by a deferred constraint trigger a service-role write cannot dodge, and again at approval; the settlement leg is computed from the batch total, so a split that did not tie would unbalance the entry. A split row's own account is null and stays null. **A learned split rule stores the accounts and never an amount** -- an amortizing payment divides differently every month, so a remembered figure would be wrong by construction and look authoritative; the rule tables have no amount column and verify fails CRITICAL if one appears. Rules match on merchant or card name and suggest nothing where the two disagree. Editor in `v2/card-splits.js` (cent-exact arithmetic, a running total, a remainder button that refuses to write a zero); bulk coding, the rule pass and the AI pass all skip split rows and say so. Splits are RPC-only writes, finance-gated, and refused once a batch leaves draft/categorized. **Two bank-feed interactions found in review and fixed here.** `plaid_guard_batch` judged direction and clearing-account treatment through one INNER JOIN on the parent's `qbo_account_id`, which a split row leaves null -- so split rows fell out of the join and skipped all four checks, letting a `card_payment` be split into expense accounts and approved where the same row unsplit is refused. Direction is now checked on the transaction and account type through the effective-lines view, per posted line. And a bank correction to a DRAFT split row now drops the split with the rest of its coding: left behind, the tie check raised inside `plaid_apply_sync` and rolled back its cursor, so one split transaction would have stopped that account's feed permanently (reproduced: $100 split $60/$40, corrected to $105). The approved payload also carries each split's memo rather than "split 2", so what posts is what the preview showed. Tests: `card-splits-database.test.mjs` (32 cases, now with the Plaid migration applied) and `card-splits-ui.test.mjs` (24), both mutation-tested, plus a real `plaid_apply_sync` correction in the Plaid suite and a preview/payload description parity pair. Needs `20260915100000` applied; verify row `Card transaction splits`. |
| 2026-09-15 | QBO history | **The archive timed out on every real report; it is now a bounded job that finishes the full-year ledger in about eight calls.** Both production imports (36,778 rows, and a half-year window) fetched their reports and then died in `archive_qbo_ledger`. Measured on a real PostgreSQL 16 with synthetic reports in the production shape: 1k rows 1.4 s, 2k 5.1 s, 4k 19.7 s, 8k 77.9 s, 16k 431 s -- quadratic, because the RPC appended every line to one growing `jsonb` value and each append copied the whole array. The 8 s ceiling falls near 2,500 rows; the full-year report extrapolates to roughly half an hour, holding the company row `FOR UPDATE` throughout (a second session waited 79 s at 8k rows). **The hand-applied 55 s function-level `statement_timeout` did nothing**, also measured: under PostgREST's transaction shape the timer is armed before the function's own SET applies, so the role's 8 s always wins. `20260915000000` makes the archive a job (`qbo_history_jobs` + two staging tables, finance-readable progress, staging closed to clients): each call of the same RPC checks at most 5,000 rows / ~3 s and returns progress; only the last call writes the import and copies the lines set-based, so nothing partial ever reaches the evidence tables the categorizer reads. A malformed row marks the job failed with the same cell-level message, drops its staging and leaves evidence untouched; a retry is a fresh job; one running job per source (partial unique index); a newer source abandons an older unfinished one; the page shows rows checked and can resume an unfinished job without re-fetching. After: 36,778 rows in 8 calls / 5.7 s (longest call 2.9 s), 40,000 in 9 calls / 5.6 s, every line preserved and reconciled. **Two phases stay unbounded by construction** -- hashing the frozen source, and the final copy, which must be one atomic statement pair because the evidence tables are immutable and a half-copied import would read as whole -- so instead of claiming otherwise the job refuses a report over 100,000 ledger rows or 8 MB of stored JSON before doing any work, naming the count and saying to archive the period in parts (measured longest call: 3.6 s at 100k, 6.9 s at 150k, against 8 s). **That refusal runs before the snapshot is built or hashed, and the byte ceiling is measured rather than round.** Hashing is worse than linear in document size -- 7.7 MB 1.21 s, 15.5 MB 7.10 s, 23 MB 9.27 s, 31 MB 21.1 s -- so 7.7 MB is the largest document that hashes comfortably inside an 8 s timeout, and the first version's 48 MB limit would have admitted documents the setup could never finish. Ordering matters for the same reason: a ceiling standing after the hash would let an oversized report be sha256'd straight through the timeout and show an operator a cancellation instead of the refusal telling them to split the period. The byte check that now runs first costs 0.6 ms. Both ceilings are needed and either can bind: a sparse ledger reaches 100k rows while small, a ledger with long memos reaches 8 MB while short. Re-archiving the same two stored report runs is answered from their ids before any of that, so a period already on the books is never rebuilt or refused as too large. Verify asserts the ORDER, not just the guard's presence. Finalization also gets its own exception block, so a final-copy error terminates the job with its reason instead of stranding it at 100% `running` with every resume repeating it. Also stopped the import audit event copying the multi-megabyte snapshot into `finance_audit_events` (the hash stays). `verify_v2_schema.sql` gains a `QBO history bounded archive` row that goes STALE on the old accumulator or on any re-applied timeout setting. Suite: 40,040-line synthetic archive, progress/completeness, mid-job failure, retry, duplicate prevention, supersession; two mutations each fail one assertion. Not a QBO backup: invoices, bills, payment applications, attachments and reconciliations remain separate. |
| 2026-09-14 | QBO history | **A real QBO general ledger could not be archived: every report failed with `Invalid ledger movement`.** The archive RPC's amount regexes required a digit before the decimal point, and QBO writes fractions as `.44` / `-.67` (779 such movements in one stored 366-day report; the trial balance parser carried the same assumption). `20260914220000_qbo_history_number_formats.sql` adds one parser for every numeric cell (sign, digits, optional fraction, or a bare fraction; cast to `numeric` as written, so no rounding), re-creates `archive_qbo_ledger`, and settles a blank amount as zero only when the row's running balance equals the balance carried in — production inspection found 41 blank cells in 36,778 rows, all on Payment / Journal Entry lines whose balance did not move. A blank beside a moving balance, a missing `value` key, a blank running balance or a negative trial-balance amount still fail atomically. A zero line (blank or `.00`) is stored as `row_kind = 'zero_amount'`, which the card categorizer's `row_kind = 'transaction'` evidence read already excludes, so a zero-dollar line can never be a merchant's coding precedent (cycle-1 review finding; no Edge Function change), and a table CHECK refuses a zero-amount `transaction` row outright so the migration itself proves no pre-existing archive carries one (cycle-2 finding; production held no archives). A missing or null summary cell is a shape failure like every other cell. Errors now name the cell, row ordinal and QBO account id (never the value). The database suite reproduces the rejection against the original SQL before applying the fix. Additive migration; the 20260913 migration is untouched. `docs/ops/qbo-history.md` carries the deploy-and-retry steps |
| 2026-09-14 | SEO workflow | **The measurement half of the SEO workflow now exists, and the doc no longer promises what it did not have.** `docs/ops/seo-project.md` had said since 2026-09-08 that a baseline is "captured by a deterministic function"; none existed, and `seo_measurements` accepted any number from any member. `20260914120000` adds `seo_capture_measurements()` (SECURITY DEFINER with an explicit company filter on every read, because the insert policy now refuses the two captured sources from any client -- the function is the only writer of a captured number, and a partial unique index backs the freeze; Search Console page rows for the company's one property, host-matched, plus Shopify top-N landing sessions for the shop serving the host; one row per metric with source, dimensions, filters, completeness and the latest page inspection as evidence; absent = NULL never 0), `seo_follow_up_window()` (same length as the baseline, ending N days after the latest publication, with a reason when not yet measurable), a trigger that refuses a publication for an unapproved or future-dated task, and follow-up ordering in both directions (a follow-up starts after the LAST publication; a correction publication may not land on or after an existing follow-up) beside the existing baseline rule. `scripts/tests/seo-workflow-database.test.mjs` executes all of it as authenticated users against the committed migrations in PGlite — the approval RLS the doc twice called "never exercised" now is — and runs in `sync-tests.yml`. Two mutations each fail one assertion. |
| 2026-09-14 | Search Console sync | **Detail rows the latest fetch did not return are now retired.** The sync only ever upserted, so a page or query Google returned last month and withholds today stayed in `search_console_page_daily` / `_query_daily` while the site row's `*_attributed_*` sums were rewritten from the fresh fetch — and the prompt tells the model to compare exactly those two numbers. After each successful run the core deletes, for the days the site cut returned, rows whose `synced_at` predates the run (never "batch differs", so an overlapping newer run's rows survive an older run's sweep), unless that cut was truncated (a prefix is not the full return). Reported as `stale_rows_removed`; mutation-tested, including the interleaved two-run case. **Second review (same day): ordering the sweep was not enough.** An older run resuming after a newer one completed would still plain-upsert its older payload over the newer run's shared identities and site totals, leaving a snapshot no single fetch produced. `20260914130000` puts one BEFORE trigger on all three tables: an update whose `synced_at` is older than the stored row is dropped, and a page/query insert for a day whose site row already carries a newer `synced_at` is dropped. The newest completed run wins for every caller, not only the two workflows that exist today; a run that lost days says so in its log (`superseded_days`). Not a GitHub concurrency group, for the reason the shopify sync removed one. Proven against the real migration in the SEO database suite (mutation: triggers dropped → fails) and through the real sync path with a fake mirroring the rule (mutation: rule removed → fails). |
| 2026-09-14 | SEO docs | **Status reconciled against Actions logs and the drift check.** `silo-chat` v72 with the Search Console prompt has been deployed since 2026-09-10 21:55 UTC (the "deploy pending" notes in the SEO doc and CLAUDE.md were stale from that evening); the backfill completed the same day; the nightly runs Search Console daily (2026-09-13: 31 days, 56.5% query share). The design-era "Workflow design" section is marked as prose the shipped schema differs from. `docs/ops/seo-competitors.md` scopes competitor research (search vs commercial competitors, a 150/300 keyword-set method over existing tables, a provider comparison and cost model with its sourcing caveat, DataForSEO recommended pending Blake's choice) and records the PR sequence. |
| 2026-09-14 | CI / finance tests | **The finance-database job had been red on every push to `main` since #684, and production was fine.** `plaid-bank-feed-database.test.mjs` executes every `verify_v2_schema.sql` check from the Plaid marker to the end of the file; #684 (profiles scope) and #686 (cashflow overrides) appended checks there without applying their migrations in the harness, so it reported `MISSING: shares_active_company helper` against a correct schema. The harness now applies those migrations (plus `cash_flow_forecast`, with `btree_gist` loaded). Found during the accounting-controls review recorded in [accounting-controls-review.md](accounting-controls-review.md), which also records that SILO has no period lock and no bank reconciliation and recommends the two bounded PRs for them. |
| 2026-09-12 | Finance / PR review | Corrected approval verification to hash the stored PostgreSQL JSONB, preserving numeric scale across API reads; updated the existing reasoned adjustment void RPC for typed and legacy sources. Confirmed postings cannot be downgraded or released by missing recovery results or late failed responses. Composer errors now distinguish an existing period entry from missing accounts and mixed connections. Added executable database, posting-handler, and composer regressions to CI; production and live-QBO validation remain separate draft gates. |
| 2026-09-12 | Finance / QBO controls | **Finance V1 approval and posting prerequisites now stay inside the existing Card Coding → journal adjustment → QBO flow.** Card batches and adjustments can reach `approved` only through finance-authorized RPCs that validate the active QBO connection and every account/location/entity reference, balance the entry, and freeze a hashed QBO-ready snapshot. RLS permits browser edits only while draft/categorized, so the approved content cannot go stale underneath the approval. Generated Shopify monthly, prepaid amortization, and fixed-asset depreciation entries carry a stable source/ref protected by a partial unique index. The one `quickbooks-post-journal` function posts only the snapshot through its explicitly bound connection; claims remain locked while `submitting` or `unknown`, retries search a deterministic DocNumber before any second write, ambiguous 408/429/5xx/network/malformed outcomes never become retryable failures, confirmed-QBO/local-persistence failures repair on the same path, and readback compares account, side, amount, location, and entity line by line. No Plaid connection, new approval app, deployment, or production write is part of this change. |
| 2026-09-10 | CI / ops | **The deferral pin is one hash per side, after collecting three across two fixes.** It ended the day pinning the deployed bundle's `ezbr_sha256`, a git TREE for the function directory, and a per-file blob map — and the tree made the blob map unreachable, since a tree hash covers every name, mode and blob beneath it and the per-file checks only ran once the tree had already matched. Removed, along with the "a drifting file the deferral does not name" branch, which the same reasoning makes dead: drift has exactly two sources, and each moves one of the two remaining pins — a repo-side change moves the tree, a deployment-side change moves the `ezbr_sha256`. `deferralBreak()` drops from 39 lines to 25 and stops shelling out to git once per pinned file. No behaviour change, and that is the claim that was actually tested: the same case matrix runs against the shipped source before and after (including a second file drifting under the deferred function, from each of the two sources in turn), and deleting the surviving tree check makes six of them pass again. |
| 2026-09-10 | Launch calendar | **An initiative's type now has a colour, and the field is called Type.** `launch_channel_items.channel` was shown as plain text in the drawer next to a pill that was orange whatever the row said, and as an orange bar on the month calendar whatever the row said — so nothing on either surface distinguished an email touch from a retail one at a glance. Each of the seven types the form offers now has its own hue, defined ONCE as CSS custom properties and read by both the drawer pill and the month chip, so the two surfaces showing the same row cannot drift into two colour languages. `other` is grey rather than a seventh hue, because it is the absence of a type. Colour is never the only signal: both the pill and the chip still print the type as text. A value outside the seven (legacy free text, or a type retired later) falls to `other` rather than being interpolated into a class name — verified in the browser with a junk value. The status pill beside it was deliberately made neutral: two coloured pills on one row and neither of them means anything. UI wording only — the column stays `channel`, and the LAUNCH's own `marketing_channel` is a different field and keeps its name. Checked in a real browser in both themes; the month chip had no dark tone at all before this and got one, since the rule was being rewritten anyway. |
| 2026-09-10 | CI / ops | **The drift check can now hold a deliberate deferral without going blind, and the last content-drifted function is reconciled.** `page-inspect` was redeployed from `main` through the workflow (v3) and verified against the repo — all four box-drawing comment rules match dash for dash, and the deployed entrypoint is a runner checkout rather than an API client, which is what made the difference in the first place. That leaves `card-categorize`, whose difference is a prompt RULE and is deferred until there is enough card coding to judge it. Left alone, that would have made the daily run red forever, and a check that is always red is one nobody reads — which costs more than the finding. But a bare ignore-list would have been worse than the red: it would also swallow the NEXT, different drift in the same function, which is the whole point of the check. So `DEFERRED_DRIFT` pins each entry to BOTH SIDES of the difference it forgives, and both must still match: the `ezbr_sha256` of the deployed bundle, and the git blob at HEAD of every repo file it names. **Pinning production alone was not enough, which review caught before this shipped:** a change merged to the function and never deployed leaves the deployed hash untouched, so the deferral would have gone on forgiving a difference that had grown into something nobody looked at — and "merged but not deployed" is the exact drift that caught `silo-chat` twice. Either side moving now breaks the pin; a slug the API reports no hash for, a file drifting that the deferral does not name, and a pinned file gone from HEAD all fail closed. Verified by running the shipped source against mocked inputs across eight cases, and mutation-tested by deleting the repo-side check and confirming the merged-but-undeployed case is forgiven again. |
| 2026-09-10 | Dashboards (v3) | **The tiles were styled like 2019 and now are not.** A presentation-only pass over `v3/js/chart-adapter.js` and `v3/dashboard.css` — no report SQL, no `visual_config`, no stored widget touched, so every board that already exists changed at once. What was actually dating them, in order of how much it mattered: a dot on every point of a line and a flat block of tinted fill under it; dashed gridlines, a drawn axis line and a tick per category; every label set in monospace; a KPI whose caption sat *under* its number; a donut with an empty hole; 4px tile corners under a 1px hairline shadow, with a filled titlebar and a `BAR` pill on every tile. Now: symbols only on a series too short to draw as a line (one point with no symbol is a blank tile, not a clean one), a gradient fading to the axis, solid hairline gridlines at a colour separated from the structural border, **words in the sans and numbers in the mono** (which is beacon's own rule), caption-then-number-then-a-tinted-change-pill, the total in the donut's hole, and a card with 10px corners, a soft shadow and a head that only looks like a drag handle in edit mode. Verified by screenshot in both themes rather than by reading the diff, which is what caught the two regressions it introduced: a horizontal ECharts scroll legend **paginates instead of wrapping**, so a bottom legend turned a six-slice donut into three names and a `1/3` pager (reverted to vertical, centre total kept), and the head's icon buttons hovered on `--bcn-surface`, invisible once the head behind them became transparent. All 25 v3 suites pass, browser ones included. |
| 2026-09-10 | Edge functions (correction) | **`sample-notify` was reconciled in the wrong direction on paper, and run #5 caught it.** The 2026-09-10 "Edge functions" row below says the repo was newer and its PPS / Full-run stage labels were deployed. Re-reading run #2's diff with the sign convention verified on `shopify-oauth-callback` (`-` is the repo, `+` is the deployment), PRODUCTION had the stage labels since 2026-08-18 and the REPO copy checked in on 2026-09-02 was the stale one. What was deployed as v20 did carry the labels (the payload was assembled from the diff, not the file), so nothing regressed in production, but the repo still lacked them and the row's claim was backwards. The repo now matches the deployment. Same run also found the `page-inspect` v2 deploy differs from `main` by the dash count in three box-drawing comment rules: an API-client deploy retyped them. Code identical; redeploy from `main` through the workflow, which reads the file. |
| 2026-09-10 | Samples / drift | **The sample-notification trigger is recorded as production runs it, by decision.** `notify_sample_events()` in production was a hand-edited version no migration contained (requested/received on insert, size-request for catalog photo pulls only); the wider 20260818 version with ASSIGNED, WAREHOUSE_READY and received-on-update never ran. Rather than re-apply a notification behaviour nobody chose, and with sample notifications heading into a Slack rebuild, `20260910160000` records the deployed body verbatim (a no-op on prod) and verify checks 28 and 33 assert that shape — check 33 now guards the OPPOSITE drift, someone re-applying the 20260818 file by hand. The repo describes production again and the drift check goes green on it; the Slack rebuild replaces this deliberately. Also `page-inspect` v2 deployed from `main` (one comment block). Left: `card-categorize`, a prompt-rule choice. |
| 2026-09-10 | Edge functions | **Six content-drifted functions reconciled, in the direction the diff said, not the direction assumed.** Reading the drift check's hunks with `-` = repo and `+` = deployed: `review-finish` (the "Employee's Comments" email header), `review-portal` (same), `sample-notify` (PPS / Full run stage labels) and `test-shopify-connection` (`shopify-scopes.ts` gained `payouts_sync` / `draft_orders_sync` / `sessions_sync`) were newer on `main` and were deployed from it (v20, v22, v20, v35, each with its existing `verify_jwt` preserved). `shopify-oauth-callback` and `quickbooks-post-journal` were newer IN PRODUCTION — the deployment already redirected to `silo-baseballism.com` where the repo still said github.io, and carried the generalised "entry" wording from the adjustments work — so the repo was updated to match them rather than overwriting a newer deployment with an older file. Still open in `bugs.md`: `page-inspect` (one comment block, deploy from `main` via the workflow) and `card-categorize` (two prompt rules for locations, one on each side; a decision, not a merge). The readback is the next drift run. |
| 2026-09-10 | CI / ops | **Run #2 of the drift check lost 28 of 148 statements to the Management API's rate limit** (HTTP 429 `ThrottlerException`) and reported them as "could not run" — a red run about the transport, not the database. Run #1 had not tripped it only because the read-only role made every statement slower. Calls are now paced 700ms apart and a 429 is retried with `Retry-After` when the API sends one, else a doubling backoff from 4s, five times; the summary line counts retries so a run that scraped through on retries is visible. `scripts/lib/management-api.mjs`, tested with a fake fetch (7 assertions; retry removal and a wrong Retry-After unit each fail the suite). The same run confirmed the expected state after #657: exactly two verify rows red (the hand-edited `notify_sample_events()`, checks 28 and 33) and six functions content-drifted, both open in `bugs.md`; the four known-unsourced functions now warn instead of failing. |
| 2026-09-10 | CI / ops | **First scheduled run of the drift check: two false alarms fixed, four real findings.** Both open questions from the PR settled: the Management API's `read_only` flag IS accepted and runs the statement as a restricted role that hides `information_schema.check_constraints` rows and refuses functions granted only to `authenticated` — two checks came back MISSING/ERROR over a correct schema, so the flag is dropped and the SELECT-only guard is the safety. `supabase functions download` re-emits source verbatim, but every function deployed before the sources were checked in (2026-09-02, #600) is stored without a trailing newline; six "diffs" were that and nothing else, now classified separately. **Real, from the same run:** (1) `CLAIMED, NOT ACTUAL` on `marketing_kpis_daily` and `ABSENCE IS NOT NONEXISTENCE` on `shopify_landing_pages_daily` — the SEO project's step-1 caveat, shipped 2026-09-08 — were dropped by later migrations that wrote `set description = '...'` instead of appending; restored by `20260910150000` as guarded appends. (2) Verify check 8's signup-flow row tested for the comment `Cross-tenant guard`, which `20260804200000` did not carry when it redefined `admin_update_profile` with the same guard; it now tests the guard. (3) **Production's `notify_sample_events()` is a hand-edited version no migration contains** and never fires SAMPLE_ASSIGNED, SAMPLE_WAREHOUSE_READY or received-on-update — open in `bugs.md`, since re-applying changes who gets notified. (4) Six edge functions differ from `main` in content, `page-inspect` unambiguously (deployed 14 minutes before its last change) — open in `bugs.md` with the deploy workflow as the fix. The known-unsourced classification also had a bug (slugs were read from disk after the download, so the four unsourced functions counted as repo functions with extra files); now read from `git ls-files`. |
| 2026-09-10 | CI / ops | **Production is now checked against the repo on a schedule, not by memory.** `deployment-drift-check.yml` (daily 12:00 UTC, on push to `main` under `supabase/`, and by hand) has two read-only jobs. `schema` sends `verify_v2_schema.sql` to production statement by statement through the Management API and fails on any MISSING / CRITICAL / STALE cell; `functions` runs `supabase functions download` over the checkout and lets git report a deployed function that differs from `main`, one deployed with no source here (four are known and warn; a new one fails), or one on `main` never deployed. Why now: the four push-triggered test workflows added this month answer "does the code do what the code says" and every recent production surprise was the other question — check 6 red for a day with 12 unstamped tables, `silo-chat` a version behind `main` twice, storage policies bucket-wide for months. The splitter (`scripts/lib/sql-statements.mjs`) is tested against the REAL verify file for balanced quotes and parens on all 148 statements, and three mutations (escaped-quote handling, comment handling, cutting at parens) each fail the suite; the runner refuses any statement that is not SELECT/WITH so the scheduler can never write. Two things only the first run can settle, stated rather than assumed: whether the API accepts a `read_only` flag (dropped on a 400 that names it), and whether the CLI re-emits source byte-identically (the log prints `git status` first so a systematic false positive is diagnosable, not mysterious). |
| 2026-09-10 | Backend / SEO | **SEO approver grant is now a button, and the table it writes to had no company backstop.** `/v2/backend.html`'s profile dialog gained "Grant / Revoke SEO approver" beside "Ask SILO access", same pattern (direct table write, `is_exec_or_owner()` RLS refuses anyone else server-side), so the approver list can be set without promoting anyone to `executive`. Preflight against the LIVE schema rather than the migration found two differences from `silo_chat_managers`: `seo_approvers.company_entity_id` is NOT NULL with **no `stamp_company_entity_id` trigger**, and `granted_by` has no `auth.uid()` default — a copy of the Ask SILO insert would have failed on the NOT NULL. The trigger gap is wider than one table: **every table created on 2026-09-09 (12 of them) lacks the stamp**, because none of those migrations called `attach_stamp_company_entity_id_triggers()`; `verify_v2_schema.sql` check 6 does flag it, so it was either not run after those migrations or its MISSING row was not acted on. `20260910130000` re-attaches the trigger everywhere (idempotent; it only fills a NULL, never overwrites a passed value, so service-role syncs are unaffected) and adds the `granted_by` default. The button passes both values explicitly so it works before the migration lands. Also checked while in there: **zero** profiles store a retired `default_page` path, so the clean-up statement recorded on 2026-09-10 has nothing to clear. |
| 2026-09-10 | Auth / routing | **Two landing-page choices and four department routes pointed at files that do not exist.** `v2/profile.html` offered `/finance.html` and `/ops.html` as a default page; neither has existed since the v2 migration, so choosing one wrote a `profiles.default_page` that `pages/login.html` (which only checks the path is internal, not that it exists) sent the user to on every sign-in. The same page then rendered the stored value as "(department default)", because a `<select>` with no matching option falls back to its first one — so the user saw a preference that looked unset and a login that 404'd, with nothing connecting the two. Fixed by listing only pages that exist and rendering a stored-but-retired value as a flagged option. `index.html`'s signed-in router carried a sibling bug: ops / planning / marketing / retail departments were sent to root `/ops.html` / `/planning.html` / `/marketing.html` / `/retail.html`, none of which exist, while `pages/login.html` sent the same departments to Finance — so a user's landing page depended on which URL they arrived at. Both routers now agree. Recorded on the roadmap since 2026-08-16 as "worth fixing next time that file is open"; it took a repo audit rather than the file being open. Stored dead values are not migrated here — see the CLAUDE.md drift note for the one-line clear. |
| 2026-09-10 | Ask SILO (SEO) | **The set-size rule covered query results but not the set the model built itself.** First live run under it (v67): **all thirteen figures correct to the digit** — 85 collection pages, 32 tied at 0%, every session count and rate verified against prod — and the answer still wrote "the lowest completed-checkout rate of **these five**" directly above a list of **four** pages, then "three of these four" three sentences later. Every example in the day-old rule pointed at query results ("all 96 collection pages", "there are 12 such SKUs"), so a count of the model's own prose did not read as covered; the narrower a rule's examples, the narrower it gets read. Now stated as a widening — a set you assembled yourself is counted like any other, and the check is to count the items you actually listed — with the trigger phrasings named rather than left to be recognised. **The point is not arithmetic:** 0.39% genuinely was the lowest of the four shown, so the finding was true and only its SCOPE was wrong, and the scope is the entire reason a scoped superlative is permitted — a wrong scope label puts the superlative back where it started. What the rule DID fix, on the same answer: the 0.00% tie that a false superlative concealed last time was surfaced explicitly, with the tiebreak named. 113 assertions (up from 106); all seven new ones mutation-tested. |
| 2026-09-09 | Ask SILO (SEO) | **A set size was stated without being counted, and the rule added the day before did not catch it.** The third live run of the SEO workflow opened "across all 96 collection pages"; `seo_collection_candidates` returns **85**, and no window produces 96 — checked at 30/60/90/180/365/730 days, per-shop and across shops. The superlative rule shipped hours earlier covers a claim about a *member* of a set ("the lowest of any candidate"); this was a claim about the set's *size*, so re-reading the rule would never have caught it. The three failures — an invented character count, an unscoped superlative, an uncounted total — share one cause, so they are now one block headed **MEASURED VS GENERATED NUMBERS** with one shared fix: quote a number a tool returned, or state none. The new rule admits a count only from rows a query returned in that conversation, names the two ways to get one (count the rows, or run a `count(*)`), and gives the fallback phrasing so the pressure is not simply to invent a number anyway. Note a set size, unlike a character count, IS measurable — which is why the old "TWO THINGS YOU CANNOT MEASURE" heading had to be replaced rather than extended. Asserted in `scripts/tests/seo-orchestration.test.mjs` §15 (106 assertions, up from 98); all seven new assertions mutation-tested. |
| 2026-09-09 | Shopify sync (docs) | **Recorded three things that existed only in a chat transcript.** (1) The landing-page top-N cap **binds on every single day** — all 730 backfilled days for the DTC store are `is_truncated`, with 7,162 distinct paths competing for 250 slots a day — so `shopify_landing_pages_daily` is a top-250 *sample* of each day and a path's absence is a statement about its RANK, never its traffic. Raising `LANDING_TOP_N` would leave old days capped and new days not with nothing marking the seam, so widening coverage means a re-backfill, not a bump. (2) Four paths in the SEO feature have **never executed against real inputs** — the five-page cap refusal, a null `inspect_url`, inspecting a `not_in_registry` page, and the multi-shop host join (the last correctly, since no non-DTC shop has any `/collections/` traffic). The multi-shop join is the load-bearing wrong-store guard, so a future multi-shop result is a first run, not a regression check. (3) `baseballismb2b.com` recorded 128 sessions and **122 landed on `/password`** — the B2B storefront appears password-gated, which is also why it holds 32 landing rows against the DTC store's 182,502. |
| 2026-09-09 | Shopify sync | **A temporal-dead-zone reference in the landing-page progress callback aborted the entire backfill on the first sweep failure (found in review of #643).** The orchestrator's inline callback read `result?.top_n` — `result` being the very const it was passed to as an initializer argument, so it sat in its temporal dead zone for the whole call. `?.` does not help: optional chaining guards null and undefined, not an unreachable binding. And it threw *inside* the day loop's `try`, so it was caught as that day's failure and broke the loop. Measured against a faithful reproduction of the shipped code: **`complete: false`, `days_written: 0`**, failure message `Cannot access 'result' before initialization` — a 730-day backfill would have stopped on day one and blamed Shopify for it. Fixed at the source (`top_n` travels in the callback payload; the callback and the coverage line moved to `scripts/lib/sync-reporting.mjs` so they are reachable from a test at all) and again in depth: `runLandingPagesSync` now isolates progress-callback failures entirely — a reporting callback is not part of the job, so its failure is counted (`progress_errors`) and dropped rather than being allowed to change the outcome. Verified that each lock independently fixes it. **Why CI missed it:** every test drove the core function with a bespoke inline callback, so the orchestrator's real callback — five lines, zero assertions — was never executed by anything. Mutation-testing the *new* tests then caught a second gap in them: asserting the callback reads `topN` from its payload still passed when the core stopped putting it there, so both halves are now asserted together. |
| 2026-09-09 | Shopify sync | **The same misleading green survived the first fix, wearing a different hat (found in review of #643).** When a store has not granted the Shopify scope a stage needs, that stage returns `{skipped: true}` — and the job was written as **`success`**. A manual backfill could therefore still finish green having done nothing, with the reason sitting in a field nobody reads. It is now recorded as `skipped` with the missing scopes named, and a **requested** stage that could not run for want of a scope **fails a manual run**. The original reasoning for exempting it — that a missing scope is a fact about the store's configuration, not a failure of the run — is true and beside the point: from outside, "we asked for sessions and got none" is the same sentence either way, and a red run naming the scope is what points at the fix. Non-fatal on a cron, as ever, or a store that never granted the scope would make the nightly permanently red. Measured before shipping: all 22 syncable connections currently hold every scope, so this turns nothing red today. |
| 2026-09-09 | Shopify sync | **"Resumable" was overstated, so it was made true (found in review of #643).** Writing each day as it is fetched *preserves* progress; it does not *continue* from it — the next run still started at yesterday and re-walked the same days, so a backfill that died at day 100 of 730 paid for those 100 days again before reaching new ground. `shopify_landing_pages_covered_days()` (`20260909360000`) lets a run skip days it already has. Days inside a **restatement window** are always re-fetched, because Shopify revises recent analytics for several days after the fact; the window defaults to 30 **so the 30-day nightly is bit-for-bit unchanged** and only a longer window — i.e. the backfill case — gets resume behaviour at all. Verified: a 90-day window that died at day 40 makes 55 Shopify calls on its second run instead of 90, and the 35 resumed days still carry the *first* run's `sync_batch_id`, which is how the test proves they were genuinely not re-fetched. A missing migration degrades to the old re-fetch-everything behaviour and says so, rather than erroring. |
| 2026-09-09 | Shopify sync | **A restated day accumulated pages that had dropped out of its top 250 (found in review of #643).** Each day stores its top N landing paths; when Shopify restates a day, a path that fell out is simply *absent* from the new result, and an upsert never deletes what it is not given. So the old row survived forever with a stale rank and stale counts, ranks collided, and "the top 250 pages on 2026-08-01" quietly drifted into "every page ever in that day's top 250". `shopify_landing_pages_sweep_day()` removes them after each successful fetch. **A day that returned no rows is never swept** — zero rows is the shape of a bad fetch as much as of a genuinely dead day, and deleting a real day's history on a transport hiccup is far worse than keeping stale rows one more run; the caller refuses, and the function refuses an empty keep-list independently. A failed sweep is counted and reported (`days_not_swept`), never silent. Caught while verifying this: `revoke ... from public` did **not** stop `anon` and `authenticated` executing the new delete path — Supabase's default privileges on the `public` schema re-grant EXECUTE on any newly created function, the same hole `20260904330000` closed for `chat_run_readonly_query`. Both roles are now revoked explicitly and `verify_v2_schema.sql` checks it, along with both functions staying `SECURITY INVOKER` (as `DEFINER` the sweep's `company_entity_id` argument would be a cross-tenant delete). |
| 2026-09-09 | Shopify sync | **A 730-day landing-page backfill finished GREEN in two minutes having written nothing, and three separate defects had to line up for that to be possible.** (1) Shopify refuses a throttled ShopifyQL query with **HTTP 200** and the rate limit inside the GraphQL `errors` array — `fetchWithRetry` only knows status codes, so the one failure these per-day loops actually hit was the one nothing retried. `shopifyql()` now recognises both throttle dialects (the calculated-cost limiter's `THROTTLED` code, and the analytics limiter's bare "Rate limited. Please retry later.", which is what fired) and backs off using Shopify's own `throttleStatus` arithmetic where it is offered, a `Retry-After` header next, and full-jitter exponential backoff otherwise. (2) The day loop accumulated every day in memory and upserted **after** the loop, so the ~100 days it had successfully fetched in 19 seconds were discarded by the failure on the next one — the table was byte-identical before and after the run. Each day is now written as it is fetched, which makes a long window resumable instead of all-or-nothing, and the 120-day clamp is raised to Shopify's own 730-day ceiling now that a stop costs only the days not yet done. (3) The result reported no coverage at all, so a run that wrote everything and a run that wrote nothing printed the same shape; it now reports `days_requested` / `days_fetched` / `days_written` / earliest / latest and a `complete` flag, and an incomplete window is recorded as **error carrying its real progress**, never as success — `rows_upserted > 0` is not evidence a backfill happened. |
| 2026-09-09 | Shopify sync | **The sessions stage of that same run never executed, and left no trace of any kind.** The dispatch carried `sessions_days: 730` **and** `skip_sessions: true` — contradictory inputs, silently resolved in favour of the skip: no `sync_jobs` row, no log line, no warning that a 730-day parameter had been supplied for a stage that would never run. The only way to discover it was to query `shopify_sessions_daily` by hand and find it unchanged at 744 days. The stage gates now go through `scripts/lib/sync-plan.mjs`, which is a pure function over the environment: it names why each stage will not run, flags a days-parameter-beside-a-skip as **contradictory**, and the orchestrator writes a `sync_jobs` row with status **`skipped`** and the reason (`20260909340000` adds that status — distinct from `cancelled`, which means a job that started and was stopped). Policy skips on the 2-hourly cron deliberately mint no row (~900/day saying nothing); a contradiction is recorded whoever triggered it. |
| 2026-09-09 | Shopify sync | **A manual backfill now ends RED when a step it was asked for did not complete**; the nightly keeps its non-fatal policy unchanged. Those are different situations wearing the same exit code: a nightly that synced every sale should not go red because ShopifyQL rate-limited the analytics stage (a routinely-red nightly stops being read, and `sales-freshness-check.yml` is the real alarm), but a hand-dispatched backfill exists *for* the stage that failed, so a green tick there tells the next person the backfill happened. The workflow passes `github.event_name` through as `SHOPIFY_RUN_KIND` so the policy keys off the trigger rather than guessing from the inputs. All three shapes fail a manual run: a stage that errored, one that stopped short of its window, and one that was skipped despite a parameter asking for it. `scripts/tests/landing-pages-backfill.test.mjs` (57 assertions, in CI) pins all of it — verified to fail against five separate re-introductions of the old behaviour. |
| 2026-09-08 | Reporting (v3) | **Two ways the ratio protection could be bypassed, both found in review of #625 and both fixed at one root cause.** `aggregate()` decided "is this a ratio" from the SEMANTIC, and only after the requested aggregate had already been honoured. So (1) an explicit **avg** skipped the ratio branch entirely — which is not a hypothetical setting, because `defaultAggregate('percent')` returns `avg`, so the inspector's Aggregation dropdown *shows* avg on every rate and writes it to `visual_config` the moment anything else in that panel is touched; from that point the tile silently stopped pooling and a conversion rate read **6%** where the pooled answer is 9.92%. And (2) every ratio that is not a percentage never reached the branch at all — **AOV of $10 and $100 summed to $110** where the pooled answer is $200 ÷ 11 = **$18.18**, and **ROAS of 2 and 8 summed to 10** where it is **5**. `isRatio(field, semantic)` now asks the `RATIOS` lookup *or* the semantic, and is asked before anything that combines rows. `first`/`last`/`min`/`max`/`count` stay honoured because they SELECT a row's value rather than manufacturing a new ratio; only sum and avg are overridden, and the result reports `overrode` rather than doing it quietly. A non-percentage ratio with no parts in the result is now refused for the same reason a rate is: $110 of AOV is not a quantity. |
| 2026-09-08 | Reporting (v3) | **The same defect existed in the chart path, and would have put two irreconcilable numbers on one board.** `shape()` groups rows before plotting, and used each measure's default aggregate — so a bar chart of ROAS by platform summed 2 and 8 into 10 while the KPI beside it, on the same report, pooled them into 5. Grouping now pools through the same function. It parts company in exactly one case: where the numerator and denominator are **not** in the result, a KPI refuses (one headline figure has nowhere to put a caveat) but a chart falls back to the unweighted mean and says so in its footer, because blanking forty bars destroys far more than the mis-weighting costs. Neither ever sums a ratio. |
| 2026-09-08 | Reporting (v3) | **A saved parameterised report added to a dashboard said its own parameter was undeclared.** The tile read "this report's SQL uses `{{report_date}}`, which is not a declared parameter" until the board was saved and reloaded, at which point it worked. The declaration was always correct: the add-widget picker selected nine columns from `silo_chat_saved_reports_v` and `parameters` was not one of them, so the locally built widget was handed `report_parameters = undefined` and `substitute()` correctly refused a token nothing declared. Reloading healed it because the VIEW supplies the column. `columns_metadata`, `answer` and `source` had the same shape of bug, quieter — a fresh tile formatted its numbers by profiling and reformatted them after a reload, and the Answer visual was missing from the inspector until then. One `REPORT_FIELDS` list and one `reportFieldsFor()` now build the denormalised half of a widget, so a locally created widget is indistinguishable from a reloaded one. Separately, a parameter declared but not yet in `filter_state` left its control rendering BLANK over results the report's own default had already filtered; `ensureParamDefaults()` seeds the value map from the declarations before the bar paints, without re-running anything. |
| 2026-09-08 | Reporting (v3) | **A text filter showed the typed value, changed no results, and was reverted by the next date change.** Dropdowns worked, which was the clue: a `<select>` commits the moment you pick, and a text input commits on `change` — blur or Enter — so a value that had been typed and not committed sat in the DOM looking applied. Every apply then rebuilt the whole filter bar's `innerHTML` and discarded it. Visible state and applied state were different things with nothing keeping them together. The bar is now `v3/js/filter-bar.js`: fields are built once and updated **in place**, typing is a named pending state (marked on the field, on a chip reading "not applied yet", committed on Enter, on blur, or after a short pause), and Save flushes anything pending before writing `filter_state`. The update guard is on PENDING, never on focus — guarding on focus is what left Reset displaying the value it had just reset away from. |
| 2026-09-08 | Reporting (v3) | **A new KPI silently drew the first numeric column**, so a card titled "Total sales" could show MLB sales. `kpiField()` now falls back in exactly one case — when the result has ONE numeric column, where "first numeric" and "the only measure" are the same statement — and otherwise renders a prompt naming the candidates. Switching visual type no longer inherits a measure from the recommendation either. **The card's title is never evidence**: it is the one place a wrong guess is invisible. |
| 2026-09-08 | Reporting (v3) | **Dashboard header rebuilt: a two-sentence description was pushing the board's own name onto a second row.** The title row now carries identity only — name, scope, unsaved state — and the description moved behind an information disclosure. Also new across the module: a compact/comfortable density (a per-reader preference that changes **no** widget geometry), skeleton loading, an empty state that names the filters narrowing it, an error state that offers a real Retry, and a labelled "Unsaved changes" pill instead of a bullet on the Save button. |
| 2026-09-08 | Reporting (v3) | **A rate is now pooled from its numerator and denominator, or refused — never averaged.** `v3/js/metrics.js` is the new shared layer above `field-semantics.js`: it answers how a column is COMBINED and what a change in it means. Conversion of 2% over 100 sessions and 10% over 10,000 is 9.9%, not the 6% an average gives; where the parts are not both in the result the aggregate is refused with a note naming the two columns that would make it possible. Measured against production while building this (Baseballism, 1–6 Sep 2026, all stores, Total Sales, `sku ilike '%mlb%'`): the pooled MLB share is **8.70%** ($55,463.51 of $637,832.00) where the average of the six daily shares is **11.74%** — three points, because 1 Sep is a third of the window's sales at a below-average share and an average weights it the same as 6 Sep. Kept as a test fixture, not as an MLB feature. A **rate's change is stated in percentage POINTS** with the relative change in brackets (4% → 5% is +1pp, not +25%), and a comparison with nothing to compare to says so rather than printing a number. **Total Sales and Net Sales are named in full everywhere** — on that same window they differ by $75,714, 11.9%. |
| 2026-09-08 | Reporting (v3) | **Three more visuals — combo, heatmap and waterfall — and a rule that a visual which cannot draw a result honestly is not OFFERED.** `validateVisual()` gates the picker, not only the draw: listing Heatmap for a one-dimension query produces a tile that says "needs two dimensions", which is a worse answer than not offering it, because by then the tile is committed. A waterfall or a donut over rates is refused outright — both add their parts up. Heatmap shares the matrix's `(row, column, cell)` shape and both of its rules: order from the query, and an absent pair drawn as a GAP rather than as the ramp's zero. Waterfall forces the query's own row order (a bridge sorted by size is not a bridge) and draws a step labelled like a total from zero rather than stacking it. Stacked bars and a KPI sparkline ship in the same release with no database change, because both live in the schemaless `visual_config`. |
| 2026-09-08 | Reporting (v3) | **Tables: search, click-to-sort, sticky headers, conditional formatting and a CSV that cannot disagree with the screen.** `tableRows()` is the single decision about which rows, and both the table and the export are built from it. A capped or filtered export names its scope IN THE FILE, on a leading comment line, because that is the copy that leaves the building. Search and sort are READER state, held outside `visual_config`, so looking at a table cannot mark a dashboard dirty or become everyone's saved position. Totals go through the metric layer, so a rate is pooled or left blank and the footer says which columns it refused. The horizontal scroller is focusable and labelled: a wide table whose rightmost columns need a mouse is unusable without one. |
| 2026-09-08 | Reporting (v3) | **Canvas: duplicate, full screen, collapsible sections, per-visual size constraints.** Duplicate deep-copies the config — a shared object would make editing one tile change the other. Full screen MOVES the tile's body and moves it back, so a chart keeps its instance and a table keeps its scroll position and every page it has loaded. Collapsing a section detaches its tiles so the board gets shorter, and expanding restores the geometry captured before the first collapse; it is view-mode only, so no collapsed state can reach `layout()` and be saved. Two GridStack traps fixed on the way: `grid.update(el, {minW, minH})` treats an omitted `w`/`h` as *unset*, so applying constraints alone wiped every tile's size; and the single-column collapse runs before any widget exists, so each tile was added at its 12-column `y` and every collision pushed the tile already there down — **inverting each row on a phone** (section, chart, KPI-2, KPI-1 where the desktop reads section, KPI-1, KPI-2, chart). |
| 2026-09-08 | Reporting (v3) | **The widget editor is Data \| Visual \| Format \| Interactions**, replacing one scroll of nineteen controls where sort order sat between the column checkboxes and the semantic picker. Four fixed tabs rather than sections that appear and vanish: splitting a panel only helps if the split is predictable. **The tile is the live preview** — ringed and scrolled into view — rather than a thumbnail that can disagree with it. Fixed while rebuilding it: the column-visibility checkbox read `cfg` and `prof`, which are locals of `renderInspector()` and not of the change handler, so ticking a column threw a ReferenceError and did nothing. |
| 2026-09-08 | Reporting (v3) | **Clicking a value can now filter the board or drill through to another one.** Cross-filtering goes through the PARAMETER system, never by hiding already-fetched rows: each tile has its own query and grain, and filtering one client-side while another keeps its total would put two numbers on one screen that cannot both be right. Only parameters some report on the board declares are offered. Drill-through carries the clicked value **and the whole current filter position**, so the destination reconciles with the number that was clicked, and lists only dashboards the user can already open — hiding an unavailable destination is a courtesy, the destination's own RLS is still the authorization. A tile that reads none of the board's filters is marked "not filtered" on its own face, naming the parameter it ignores. |
| 2026-09-08 | Reporting (v3) | **Personal saved filter views** (`dashboard_filter_views`, `20260908130000`). A board already stored one shared filter position; keeping "my cut" of it previously meant duplicating the dashboard, which duplicates its widgets, which duplicates nothing useful. A view is the values and nothing else. **Creator-only by RLS on purpose** — a saved cut is a working habit, not a publication. The page feature-detects the table and hides the control when it is absent, so page and migration can ship in either order. Filters also gained inclusive date RANGES: a start/end pair on a shared prefix becomes one control with presets, every one of which is asserted for its real day count ("last 7 days" is 7 counting today, the most common off-by-one in a hand-built dashboard). |
| 2026-09-08 | Reporting (v3) | **One Reports entry, and the v3 pages are in the sidebar for the first time.** `/v3/dashboards.html` is now a hub with a Dashboards tab and a Saved reports tab over the same library the add-widget picker reads — until now the only way to browse saved reports was inside the add-widget modal of a dashboard you had already created. Three nav rows (Dashboards, Saved reports, Report builder) under the **unchanged** soft-launch gate the previously commented-out row already carried, so nobody who could not already reach these pages can now. Accounting Export, PO Builder, Planning Scenarios and the rest stay where they are: they are operational workflows that contain numbers, not reports. |
| 2026-09-08 | Reports | **Product Type Performance honoured the MONTHS of a custom range, not its days.** Reported against a real Aug 9 → Sep 7: the drill-through opened Aug 1 → Sep 30 and the category totals matched that wider window. Three independent places rounded to month boundaries — the fetch started at `months[0]-01`, `recompute()` filtered rows by month MEMBERSHIP rather than by day, and `currentRange()` rebuilt the link range from the month list, widening BOTH ends and reaching past today for days that do not exist. Measured on the reproduction: a category reading $380 where the honest answer is $300, because a full 31-day August was counted for a range starting on the 9th. All four readers (fetch, totals, URL, drill-through) now take one `scopeRange()`, so they cannot disagree about what is on screen. A month the range only partly covers is now marked PARTIAL **whichever end is clipped** — previously only the current month was, so a 23-day August printed under a plain "Aug 2026" heading; the header says which days it covers, and the CSV carries the same marking plus the range in its filename. A preset range ends TODAY rather than at month end. |
| 2026-09-08 | Planning | **Planning Scenarios clipped its own tables instead of scrolling them.** `.ps-shell` centres itself with `margin: 0 auto`, and an auto cross-axis margin disables `align-items: stretch` — so in `.silo-main`'s column flex container the shell sized to max-content (1886px) instead of the 1216px available. `.thin-scroll` therefore never got a width to constrain against and its scrollbar never appeared, and `.silo-main` is `overflow-x: hidden`, so the excess was CUT OFF rather than scrolled. Measured at a 1440px window: the scenario matrix needs 1852px, the content area is 1216px, and **636px of columns were unreachable by any means** — it looked like a page that simply ended. Fixed with `width: 100%` on the shell plus `min-width: 0` down the card chain, scoped to this page rather than added to `beacon.css`. **The regression test that used to guard this was checking the wrong thing**: "the page body does not scroll horizontally" passes on a page that clips, because clipped content makes the body no wider. It now asserts that every oversized element sits in a container that both fits the content area and really scrolls — verified to fail at all six widths against the pre-fix CSS. |
| 2026-09-08 | Reports | **Daily Sales Trend: each headline card keeps its own metric.** Switching the chart from Net Sales to Units changed the year-over-year figure on BOTH cards and stripped the currency off Net Sales. Three faults in one function: `fmtVal()` (which follows the chart metric) was applied to the net value; a single `varPct` computed from the chart metric was written to the net card; and the units card did no arithmetic at all — `el.kpiPeriodQtyVar.textContent = el.kpiPeriodVar.textContent`. That last one means **the units comparison was wrong at the default metric too**, not only after switching. Rules extracted to `v2/daily-trend-kpis.js`, whose `headlineCards()` takes only the day rows — re-coupling them to the chart now means changing a signature and a test. Verified the suite catches the defect: against the original `renderKpis` it fails 11 of 19. Also disclosed rather than changed: the prior-year convention is stated on screen with the dates it compares against and named as a calendar-date (not 364-day) comparison, and days still filling up are flagged IN PROGRESS, resolved on Pacific. |
| 2026-09-08 | Planning | **Projections: choosing one location drew 30 columns anyway.** `renderMatrix()` used `state.locations` unconditionally, so the location filter narrowed the rows and left 29 empty columns — and with 30 unconstrained `<col>`s every column collapsed to ~30px. Columns now follow the filter, then a per-user picker, then all-at-a-readable-92px with Date/Launch pinned and the grid scrolling sideways. Scope and date range moved above the summary they describe; advanced filters collapsed behind a badge; applied conditions show as removable chips with one Reset all; a legend names the cell states and says actuals are not overlaid (that needs a `sales_by_day` join and a `location_id → location_tag` mapping this page does not have — kept separate). |
| 2026-09-08 | Planning | **Planning Scenarios: a stepped workflow and edit protection.** Scope, the growth assumption and every advanced input were presented at once. Split into scope → scenario → advanced, with advanced collapsed and a badge when anything in it is off-default. The three plan tables are typed into the DOM and persisted nowhere, so a refresh discarded them silently: typing now raises an UNSAVED EDITS chip, keeps a per-browser draft, and warns before unload; a reload OFFERS the draft back rather than restoring it underneath you. **Saving or duplicating a named scenario is NOT implemented** — there is no scenario table behind this page (`txtScenarioName` is a CSV label), so persistence would be a schema change and is deliberately out. |
| 2026-09-08 | Reports | **Connected sales navigation (`v2/bi-nav.js`).** Trend → category → product → records now carry dates, store group, category and (where it means the same thing) the metric through URL parameters, so a link survives copy-paste, refresh and Back — which the existing `silo:bi:filters` sessionStorage hand-off does not. That hand-off is kept and still works for sidebar navigation; a URL beats it, because a link is an explicit request and a remembered filter is not. **A destination either honours a filter or says it cannot**: anything the target has no control for is named on screen with "those are not applied below", instead of showing unfiltered numbers under a heading that implies otherwise. |
| 2026-09-08 | Reports | **Product Type Performance:** months still filling up are marked PARTIAL (resolved on Pacific) so they do not read as comparable to complete months; a custom date range sits beside the 3/6/12-month presets; category rows drill into Product Search carrying category, dates and store group; and Net Sales is offered as a metric using `total_net_sales` — the same column the other sales pages already call net. No new revenue definition. |
| 2026-09-08 | Reports | **Product Search: a consolidated SKU view.** The same SKU is often recorded under several historical product names; those are the same item, not duplicates to delete. "Consolidate by SKU" drops the name from the grouping key and reports how many names each SKU has sold under, with the names themselves on hover; unticking returns the per-name detail. The row grain is stated in a line above the table, and the Store/Month toggles are relabelled "Split by …" because they change the grouping rather than adding a column. **Totals are asserted identical across every grouping.** Filters show as individually clearable chips with a Clear all. Fixed while wiring this: `renderTypeOptions()` restored the type filter from the `<select>` rather than from state, so a deep link's category was silently dropped while the chip claimed it was applied. |
| 2026-09-08 | Reports | **Sales Report:** an active exception filter (Refund Discrepancy / Blank SKU / Negative Net) is now styled apart and shown as a chip, because it makes every figure below a subset and that is the one filter you must not forget is on. All four filters, the location rollups, exports, sorting, pagination, the full-vs-paginated distinction and the incomplete-day warning are unchanged. |
| 2026-09-08 | Inventory / storage | **Deleted the retired Better Reports inventory archive: 3,400,748 rows, ~4.9 GB of a 5,271 MB table.** `inventory_on_hand` was 94% dead weight — daily snapshots written between 2026-04-22 and 2026-07-08 by the Google Sheets / Better Reports pipeline, which was retired on 2026-07-08 (the date of its last row). Nothing read it: the only reader of this table is the current-snapshot layer, and **zero** rows in `inventory_on_hand_current_mv` depend on a `better_reports` row. The migration re-asserts that before deleting, and aborts if anything has started writing to the retired source again. Also dropped two `sync_batch_id` indexes (524 MB and 80 MB against the full archive) with **0 scans** across a stats window open since 2026-06-15 — i.e. the entire Shopify-sync era. `DELETE` does not return disk; `VACUUM (FULL, ANALYZE) public.inventory_on_hand` has to be run separately, outside the 08:30/14:30 UTC sync windows. |
| 2026-09-08 | Inventory / storage | **The fact that fell out of that, and was recorded nowhere: SILO stopped retaining on-hand history on 2026-07-08.** The Shopify sync that replaced Better Reports calls `purgeShopifyInventoryForConnection()` and then upserts, so every run deletes the previous snapshot — `inventory_on_hand` now holds exactly one, currently 70,622 rows and ~100 MB. That was never a decision; it fell out of the purge-and-replace pattern. Between 2026-07-09 and 2026-09-06 inventory *was* still being synced (2,587 `inventory_snapshot` jobs) and every one of those snapshots was overwritten by the next, so the 61-day hole is discarded retention, not missing collection. **It is unrecoverable** — Shopify's Admin API reports only current inventory levels, with no historical endpoint to backfill from. That is why the Apr–Jul fragment was deleted rather than kept as a seed: it could never have been joined to a new series. If on-hand history is wanted, it needs building as a small daily rollup going forward. |
| 2026-09-08 | Inventory | **Days cover is `number \| null` again, and null is never coerced.** `inventory_workboard_v.days_oos` is NULL whenever there is no demand basis to divide by; `/v2/inventory.html` read it through a `num()` helper that returns 0 for null, so "no cover figure exists" and "zero days of cover" became one value. Every downstream claim inherited it. The rules moved out of the page into `v2/inventory-signals.js` so they can be tested without a browser. |
| 2026-09-08 | Inventory | **What that was costing, measured against Baseballism (66,745 rows, 2026-09-07): the "cover ≤ 7 days" lens returned 60,568 rows holding 256,961 units. The honest answer is 2,184 rows and 217 units.** Only 8,361 rows have a measurable cover basis at all; 26,947 have no matched sales history (3,267 of them holding 141,242 units) and a further 31,227 matched with no sales in the recent windows. The lens was mostly returning rows it knew nothing about — including a Sonic collectible pin with 1,002 units and no recorded sale. The AVG COVER KPI moved 25.3 → 202.3 days for the same reason: it was averaging 58,384 unknowns as zero. Units, retail value and sold-30d totals are unchanged. |
| 2026-09-08 | Inventory | **Five stock states are now distinct where there was one ambiguous blank:** Out of stock, Low cover (with a measured basis), No recorded sales, Unknown demand, and Negative inventory / data review. `velocity_matched` and `velocity_source` are fetched for the first time — the view has exposed them since `20260827190000` and the page ignored both. A rollup reports partial demand coverage ("measured from 3 of 5 underlying rows") rather than presenting part of the stock as all of it. |
| 2026-09-08 | Inventory | **Inventory status and transfer eligibility are separate columns.** One column was doing both jobs, and its ladder fell through to "OK" whenever no transfer rule fired — so two zero-stock Sonic Team Sonic Youth T-Shirt sizes selling ~450 and ~367 units a month read as healthy, with a blank days cover. Transfer now only ever says "Short here", "Excess here" or a dash, and carries a caption saying a dash is not a health verdict. Nothing can reach the OK status without a measured cover inside the healthy band. |
| 2026-09-08 | Inventory | **"Clear lenses" cleared the search box and the location scope too** — it was wired straight to `resetFilters()`, so it and "Reset everything" were the same function under two names. A lens now owns exactly one named field and clearing lenses reverts only those; **Reset all** is a separate control. A value typed over a lens belongs to the person who typed it and survives. |
| 2026-09-08 | Inventory | **Filter surface rebuilt:** search / location / product type and the four view levels moved into a compact toolbar above the table; tags, indicators, thresholds, signal and sort moved into a collapsible **More filters** drawer that narrows the table rather than covering it. Applied conditions show as removable chips with a filtered row count. Everything applies live, so there is no edited-but-unapplied state to signal. A filter with no meaning at the current level (tags at the Product-type rollup) is shown struck through and skipped, instead of silently returning zero rows. |
| 2026-09-08 | Inventory | **Initial load trimmed to an explicit 29-column fetch, and a first attempt at doing more than that was reverted the same day.** The page reads `inventory_workboard_v` in 10,000-row pages; `select("*")` returned 41 columns where the page maps 25, so naming them took the planner row width 832 → 618 bytes (~26%), `row_hash` alone being 4.2 MB across the set. It also added `velocity_matched`/`velocity_source`, which the null-cover fix needs. **The reverted part:** the seven paged requests were replaced with one ordered request for all 66,745 rows, because offset paging re-runs this view's whole 56,782-group aggregate per page (475 ms at offset 0, ~2,000 ms at offset 60,000). It measured 2.1 s — and then timed out in production against the 8 s `authenticated` cap. Two mistakes worth keeping: **the 2.1 s was a warm-cache repeat of a query just run**, where a page load is always the cold case; and `order by id` is not free here — the driving index scan is on `company_entity_id`, so ordering forces a sort of every joined row (~23 MB external merge) before the FIRST row returns. Measured cold under the real cap, `order by id limit 10000` times out on its own where the same page unordered is 474 ms. Paging is now unordered again, as it always was: LIMIT/OFFSET without ORDER BY has no guaranteed row order, but the source is a matview refreshed only by the nightly sync so it is static for the whole page load, and an ordering guarantee is not worth a page that will not load. Also kept from that pass: `product_tags` fetched in parallel rather than awaited first, the never-read `_raw_supabase` copy of all 66,745 raw rows dropped, the per-paint recompute of suggested quantities gone with the planning engine, and a refresh that leaves the previous result on screen instead of blanking the table. |
| 2026-09-08 | Inventory | **Purchasing surfaces removed from the inventory page** (they belong on the purchasing pages): Suggest PO, Add → Draft, the PO draft drawer and its localStorage batch, the Suggested KPI, the Cat buy / Mix Δ / suggested-quantity columns, the BUY and BUY NOW flags, the planning engine (strategy, target cover, safety, min reorder, growth uplift, seeded velocity) and the Buy/Mix default sort. Nothing shared was touched — the draft was page-local and no purchasing page read it. Tagging, selection, exports, the four view levels and the size/SKU and location breakdowns all stay. Default sort is now inventory-signal severity. |
| 2026-09-08 | Inventory | **Column picker labels now match the headers they toggle** (the header used a separate short-label map: header "OH", picker "On Hand"), `_select` and `_details` are no longer offered as blank-labelled entries, and the Image checkbox is no longer inert — it was force-enabled on load while the picker ignored changes to it. Column visibility is stored **per view level**: it was one flat map built by concatenating the four column sets, so a key defined in more than one set took whichever default was listed last, and the SKU view silently lost `variant`. Retail value is labelled `RETAIL VALUE @ SELL` throughout with an explicit "not inventory cost" tooltip. "Product Planning Details" is now "Product Inventory". |
| 2026-09-08 | Inventory | **Regression suites added** (`v2/tests/`, CI as `v2-inventory-tests.yml`, no secrets): 5 suites / 201 assertions over fixtures captured from the real view, including the `days_oos: null` that caused all of this. Four unit suites need nothing installed; the browser suite drives the unmodified page in Chromium end to end. A bug of this shape is invisible to someone reading the screen — the number looks like a number — so it has to be checked by a machine. |
| 2026-09-07 | Reporting (v3) | **A saved report now knows how big it is, and says so while you are still authoring it.** `chat_run_readonly_query` pages at 1000 rows and every rendering surface was already honest about that — a table tile offers `Load next 1000 rows`, a chart tile says it hit the cap. All of those fire once the report is already a tile on somebody's dashboard. Nothing told the author. Measured on prod the day this shipped, two tiles were already over the cap and finding that out took wrapping each query in a `count(*)` by hand: **`Inventory exposure — stock and incoming by SKU` returns 7,231 rows and shows 1,000**; `Logistics · Top products by units sold` returns 2,049 and shows 1,000. The first was authored that same day — ask for "inventory cash exposure" and SKU grain is the natural way to write it, and SKU grain here is 7,231 rows and climbing. The builder's preview now leads with the true total ("2,500 rows · showing 1,000"), warns what a tile will do with it, stores the figure in `silo_chat_saved_reports.row_estimate` / `row_estimate_at`, and the widget picker shows a red row-count pill over the cap. |
| 2026-09-07 | Reporting (v3) | **The count runs only when the preview came back full** — a page shorter than the cap IS the whole answer, so counting it again would be a second query to learn what was just counted. It is wrapped as `select count(*) as n from (<sql>) z`, verified through the real runner against all three shapes a report takes (plain SELECT, WITH/CTE chain, a query carrying its own ORDER BY) before shipping; the trailing-semicolon strip is load-bearing, since the runner rejects semicolons and hand-written SQL routinely ends with one. It renders after the rows, so the preview is no slower than it was, and a stale count from a superseded run is discarded rather than shown. |
| 2026-09-07 | Reporting (v3) | **NULL means never measured, never "small"** — and that rule is enforced all the way to the column. A count that fails or times out persists null rather than the page size, because writing 1,000 for a 7,231-row report is precisely the confident wrong number this feature exists to catch. `verify_v2_schema.sql` asserts the two columns are null or non-null together: a count with no date on it is not interpretable, since data volume moves. **No backfill**, deliberately — a migration runs as `postgres` with no active company, so every count would have come back 0 and recorded a wrong estimate on all 96 reports. They fill in as reports are previewed. |
| 2026-09-07 | Reporting (v3) | **Nothing blocks a large save.** An export-shaped report is a legitimate thing to build, and a builder that refuses one just sends the author to a duplicate — the exact behaviour the saved-report editor was built to stop. The size is stated in the preview AND repeated in the save dialog (including on a first save, where an oversized report becomes everyone's problem); the decision stays with the person. Also not raising the cap: a tile wanting 7,231 SKU-grain rows is asking a question a dashboard cannot answer, and a bigger cap hides that signal rather than acting on it. |
| 2026-09-07 | Reporting (v3) | `silo_chat_saved_reports_v` carries an **explicit column list**, so the two columns had to be named into it — a `create or replace` that drops them does not error, it just makes every report look small in the picker again. The view was recreated with them appended at the END (column order is part of its contract) and with `security_invoker = true` restated explicitly, since recreating it without that would drop RLS propagation and hand every company's saved reports to every user. `verify_v2_schema.sql` now checks the view, the invoker flag and anon's grant, not just the table. New browser suite `v3/tests/browser/report-size.test.js`; `pagination.test.js`'s preview assertions were updated to the stronger wording (it now asserts the true total is named, not just that a page was returned). |
| 2026-09-07 | Marketing | **New: the Reality check card on `/v2/wow-report.html`** — `wow_paid_media_reality()` was built 2026-08-27, is grain-aware, and was displayed nowhere. Every other paid-media card on that page reports `conversion_value` as "revenue"; it is not revenue, it is what each platform claims it drove inside its own attribution window, and no platform subtracts another. The card shows the measures that **cannot** double-count — nCAC (spend ÷ new customers), MER online, MER blended — beside platform-claimed revenue and the claim ratio. It sits **above** the Meta/Google/TikTok sections, not below them: a reader who meets the claimed numbers first has already formed the figure they will carry into the next meeting. It re-attributes nothing, because modelling incrementality here would produce a confidently wrong number, which is worse than a blank (same stance as `launch_actuals_v`'s `sku_source = null`). |
| 2026-09-07 | Marketing | **What it surfaced on its first render: YTD 2026 the ad platforms claim $15,905,329 against $15,952,535 of actual online net sales — 99.7% of every online dollar.** At week grain it is 82%, at day grain 76%. A number that high means per-platform ROAS is not incremental and cannot be summed; that fact existed in the database since August and on no screen. |
| 2026-09-07 | Marketing | **The claim ratio is deliberately NOT scored, coloured, or thresholded** — inventing a good/bad cutoff would imply a precision it does not have, the same reason `product_concepts.evidence_strength` is never a percentage. The "per-platform ROAS cannot be added up" caveat is printed unconditionally because it is unconditionally true at any ratio. The one call-out is `claim_ratio >= 1.0`, which is a definable contradiction (the platforms claiming more than the storefront took in total) rather than a judgement. A configured-but-silent platform is also flagged, since missing spend deflates the denominator and makes every ratio on the card look better than it is. |
| 2026-09-07 | Marketing | **A rounding bug caught by rendering the card rather than reading the code:** YTD's 0.997 printed as "100% of every online dollar" under a plain `toFixed(0)`, while the `>= 1.0` contradiction warning correctly stayed silent — so the card contradicted itself on the single most important number it publishes. One decimal is now added only in the 99.5–99.9 band where the integer would lie. Verified against the four real prod payloads (day/week/month/ytd) plus the empty-denominator and load-failure cases; null ratios render as an em dash, never `0`, because "nothing spent" and "spent and got nothing" must not read alike. |
| 2026-09-07 | Marketing | **Roadmap correction: FB / Instagram organic was listed as "tables exist, 0 rows, blocked on Meta" and had been working for weeks.** Measured 2026-09-07: `facebook_page_insights_daily` holds 407 rows spanning 2025-07-28 → 2026-09-07 and `instagram_media_insights` 211 posts through 2026-09-05, last synced that day. The scopes and Page role that blocked it were granted at some point and nobody closed the item — the second stale roadmap entry found this week, both in the optimistic direction. |
| 2026-09-07 | Ask SILO | **`silo-chat` prompt drift closed — prod was a version behind since 2026-09-04.** The row-cap raise (500 → 1000, `20260904320000`) changed `chat_run_readonly_query` AND the system prompt line that tells the model what the cap is, but merging a PR does not deploy a function, so prod kept running version 57 telling the model to aggregate down to 500. Deployed as **version 58**. Diffed the deployed v57 source against `main` BEFORE deploying, because this repo has documented cases of prod functions drifting from the repo (`notify-slack`, `notify_slack_sample_created()`, three deployed functions with no source here at all) — exactly one line differed, so the deploy discarded nothing. Shipped through the `Deploy Edge Function` workflow rather than the Supabase API: the CLI reads the file off the repo, so what runs is byte-for-byte the branch. Confirmed after the fact by sha256 — `b345a7ee…`, 118,463 bytes, identical on both sides, `verify_jwt` unchanged. |
| 2026-09-07 | Reporting | **Materialized the product-title sales rollup — and the roadmap's stated reason for it was wrong, which is recorded here rather than quietly corrected.** The roadmap's top "Now (stability)" item said a 30-day product rollup over `sales_by_product_title_daily_v` measures ~3s. Re-measured before building, through the real view as a real user with RLS applied, that read is **229ms**: `is_admin_user()` went SECURITY DEFINER (`20260904220000`) the day after that note was written, taking 50x off every policy evaluation underneath it — including the `for all` write policies on `sales_by_day` and `products_master`, which Postgres also evaluates on select. The premise had expired. What survived is worse than the note claimed, and it is on the **unbounded** reads: `select max(day_date)` over the view measured **6,890ms**, hash-aggregating 1.16M sales rows into 700k groups and spilling 127MB to disk in order to return one date. That is not hypothetical — the `Top products by units` saved report runs exactly that as a correlated subquery on every execution, to clamp its window to the last day with data, so the tile paid ~7s before reading a single row it would display; `Logistics · Top products by units sold` (open-ended upper bound) sat on the same footing. Materialized to `sales_by_product_title_daily_mv` with a `(company_entity_id, day_date)` index: **max(day_date) 6,890ms → 1.5ms** (index-only scan, zero heap fetches), **30-day rollup 229ms → 22ms**, same 1,487 groups. All six saved reports that read it still run; five now finish in 12–79ms and the sixth (`Daily Sales`, 1.9s) is dominated by its `shopify_orders` join, not this. |
| 2026-09-07 | Reporting | **The matview is the view STORED, not a further aggregation** — identical select list, identical grain — which is what lets `variant_skus` survive: it is a `count(distinct)`, and a re-summed pre-aggregate would have silently corrupted it the way summing `total_orders` corrupted `Daily Sales`. The migration proves that rather than asserting it: `EXCEPT ALL` in **both** directions against a fresh recomputation of the old view body (row-exact, 704,548 rows), plus every summed measure against raw `sales_by_day`, raising rather than installing if either differs. Afterwards all four `silo_report_tieouts` reconciliations for this view passed — the two money checks to **the cent** ($2,079,733.75 and $7,382,715.58 against `sales_by_day` by an independent path). First run of those tie-outs was a **false pass**: run as `postgres`, `active_company_id()` is null on both sides, so both came back null and the runner scored 0.00 difference. A check that cannot fail is worth nothing; re-run under impersonation it produced real figures. |
| 2026-09-07 | Security / multi-tenancy | **Moving a view onto a matview moves the tenant boundary off RLS, so it had to be rebuilt explicitly.** A matview carries no RLS at all, so the filter now lives in a definer-owned wrapper (`security_invoker = false`) as `company_entity_id = active_company_id()` — character-for-character the SELECT policy on both base tables (`sales_by_day_active_select`, `products_master_active_select`), so the wrapper shows exactly the rows the invoker view showed and not one more. Proven by impersonation, not by reading: Baseballism sees 699,981 rows, Test Company 4,567, summing to the matview's 704,548 with one company each and no overlap. The matview is revoked from `public`/`anon`/`authenticated` — and note the wrapper must NOT be `security_invoker`, since such a view over a matview RAISES 42501 for every user rather than returning fewer rows (the `demand_coverage_by_type_v` failure of 2026-09-04). |
| 2026-09-07 | Security | **`create view` re-granted SELECT to `anon`, the same mechanism that reopened `chat_run_readonly_query` four times.** Supabase's default privileges on the `public` schema grant `anon` SELECT on every newly created view, so recreating the wrapper handed it back. Verified this was a tidy-up and not a live leak before reporting it as either: `anon` holds no EXECUTE on `active_company_id()` (`20260713183258`), so an anon read already raised 42501 inside the filter rather than returning rows. Revoked explicitly anyway — a guarantee resting on a second object's grants is not a guarantee. The four sibling wrappers (`wow_sales_daily_type_v`, `inventory_on_hand_current_v`, `sales_velocity_by_sku_location_v`, `sales_monthly_product_type_rollup_v`) carry the same inert grant and are safe for the same reason; left alone rather than widening this change into views it does not own. |
| 2026-09-07 | Reporting | `verify_v2_schema.sql`'s product-title check was rewritten for the new shape and now fails on each way this can break: the matview missing, the wrapper not `security_invoker = false`, the `active_company_id()` filter gone from the definition, the matview granted to anon or authenticated, or the refresh function missing. Both security predicates were proven to **discriminate** rather than just return ok — `mail_items_v` (invoker, no tenant filter) trips both branches, the two real wrappers pass. Matviews were also claimed in `silo_chat_schema_catalog` as hidden/non-reportable: `refresh_chat_schema_catalog()` picks up relkind `m`, and offering a matview to Ask SILO does not degrade the answer, it raises 42501 and fails it outright. |
| 2026-09-04 | Accounting | QuickBooks Reports gained number formatting, a fix for the drill-down cutting off long transaction lists, and a verification that the account pre-fill carries through to the posted journal entry (`5f4de58`). |
| 2026-09-04 | Accounting | QuickBooks Reports' Adjustments list now includes Card Coding batches alongside hand-written journal adjustments (`d28e5d2`) — both post through `quickbooks-post-journal`, so listing them apart made "what did we post this month" a two-page question. |
| 2026-09-04 | Reporting (v3) | **On a phone, editing a dashboard's Name/Description/Visibility rendered on top of the blank-state message.** `.v3-meta-bar` carries `class="bcn-filter-bar v3-meta-bar"`; `.bcn-filter-bar` (beacon.css) hardcodes `height: 36px` for what is normally a single-row, non-wrapping filter strip. On a narrow viewport the meta-bar's own three fields wrap onto three rows (170px of real content), but the fixed height from the other class won the cascade and clipped the box to 36px anyway — so the wrapped rows painted straight past their own container, on top of the canvas below. Verified via `getBoundingClientRect()`/`scrollHeight` before guessing: `#metaBar` reported a client height of 35px against a scroll height of 170px. Fixed with `height: auto` plus `flex-shrink: 0` (the second, independent half — `.silo-main` is a flex column with a hard `100vh` cap and a `flex: 1` sibling, so the box could still be compressed under space pressure even with height unclamped; the same reason `.bcn-header` already carries `flex-shrink: 0`). `.v3-slicer-bar` got the same `flex-shrink: 0` preventatively — it doesn't carry `.bcn-filter-bar` so it had no active height clamp, but it is the same shape and would hit the identical compression once enough slicers wrapped to two rows. New regression coverage in `v3/tests/browser/dashboard.test.js` at a 390px viewport, confirmed to fail against the unfixed CSS before landing. |
| 2026-09-04 | Reporting (v3) | **New: the Answer widget.** A dashboard tile that renders a saved report's written `answer` as markdown — no query, no rows. Found live on the Ownership dashboard: a 25-query Ask SILO answer ("tell me about the business and suggest action items") got added as a table pointed at the transcript's LAST query, which correctly returned 0 rows — matching the written answer's own finding ("no PO in the pipeline for this SKU") — but a bare 0-row table carries none of that context alone. `queries_run` is a transcript; a genuinely open-ended question can run 20+ queries and never reduce to one dataset, and the actual deliverable was five paragraphs of synthesis no table/chart/KPI/matrix could show. `visual_type = 'answer'` requires `report_id` (section's inverse, which requires the opposite) and renders `silo_chat_saved_reports.answer` through the same `marked` + `DOMPurify` pipeline `v2/silo-chat.html` already uses for chat bubbles, including the same `del`-tokenizer patch so `"~$24K"` doesn't become strikethrough. Offered in the "+ Add widget" picker (pitched hardest past 3 queries: "analysis, not a dataset") and the inspector's Visualization picker, both gated on the report actually having answer text — a manual/system report never does. Switching is non-destructive: an existing table/chart widget can become an Answer widget from its own inspector, no delete-and-re-add, and switching back to Table later still lands on a real dataset rather than a blank tile. Covered end to end in `v3/tests/browser/answer-widget.test.js` — the harness now vendors `marked`/`dompurify` locally, the same fix `echarts`/`gridstack` already needed, since a sandboxed CI runner has no real path to a CDN. |
| 2026-09-04 | Security | **`chat_run_readonly_query` was reachable by `anon` — found while applying the pagination migration below, not by an audit.** The function executes arbitrary read-only SQL as `SECURITY INVOKER`; most RLS policies deny an unauthenticated caller rows outright, but the function should never depend on that alone. `revoke all on function ... from public` does **not** touch a role's own explicit grant, and Supabase's default privileges on the `public` schema grant EXECUTE on every newly created function to `anon` *and* `authenticated` at CREATE time. The original migration (`20260813171926`) knew this and revoked from `public, anon` explicitly; every migration since that recreated the function via drop+create — the whitespace fix, the 30s-timeout change, the column-order fix, and this session's own pagination migration — revoked only from `public`, so each drop+create silently reopened the hole. Closed (`20260904330000_readonly_query_revoke_anon.sql`), and `verify_v2_schema.sql` now asserts `anon` holds no EXECUTE grant on it so a repeat fails loud instead of sitting open. Worth an audit of other functions recreated the same way — not yet done. |
| 2026-09-04 | Reporting (v3) | **`chat_run_readonly_query`'s row cap raised 500 → 1000, and it can now page.** The cap existed to bound a runaway query, not to silently drop everything past row 500 — but that was the failure mode: a card-transaction export or an order-line season past the cap just lost the rest, with no way to see it. A new `p_offset` argument (default 0, so every existing caller behaves exactly as before) pages through more. Two surfaces use it, both append rather than re-render so a reader mid-scroll doesn't lose their place: the report builder's Preview gets a **Load next 1000 rows** button, and a dashboard **table** widget gets **Load more** in its footer, ending with "N rows loaded — that's everything." **Charts do not page** — a bar/line/donut/KPI/matrix is a computed shape over whatever page it drew, and reshaping it live under someone reading it is worse than the existing "hit the cap, aggregate in the report" note those visuals keep. Covered end-to-end in `v3/tests/browser/pagination.test.js` against a real 2,500-row fixture (full page, full page, partial page that ends it), not a mock of the lifecycle. |
| 2026-09-04 | Accounting | **New: Cash Flow Forecast** (`/v2/cash-forecast.html`) — a 13-week liquidity planner, not QBO's indirect-method Cash Flow Statement (added 2026-09-02, still separate and still available). Requested as "cash account cashflow... no financial categories and cards" then clarified against a real spreadsheet reference into: a **liquidity stack** (cash on hand plus available credit) and a **13-week rolling forecast** blending Request Manager, revenue actuals/plan, and two new manual registers. Rolling 7-day buckets starting today, not calendar weeks — chosen because `payment_requests.due_date` is often null (8 of 96 open requests) and `revenue_projections` is daily grain, so daily buckets would be false precision at a 90-day horizon while calendar-week alignment would just add an edge case for no benefit. **Liquidity stack**: one live QuickBooks Balance Sheet fetch supplies both bank balances (asset side) and credit-facility balances owed (liability side) in a single call — cached client-side (`lastBalanceById`) so adding or removing a `credit_facilities` row never triggers a second slow QBO round trip. QBO tracks a card or line's current balance but never its *limit*, so `credit_facilities` pairs a manually-entered limit with an optional link to the liability account carrying the balance owed; verified against Baseballism's real chart before building that a card/loan can't be reliably auto-detected from QBO's own report grouping (Brex/Divvy/FlexOne/Parker sit under Accounts Payable, not "Credit Cards"; the real working-capital loans sit under Other Current Liabilities) — this is a human-maintained register on purpose, not an inferred list. **13-week forecast**: AP outflows read `payment_requests` filtered client-side by `!completed` (matching Request Manager's own exact semantics — a server-side `.eq('completed', false)` would silently exclude a legacy row with `completed` left null, which the app itself already treats as still-open); a request with no due date is flagged as its own total rather than guessed into a week, and an overdue one rolls into week 0 rather than being dropped. Revenue prefers `revenue_projections` (the human's plan) per week and falls back to a trailing-30-day Shopify payout average (actual cash landed, not gross sales) only for weeks with no plan — every bucket is tagged `planned` or `trend` so the source is never ambiguous, the same "always label provenance" discipline as `velocity_matched`/`title_source` elsewhere in this app. **`cash_forecast_items`** covers what neither Request Manager nor revenue projections capture: recurring fixed costs (Rent, Payroll, ad spend — often autopay, not an AP request) and one-time inputs (a loan draw, a known one-off payment), one register for both since they're the same shape and only `cadence` (null for one-time) tells them apart. Headline output is a **cash-low callout** — the lowest projected balance in the window and when it hits, flagged red if it goes negative — because that is the number a liquidity plan is actually for. Verified end-to-end in Chromium against the real file with a stubbed Supabase client and a frozen clock (19 assertions): bank + facility balances render correctly, an unlinked facility shows its full limit with a flag rather than a silent zero, AP buckets overdue/no-due-date/null-completed rows correctly, revenue is tagged planned vs. trend correctly, a monthly recurring item generates the right number of occurrences across the window (3, not 1 — caught as a wrong test assumption, not an app bug, before shipping), the running balance and cash-low figure compute correctly, and adding/removing a credit facility triggers zero additional QuickBooks calls. |
| 2026-09-04 | Reporting (v3) | **Tie-outs: every `system` report now has a second, independent path to its numbers.** Three boards shipped in one day and all three needed correcting afterwards — timed-out tiles, column order wrong since the runtime shipped, a UTC/Pacific day boundary, two revenue definitions one tile apart. Every one was found because a person looked, not because a check ran. That is survivable for an ad-hoc query, where a wrong number is one person's; it is not survivable for a report labelled SILO, which carries the platform's name, sits on everyone's screen, and that nobody re-derives. `silo_report_tieouts` holds 29 checks across all 21 system reports — a rollup against its base table, a summary view against its own line-level view, a rebuilt aggregate against the shared view it replaced. `kind` separates a real reconciliation (24) from a structural sanity check (5), because re-running a report's own SQL and comparing it to itself proves nothing. `run_report_tieouts()` is SECURITY INVOKER, so each check is scoped by the caller's RLS and a Test Company user reconciles their own data. The table is **migration-only** — the runner EXECUTEs what is stored there, so it is code: select policy, no write policy. |
| 2026-09-04 | Reporting (v3) | **The tie-outs paid for themselves on the first run, finding a bug in a system report that had shipped weeks earlier.** `Daily Sales` published **9,995 orders for a day that had 2,298** — it summed the `orders` column of a *product-title* rollup, so an order containing three products counted three times. Nobody had noticed because the number looked plausible and nobody re-derives a canned report. Rewritten to take order counts from `shopify_orders_v` (one row per order). |
| 2026-09-04 | Reporting (v3) | **One of the tie-outs was itself wrong, and that is recorded rather than quietly rewritten.** A sanity check asserted "no data exists for today", which is a different and weaker claim than "the report does not reach today" — today's partial rows do exist (626 of them) and the report correctly ignores them. A second check reported a spurious 11-unit gap in dead stock because it compared on `(location_tag, variant_sku)`, which is not unique in the inventory snapshot; the report was fine and the check was not. Both rewritten. A check that tests the wrong thing is worse than no check, because it launders a guess as a verification. |
| 2026-09-04 | Reporting (v3) | `verify_v2_schema.sql` gained `system_reports_tie_out`, which fails when any system report has no tie-out, when a report has only sanity checks and no independent reconciliation, or when anyone adds a client write policy to a table whose contents get `EXECUTE`d. It caught three reports (Running thin, Overstocked, Dead stock) sitting on sanity checks alone, which is how they got real reconciliations. |
| 2026-09-04 | Reporting (v3) | **`current_date` is UTC and the business is Pacific — so the Ownership board called a partial day "yesterday" every evening.** From 17:00 Pacific (00:00 UTC) the two disagree: measured at 2026-09-03 00:06 UTC, `current_date - 1` returned 2026-09-02, a day still being sold, where Pacific yesterday was 2026-09-01. For the last seven hours of every working day the headline tile would have shown a collapse — exactly when someone checks the numbers before going home. It also explained a **$10,220 reconciliation gap** between the sales rollup and the base table: the UTC window reached into a day the matview had not covered yet; with the Pacific anchor the two agree **to the cent**. Added `silo_business_today()` / `silo_business_yesterday()` and repointed every seeded Ownership and Logistics report onto them. The rest of SILO already knew this — the comp summary anchors to Pacific and the sync must not run before 08:00 UTC — but the rule lived nowhere callable. |
| 2026-09-04 | Reporting (v3) | **Two revenue definitions on one board, which is the exact failure CLAUDE.md records for the Marketing pages.** The channel tile summed `shopify_orders.total_price`, which includes tax and shipping: **$2,248,031** over 28 days against the same board's **$1,948,215** of net sales, one tile apart and never tying ($115,439 tax + $109,778 shipping). Switched to `subtotal_price` — merchandise only, already net of discounts, within ~4% of net sales with the remainder being refunds, which are separate transactions in `shopify_orders` and cannot be netted there. Renamed the column **Merch Revenue** so nobody expects it to tie. |
| 2026-09-04 | Reporting (v3) | **A correction to my own read of the data:** yesterday's +474% was described as a Labor Day sale. It was not — Labor Day 2026 is Sep 7, and the top five products that day were all **Sonic The Hedgehog** (the launch calendar shows Sonic ICYMI on Sep 3). More importantly the comparison is DATE-aligned: Sep 1 2026 is a Tuesday, Sep 1 2025 was a Monday, and the surrounding two-week daily average is **+12% YoY, not +474%** — the swing is one launch day measured against an ordinary one. The figure is arithmetically right and rhetorically misleading, so the section now says the comparison is by date and a launch day will read as an enormous swing. |
| 2026-09-04 | Reporting (v3) | **Ownership dashboard**, built from the questions Jon Loomis actually asked Ask SILO — he requested a "chief of staff style report" twice, and every version wanted the same four things: yesterday / last 7 days / MTD against the same period a year ago, channel mix, paid media efficiency, and what launches next. The questions were right; the ad-hoc SQL behind them was not reusable, so this is a rebuild rather than a save of his transcripts. 13 tiles over 7 new `system` reports, three of them reusing the Logistics definitions so the inventory position sits next to the sales picture and there is one place to correct it. |
| 2026-09-04 | Reporting (v3) | **Three data traps found by verifying rather than trusting the prior queries.** (1) `sales_by_day` holds one row per SKU per day and **each row repeats the order count** — summing `total_orders` gives 9,995 for a day that had 2,298 real orders, which would print AOV as $27 instead of $116; order counts come from `shopify_orders_v` instead. (2) **GA4 sits in `marketing_kpis_daily` with zero spend and 517,970 "conversions"** over 28 days because it is analytics, not an ad platform — left in, cost-per-conversion reads $0.75 and ROAS is meaningless. (3) **Today is always a partial day** (the sync runs each morning), so every period anchors to `current_date - 1`; a board including today would show a collapse every morning. |
| 2026-09-04 | Reporting (v3) | **`AS MATERIALIZED` took the headline tile from 14,949ms to 588ms.** "Sales vs last year" computes eight period sums, and without the keyword Postgres inlines the CTEs and re-scans twenty months of sales and orders once per lateral — twelve scans. The two marketing tiles had a different cause with the same effect: `v_marketing_mer_daily` aggregates **all** of `sales_by_day` (1.14M rows) inside a CTE with no date bound, so an outer filter cannot save it; rebuilt from the base tables with real date bounds, verified to return identical figures (375,589 / 924,778 / 1,133,188), 5,622ms → 501ms. Whole board **26,823ms → 1,187ms**, slowest tile 589ms, zero failures — measured through the real RPC before shipping this time. |
| 2026-09-04 | Reporting (v3) | **The claimed-vs-actual tile surfaces a number that existed nowhere in the UI.** SILO's three Marketing pages do not share a revenue definition — two read actual Shopify sales, Explorer reads platform-claimed `conversion_value` — and `wow_paid_media_reality()` computed the gap but nothing displayed it. Over the last 28 days the platforms claim **$925k** against **$1.13M** of real online net sales: a claim ratio of 0.82, so they under-credit by 18%, and real online ROAS is 3.02x against the 2.46x they report. The tile states both numbers side by side rather than picking a side. |
| 2026-09-04 | Reporting (v3) | **Every table tile in v3 rendered its columns in the wrong order, since the day the runtime shipped.** `chat_run_readonly_query` aggregated with `jsonb_agg`, and **jsonb does not preserve object key order** — it stores keys sorted by length then bytewise. A report selecting `product_title, product_type, units_sold, net_sales, days_with_sales` came back as `net_sales, units_sold, product_type, product_title, days_with_sales`, and the renderer faithfully drew that. Measured side by side on one row to be sure it was the transport and not the renderer. Fixed by returning **`json`** instead of `jsonb` — `json` preserves the select list exactly, and jsonb cannot at any cost because key order is not part of its data model. That is a return-type change, so a drop-and-create; no caller cares, since all four consume it as a JSON array over the wire. This is why `v3/README.md` claimed column order comes from the query while the rendered board disagreed. |
| 2026-09-04 | Performance | **`is_admin_user()` cost 50× what it should, in 47 policies across 31 tables.** Measured in one session: `is_admin_user()` ×176 = **755ms**; `active_company_id()` ×176 = **15ms**. Both read one row of `profiles` by primary key — the difference is that `is_admin_user()` was `SECURITY INVOKER`, so every call re-evaluated profiles' three SELECT policies, two of which call functions that query profiles again. Made `SECURITY DEFINER`, matching `active_company_id()` and the documented `is_employee_manager()` pattern. **This cannot change the answer**, which is the only reason it is safe on an authorization function: it reads only the caller's own `profiles` and `entity_memberships` rows, and both tables carry a PERMISSIVE own-row SELECT policy, so those rows were always visible anyway. Verified rather than argued — evaluated as all 34 real users before and after: 32 admins, 2 non-admins, **zero differences**. Every PO, mailroom, payment-request, products and inventory page in SILO had been paying this. |
| 2026-09-04 | Reporting (v3) | **Seven Logistics tiles timed out, and the tiles were innocent.** Each read `demand_coverage_by_type_v`, which re-scanned 66,539 inventory rows; serially that measured 787ms and looked fine, but fired together by a dashboard they contended and every one hit the 30s statement timeout. **Testing a dashboard query serially does not test a dashboard.** Two fixes: the `is_admin_user()` change above (the real cost — `po_headers` RLS made reading 176 PO headers take 679ms), and `demand_coverage_base_mv`, which materialises the sales and inventory halves to ~250 rows. Purchase orders deliberately stay live in the view, because `po_headers` RLS is narrower than company and a matview bypasses RLS — baking POs in would widen units-on-order. Whole board **6,496ms → 986ms**; slowest tile 337ms. |
| 2026-09-04 | Security | **A cross-tenant leak I created and the new verify check caught minutes later.** `CREATE MATERIALIZED VIEW` inherits Supabase's default privileges on the public schema, which grant SELECT to `anon` and `authenticated` — and a matview has **no RLS**. So `demand_coverage_base_mv` briefly exposed every company's product-type sales and on-hand quantities to any logged-in user and to anon. Every pre-existing matview carries an explicit revoke; it simply was not copied. Revoked, and the revoke is now in the migration so `apply_all_post_merge.sql` cannot rebuild the leak. The argument for writing the check as "**no** matview may be reachable by anon/authenticated" rather than naming known ones: it caught an object that did not exist when it was written. |
| 2026-09-04 | Reporting (v3) | **Section headings were reflowed to the bottom of the board.** `gs-min-h` was set to a flat `2` for every widget, but a section is one row tall — GridStack cannot honour a height under its minimum, so sections were grown to 2 and the resulting collisions pushed them out of position; two of three headings on the Logistics board ended up stacked under the last tile. Minimum height is now per visual type. Three checks added to the dashboard suite, driven through the page's own **+ Section** button so the real `addWidget` path is what is tested, and asserting on `el.gridstackNode` rather than the `gs-*` attributes — GridStack clears those once it owns the element, which is the same reason the renderer's save path reads the node. |
| 2026-09-04 | Reporting (v3) | The Logistics board's `Since` slicer stored `today-28d` instead of `today-29d`: 29 is not one of the date presets, so the control fell back to "Specific date…" and showed a resolved date — one click from freezing the report on the day it was opened, which is the exact failure relative tokens exist to prevent. 28 days is also a cleaner four weeks. |
| 2026-09-04 | Reporting | **`demand_coverage_by_type_v` raised for every authenticated user — it did not return fewer rows, it errored.** `ERROR: 42501: permission denied for materialized view sales_monthly_product_type_rollup_mv`. The view is `security_invoker = true` and read two matviews directly; a matview has no RLS and is therefore not granted to `authenticated`, so under security_invoker the caller needed a privilege nobody has. Any page or Ask SILO answer touching it failed outright. **The obvious fix is the wrong one**: granting select on the matview would silence the error and hand every authenticated user every company's monthly sales, since there is no RLS on a matview to stop them querying it directly. Repointed at the wrapper views that already existed (`sales_monthly_product_type_rollup_v`, `inventory_on_hand_current_v` — both `security_invoker = false` with the tenant filter inside). |
| 2026-09-04 | Reporting | **Kept the view as `security_invoker`, which mattered more than it looks.** Flipping the whole thing to definer would have fixed the error in one line and quietly widened PO visibility: `po_headers_active_select` is `company AND (is_admin_user() OR created_by = auth.uid())`, narrower than company alone, and a definer view bypasses it — the units-on-order column would then have been readable by people the PO table refuses. Only the matview reads are delegated. `verify_v2_schema.sql` gained a `reports_runnable` row that fails if any matview is ever granted to `authenticated`, or if this view goes back to reading one directly. |
| 2026-09-04 | Reporting (v3) | **Logistics dashboard** (`/v3/dashboard.html?id=da5b0a2d-0000-4000-a000-00000000000c`) — 15 tiles over 10 new `system` report definitions covering purchase orders, inventory cover and sell-through. Every source verified `security_invoker` before seeding, because a global `system` report reading a matview would hand every tenant's rows to every tenant. Whole board measures 6.7s of database time serially, ~800ms wall-clock since tiles fetch in parallel; slowest tile 784ms. **Built around what the data says rather than a template:** nothing at Baseballism is running thin (cover is 27–43 weeks on every merchandise type), so a reorder-now board would have been empty theatre. What the data does say is that **24 open POs are past their expected arrival**, one by 62 days, and that some lines carry years of cover — so the board leads with overdue POs and overstock, and the "Running thin" tile is kept mainly because empty is a real answer. |
| 2026-09-04 | Reporting (v3) | **Three exclusions the logistics reports need, each one a wrong answer avoided.** The dead-stock report filters `velocity_matched` — 26,966 of 66,659 workboard rows carry `velocity_matched = false`, where every qty column is a `coalesce(...,0)` artefact meaning *unknown*, not *none*; ranking dead stock without it is exactly the bug that put Shopify's #2 and #4 best sellers on a dead-stock list for an exec. Product-type reports filter to `has_inventory and has_purchase_history`, because Package Protection, Bundles, Uncategorized and custom_sale are virtual products that hold no stock and dominated the "running thin" list on first run. Top-products excludes `x-redo`, the Redo checkout line item, which outsells real merchandise. |
| 2026-09-04 | Reporting (v3) | **A meaningless total, caught by rendering the board rather than reading the config.** Batch-2 totals sum every currency/count/number column — right far more often than not, but the overdue-PO tile printed `TOTAL … 361` under **Days Late**, and 361 days late across eight POs is a number that does not exist. Totals dropped from that tile; also from top-products, for a different reason (1,505 products sold in the window against `chat_run_readonly_query`'s 500-row cap, so any total would be of whichever rows survived it). Dead stock **keeps** its total: 48 rows is the whole answer, so $250,184 at retail is the real value sitting still. Worth remembering as a limit of the totals feature rather than a bug in it — a `count` that should not be summed has no marker. |
| 2026-09-04 | Security / multi-tenancy | **Storage had no company isolation at all — 27 policies, none of them mentioning a company.** The database tier is properly multi-tenant, but every policy on `storage.objects` gated on the bucket alone: `using (bucket_id = 'payment-request-files')` means *any authenticated user of any company may read or delete any object in it* — 375 payment-request attachments (invoices, commission statements, AP confirmations) and 51 mailroom scans. The buckets being "private" bought nothing: private means the CDN will not serve the file anonymously, and RLS is what decides who may. Verified by impersonating the other company's real user before and after: **375 and 51 visible before, zero after**, with a Baseballism admin still seeing 51/51 and 326/326 of their own. |
| 2026-09-04 | Security / multi-tenancy | **The fix reuses the parent row's RLS rather than writing a second rule.** Every private bucket already writes its parent id as the first path segment (`<payment_request_id>/<file>`), and the parent tables are the ones with correct policies — so the object inherits the row's visibility through a plain `EXISTS`: no `SECURITY DEFINER`, no path rewriting, no backfill. For payment requests that is *stricter* than company scoping and is the right answer, since `payment_requests_active_select` is "your own request, or you manage AP, or you are an admin" — an attachment is now exactly as visible as the request it belongs to. Keyed on the parent row and not the `payment_request_files` metadata row on purpose: the object is uploaded **before** its metadata row exists, so keying on metadata would make a failed-insert upload permanently undeletable, and Request Manager's cleanup calls `.remove()` on exactly those. |
| 2026-09-04 | Security / multi-tenancy | **Public buckets keep public reads, because tightening them would be theatre** — a public bucket is served at `/object/public/...` with no RLS evaluated at all, so no policy can hide a file from someone holding the URL. What a policy *can* stop is another company deleting it, so writes narrowed: `sample-images` to its parent sample (the path carries the id), and `launch-images` / `product-concept-images` to the uploader, since neither has a usable key — `launches/<launchId>-<ts>.png` is not a folder and the id is literally `new` for an image attached before the launch row is saved. Nothing in the app deletes from either bucket today, so nothing broke. `avatars` was left alone: it was the one bucket already scoped, to `auth.uid()`, which is narrower than company scoping. |
| 2026-09-04 | Security / multi-tenancy | **`schedule-item-files` is the evidence this was copied rather than decided:** its three policies were *named* "readable by company" / "writable by company" / "deletable by company" and contained no company clause at all, because they were modelled on the payment-request ones with the comment "same treatment as payment-request-files" — which was itself unscoped. The bucket has zero objects and nothing writes it yet, so the migration fixes the convention now (`<schedule_item_id>/<file>`) before a page hardcodes something else. Also found: **48 orphaned objects** in `payment-request-files` whose request row no longer exists and which have no `payment_request_files` metadata row either — dead files from deleted requests, now unreachable. Not deleted here; that is a cleanup decision, not a security one. |
| 2026-09-04 | Security / multi-tenancy | `verify_v2_schema.sql` gained a `storage_isolation` row, and `docs/ops/storage-isolation.md` records the whole thing plus how to re-prove it — the SQL impersonation test, and the five app flows that have to keep working (attach on Purchase Request, view/upload/delete on Request Manager, Mailroom scan, sample photo, and a launch image on a **brand-new unsaved launch**, which is the `new-<ts>` path no parent check could satisfy). Storage RLS is easy to forget: it lives in another schema and no normal migration touches it. |
| 2026-09-04 | Reporting (v3) | **Saved reports are editable — the actual cause of library rot, not a missing convenience.** The workbench was create-only, so a typo'd title, a column that should be labelled, or a hardcoded date that should be a parameter could not be corrected: the only fix was to save a *second* report and leave the first in the shared list. That is how one user produced 22 shared reports with three near-duplicates; at 29 people the duplicates belong to other people and nobody can clean them up. `/v3/report-builder.html?id=<uuid>` now opens an existing report, reachable from **Edit this report** in a widget's inspector — the tile is where you notice a report is wrong. **No policy changes:** `silo_chat_saved_reports_update` already said creator-or-exec, same company, `source` pinned to ask_silo/manual so an edit can never promote a report to a global `system` definition, and `company_entity_id IS NOT NULL` so a global one can never be edited at all. |
| 2026-09-04 | Reporting (v3) | **The editor states its blast radius before you save.** Editing a shared report changes every tile built on it — that is the point, and it is the danger: drop a column and nine tiles go blank with nothing connecting the blank tile to the edit. The save dialog now says how many tiles across how many dashboards draw the report, and names what the edit breaks: a column those tiles reference that this version no longer returns, a parameter a dashboard still supplies that the edit undeclares, and a widget pointing past query 0 when the edit collapses a multi-query chat report to one. **None of it blocks the save** — removing a column a tile draws is sometimes exactly the intent, and a builder that refuses just sends the person back to saving a duplicate, which is the behaviour this change exists to stop. |
| 2026-09-04 | Reporting (v3) | **`saved_report_usage()` is SECURITY DEFINER on purpose.** `dashboard_widgets` RLS scopes reads to dashboards the *caller* can see, so counting tiles from the browser misses widgets on a colleague's private board — and an undercount in a safety warning reads as safety. The tenant check is written out by hand since RLS is bypassed, and a report the caller cannot see returns **no rows at all** rather than a zero row, because zero would confirm the id is real. It returns counts and column names only, never dashboard or widget titles. |
| 2026-09-04 | Reporting (v3) | **A guided report reopens guided.** Saving stored only the generated SQL, which made the guided builder a one-way door — the second edit of any report was a SQL edit, for someone who had never written any. `silo_chat_saved_reports.builder_config` stores `{relname, cfg}`. Nullable, and null means "edit this as SQL", not "something is missing": an Ask SILO save and a hand-written report have none and never will. It is explicitly **not** the source of truth — `queries_run` is — so the moment the SQL is hand-edited away from what the config generates, the config is dropped on save. Otherwise the next edit reopens guided and regenerates a query the report does not run. |
| 2026-09-04 | Reporting (v3) | **An RLS refusal on an update is a success with zero rows, not an error** — so "you may not edit this" had to be handled as its own case rather than falling into the error branch. The page now flips to read-only and offers a copy instead of leaving someone at a Save button that silently does nothing. Two role paths are covered by tests: an owner correcting a colleague's shared report (what exec write access is for) and an admin who must not be handed a Save that RLS will refuse. A fork is always `manual` regardless of the original's source — a hand-edited copy of a chat answer is not a chat answer — and the page then becomes an editor for the copy, so the next save updates it rather than minting a third. |
| 2026-09-04 | Reporting (v3) | **Two test-suite fixes worth keeping.** A hardcoded `=== 10` on the report picker's card count broke the moment fixtures were added for the edit path; it now counts the fixtures with SQL, since the picker's rule is "every saved report that has SQL" and a magic number turns any new fixture into an unrelated failure. And a test premise was wrong rather than the code: the stub user is an `owner`, who legitimately *can* edit a colleague's report, so the read-only assertions needed a second role rather than a fix to the page. |
| 2026-09-03 | Reporting (v3) | **Presentation batch 2 — totals, column control, chart labels, section headings. Module-wide, same as batch 1.** Everything here lives in `chart-adapter.js`, so it applies to every tile on every board rather than to the one report that prompted it. **Totals** are off by default (a total is a claim, and most tables are not lists of things that add up) and, when on, refuse what cannot be summed: a **rate is left blank**, never summed and never averaged, because both are numbers that do not exist — the matrix says so out loud. A truncated table's footer says the total covers only the rows shown, since a total under 20 of 200 rows otherwise reads as the total. **Column hide/reorder** moved onto the widget, not the report: two tiles on one report can legitimately want different columns, and a column the query stopped returning drops out instead of leaving a permanent blank stripe. An empty column list means "everything", never "hide everything". **Value labels and stacking** on bar/line charts, both off by default, with labels suppressing themselves past ~24 points where they collide and stacking **refused across mixed semantics** — dollars stacked on a ratio is a bar whose height means nothing. A line never stacks even when asked; that is an area chart nobody requested. |
| 2026-09-03 | Reporting (v3) | **Section headings, so a fourteen-tile board has structure.** A section is a `visual_type = 'section'` widget with **no report** — it takes grid space, drags and resizes like any other tile, and needed no new table, no new renderer path and no special case in the save buffer, because `report_id` was already nullable (it is `on delete set null` so deleting a report never deletes someone's tile). The only migration was the `visual_type` CHECK, which remains the one part of a visual that ever needs one. A second constraint refuses an untitled section: with no report, the title *is* the content, so an untitled one is an invisible tile that still occupies the grid and is baffling to drag. |
| 2026-09-03 | Reporting (v3) | **A layout bug the tests could not have caught, found by looking at the render.** `.dw--section` set `align-items: flex-end` to bottom-align a heading — but `.dw` is a **column** flex container, so the main axis is vertical and `align-items` controls the *horizontal* axis: every section heading rendered hard against the right edge of its tile. `justify-content: flex-end` is the bottom alignment that was intended. Second time in two days that a screenshot caught something no assertion did (the first was `linkLabel()` falling into its catch branch because the test VM had no `URL`); worth keeping the one-look step in the loop. |
| 2026-09-03 | Reporting (v3) | **`verify_v2_schema.sql` now checks the `visual_type` CHECK itself.** A stale constraint rejects every save of a board using a newer visual, and the page reports that as a failed save with no clue why — exactly the failure the verify script exists to name. It now asserts the constraint admits `matrix` and `section`, and that `dashboard_widgets_section_has_title` exists. Four unit suites missing from `v3/tests/README.md` were added to its table at the same time. |
| 2026-09-03 | Reporting (v3) | **Presentation batch 1 — and it is module-wide, not per-report.** The tiles were correct but wearing their SQL on the outside: raw aliases as headers (`product_type_snapshot`, and the P&L matrix's corner cell literally read `line`), date columns rendering `2025-01-01` across a financial statement, negatives in plain black next to positives, and KPIs showing a bare number with nothing to compare it to. Five defaults added: readable column labels (acronyms preserved — `platform_roas` → "Platform ROAS"), month-grain date headers (`Jan 2025`), negative emphasis on currency/number/percent but **never on a count**, KPI deltas against a named column or the previous row, and per-tile number abbreviation. **All of it lives in `chart-adapter.js`** — the one file every tile on every dashboard renders through — so no report SQL changed and every existing tile improved at once, including ones nobody has opened since. The override rule is now written down: a column *label* belongs to the **report** (one correction fixes every widget, like semantics), while *display* choices belong to the **widget** (a narrow tile and a wide one can legitimately differ). |
| 2026-09-03 | Reporting (v3) | **Three older assertions updated on purpose:** they pinned raw column names and ISO date headers, which is exactly the behaviour batch 1 replaces. Both matrix tests now assert `Jan 2025`, and the browser suite asserts labelled headers *plus* that the real column name survives on the header's `title` — so the thing those tests were protecting (headers come from the query's own columns, in query order) is still covered. |
| 2026-09-03 | Reporting (v3) | **`month_end` and `year_end` date tokens.** A planning report asks "arriving by end of year" far more often than it asks about the start of one, and the alternative was a literal `'2026-12-31'` default that freezes the report on the year it was written — the same class of mistake as storing a resolved date instead of a relative token. `month_end` is day 0 of the next month, which handles February and leap years without a table of month lengths. Both are offered in the dashboard's date-preset dropdown. |
| 2026-09-03 | Reporting (v3) | **Saved-report library curated — and an assumption caught before it did damage.** The plan was to clear ~17 chat-scratch reports out of the shared shelf. Checking ownership first showed **seven of them belong to other people** — five company-visible ones are Jon Loomis's, plus one each from Sammie and Chris — so the sweep would have hidden a colleague's reports without asking. Only Blake's own were touched. The other correction: the shared library is **22 items, not 60**. The other 38 are private and were never in anyone's way, so "clean the library" is a much smaller job than the raw count suggested. Result: two of Blake's transcripts and one raw table dump made **private, not deleted** (reversible, nothing destroyed); `Youth 8/20-12/31 Demand vs On Hand` rewritten as `Purchasing · Youth demand vs incoming` with a `{{cutoff}}` slicer defaulting to `year_end` instead of a hardcoded 2026-12-31; and the Commercial overview's QBO tile re-pointed from the raw-JSON report to the parsed P&L matrix, so it improved rather than going blank. Jon's five remain untouched and are his call. |
| 2026-09-03 | Reporting (v3) | **A matrix visual, because a P&L is not a list.** Every other visual reduces a result to one dimension and one measure; a financial statement is lines **down** and months **across**. Rendered long, the monthly P&L was 160 correct rows that read as nothing. `matrix` takes `row_field` (down), `x_field` (across) and `y_field` (cells). Two rules: **row and column order come from the query**, in order of first appearance rather than sorted — that is what keeps Income → COGS → Gross Profit → Net Income in statement order, which alphabetising destroys (dates are the one exception, always chronological); and **an absent cell renders empty, never 0**, because "no row for August" and "August was zero" are different facts. Only the `visual_type` CHECK needed a migration — everything else about a visual lives in the schemaless `visual_config`, which is why adding chart options never needs one. |
| 2026-09-03 | Reporting (v3) | **CSS specificity trap worth remembering:** Beacon declares `table.bcn-table { table-layout: fixed }` — element **plus** class. A matrix carries both `bcn-table` and `dw-matrix`, so every one-class override lost silently and the table kept fixed layout, which divides width equally across columns and ignores content. With 20 months that gave each column ~60px and the row labels painted straight over January. Four CSS attempts chased the symptom before measuring the computed style showed `table-layout` was still `fixed`; the fix is `table.dw-matrix`. Anything overriding a Beacon table needs the element selector too. |
| 2026-09-03 | Reporting (v3) | **An Ask SILO answer saved as a report can be unusable on a dashboard, and this was the clearest case yet.** "Financials by Month (QBO)" stored `select raw_response from quickbooks_report_runs` — one row, one column, the entire QuickBooks payload. The beautiful monthly P&L in the saved answer was the *model* reading that blob and formatting it; no SQL ever produced those tables, so a tile could only draw the JSON. Replaced with SQL that walks the QBO structure (unnest `Columns.Column` for months, `Rows.Row` for each section's `Summary.ColData`) into long format — figures match the original answer exactly. Two things it surfaces rather than hides: `qbo_pulled_on`, because this reads a **cached** report run and a P&L silently showing a three-month-old pull is dangerous; and months after the current one are excluded, since QBO returns $0 for months that have not happened and a chart of trailing zeros reads as a collapse. |
| 2026-09-03 | Reporting (v3) | **Link and image cells: a table can now show the creative, not just the ad name.** Two new semantics — `link` renders an anchor labelled `host/last-segment` with the full URL on hover, `image` renders a 44px lazy thumbnail. **Detected from the values, not the column name**: a column whose every non-null value is an `http(s)` URL is a link, whereas a column merely *named* `link` might hold anything. These are the only cells that put a database value into an HTML **attribute** rather than a text node, so both pass one guard — `^https?://` with no whitespace — which rejects `javascript:`, `data:`, `vbscript:`, `file:` and protocol-relative `//evil.com` (that last one silently inherits the page's scheme). A value that fails renders as ordinary escaped text: visible to the reader, inert. Thirty-one unit checks, including that each of those schemes can reach neither an `href` nor a `src`. Image height is capped rather than width, since ad creatives arrive in wildly different aspect ratios. |
| 2026-09-03 | Reporting (v3) | **Publishing a dashboard now publishes the reports it shows.** A company-visible board whose reports were private rendered *blank tiles* for everyone else — `dashboard_widgets_v` is `security_invoker`, so a report the viewer cannot see yields a null `query_sql`. That is not theoretical: a board was shared and the recipient got nine empty tiles, because sharing the arrangement is not sharing the data. Saving a board as company-visible now promotes its private reports and **says how many**, since this widens who can read them — never silently. Only reports the saver is permitted to update move (RLS is creator-or-exec); someone else's private report stays private and the message names how many tiles will still be blank for everyone but their owner. |
| 2026-09-03 | Reporting (v3) | **Test-environment gap closed:** the node VM the unit suites run in had no `URL` global, so `linkLabel()` silently fell into its catch branch and every assertion exercised the fallback rather than the real code path. Caught by eyeballing a rendered screenshot, not by a test — the label was the full URL where it should have been `adsmanager.facebook.com/ads`. `lib/load.js` now provides `URL`, `URLSearchParams` and `Intl`. |
| 2026-09-03 | Reporting (v3) | **The v3 tests are in the repo and running in CI.** They had been living in a scratchpad directory that dies with the session that wrote them — ~310 checks protecting ~2,500 lines of code with no redundancy in it (`chart-adapter.js` is the only file that touches ECharts; `report-params.js` is the only place a UI value becomes part of a query). Now `v3/tests/`: five unit suites and five browser suites, one runner, and `.github/workflows/v3-tests.yml` on every push or PR touching `v3/`. **No secrets** — the pages are served from the checkout and Supabase is stubbed in-memory, which is what makes it safe on pull requests. The split matters: `node v3/tests/run.js --unit` runs on a bare checkout with nothing installed, and browser suites are SKIPPED with instructions rather than failed when Playwright is absent, because a test nobody can run without a working `npm install` is a test nobody runs. The five browser suites' copy-pasted setup — including a hardcoded Chromium path in each, which would have gone stale one file at a time — collapsed into one `lib/harness.js`. ECharts and GridStack are pinned to the exact versions `v3/*.html` loads, since a test passing against a different ECharts than production runs is worse than no test. |
| 2026-09-03 | Reporting (v3) | **Calculated measures in the report builder — a measure over two aggregates, not one.** ROAS is `sum(sales) / sum(spend)` and no column holds it, so wanting a rate meant leaving the guided tab for the SQL tab, which is where a self-serve builder stops being self-serve. Three calculations (A ÷ B, A as % of B, A − B), every division through `nullif(x, 0)` because a zero denominator is ordinary in real data and must empty the cell rather than fail the tile. **The calculation declares its own semantic**, which is the part that matters: a calculated column exists in no catalog, so the grounded typing layer that stops `total_units` printing as currency has nothing to say about it, and name heuristics get `net_sales_pct_of_total` exactly wrong — "sales" reads as money, so 12.4% would print as $12.40. Half a calculation is dropped rather than emitted as its left half; a measure showing `sum(sales)` where someone asked for a ratio looks like it works and is wrong. |
| 2026-09-03 | Reporting (v3) | **A widget now defaults to the last non-probe query of a saved answer, not query 0 — after two tiles shipped showing a list of column names.** An Ask SILO answer's `queries_run` is a transcript, not a dataset list: entry 0 is routinely `select column_name from information_schema…`, the model orienting itself before it can write the real query. "Open payment requests by vendor" was added to the Commercial overview board twice, both pointed at index 0, and both rendered the schema probe — which looks like a working tile because it has rows and headers. Fixed the two widgets (a one-field `query_index` change, which is the design working as intended) and fixed the cause: `defaultQueryIndex()` walks back from the last query and skips anything reading `information_schema`/`pg_catalog`, and the query picker badges those cards "schema lookup — not an answer". Dimmed rather than hidden, so the numbering does not lie about the transcript. |
| 2026-09-03 | Reporting (v3) | **Commercial overview parameterised, and two pre-existing performance problems found by testing it.** All ten period reports now take `{{date_from}}`/`{{date_to}}`; Sales Variance (which was hardcoded to 2026-09-01..02 and had been frozen since the day it was saved) takes `{{date_to}}` as its as-of date and derives MTD/YTD from it. Open POs and open payment requests are deliberately left unparameterised — they are current state, not a period. Period suffixes were stripped from tile titles (`Net sales · 7d` → `Net sales`) since a filter makes them lie. **The two slow tiles were not caused by this** — measured both ways, the originals were 8.9s and 16.3s — but slicers amplify the cost, because before you loaded the board once and now a filter change re-runs it. `Net sales by channel` was reading `sales_by_product_title_daily_v`, whose whole purpose is resolving sku → product_title, to group by LOCATION and never look at a product: it paid for a 1.14M-row join and discarded the result. Pointed at `sales_by_day_verification_v` instead — **11,738ms → 31ms**. Sales Variance built its location list with an unfiltered `select distinct location_tag` over the whole view (16.3s alone); it now derives locations from the four windows it already computed — **9,723ms → 453ms**, at the cost of dropping one location that has no sales in any of the four windows (27 rows → 26). Whole board: ~27s of database time → 4.5s. `Top products by units` stays at ~3s and is the honest floor — it genuinely needs the product-title join; a materialized view is the fix and belongs in its own change. |
| 2026-09-03 | Reporting (v3) | **Dashboard slicers: a saved report can now declare parameters, and one control drives every tile that shares a key.** Every saved report baked its window into its SQL, which is why the Week over Week *dashboard* was frozen at week grain while the Week over Week *page* has a Day/Week/MTD/YTD switch — the page passes a grain into `wow_window()`, and a saved report had no way to be passed anything. Nine tiles on nine frozen queries is a screenshot, not a report. Now `silo_chat_saved_reports.parameters` declares what a report's `{{token}}`s need and `dashboards.filter_state` supplies them; matching is **by key across reports**, so nine reports declaring `report_date` get one control, not nine, and changing it re-runs only the widgets whose SQL actually reads that key. **Substitution is typed, never string interpolation** (`v3/js/report-params.js`): a `number` survives `Number()`+`isFinite` or is refused, a `date` must parse, an `enum` must be `===` one of the *declared* options, `text` is quote-doubled and refuses semicolons. A `{{token}}` nobody declared is an **error**, not a passthrough — the declaration is the allowlist. This is not an RLS hole either way (`chat_run_readonly_query` is SECURITY INVOKER), but a value that can rewrite the report into a question nobody asked is still worth refusing, and "the blast radius is small" is a bad reason to build an injection point. Slicers work in view mode, because changing the date range is how you *read* a dashboard, not an edit; a viewer's change is session-only and only an editor's Save persists it. |
| 2026-09-03 | Reporting (v3) | **The Week over Week board converted to slicers, and its grain-baked column names fixed on the way.** All nine reports now take `{{grain}}` and `{{report_date}}`. Two details that were not obvious going in: (1) `report_date` is wrapped in `least(..., complete_through)` so the slicer can only move the window *backward* from the last complete day — the useful direction, and it preserves the property the board was built with, never reporting a partial day; (2) a date slicer stores the **token** (`today`, `today-27d`), not the date it resolves to today, because storing the resolved date would freeze a rolling board on the day it was saved. The concrete date is printed under the control so nobody has to work it out. And the column aliases were renamed off the grain — `this_week` → `current_period`, `wow_pct` → `change_pct`, `sessions_wow_pct` → `sessions_change_pct` — because at day grain a column headed "this_week" showing a single day is a wrong label, not a cosmetic slip. Verified by running all nine reports at all four grains through `chat_run_readonly_query` as a real user: 36 of 36 clean. |
| 2026-09-03 | Reporting (v3) | **Noted while testing, not a regression:** at *day* and *month* grain the landing-pages and discount-code tiles come back empty. That is data lag, not the conversion — GA4 sessions and Shopify discount-code data run about a day behind `wow_data_through()`'s `complete_through`, which follows the sales feed. Walking the date back confirms it: Aug 26–31 return 15 rows each at day grain, Sept 1–2 return 0. The Week over Week *page* shows the same thing today. Worth knowing before someone reads an empty tile as a broken slicer. |
| 2026-09-02 | Reporting (v3) | **v3 assets are now cache-busted with `?v=`, after a stale browser copy cost a debugging round.** A dashboard inspector was showing the previous release's single-measure dropdown while the same page's semantics controls were current — a browser running a `dashboard-builder.js` cached between two merges. The site is served statically with no build step, so nothing invalidates a changed file; the symptom is a half-updated page rather than an error, which is worse than a break because it looks like a bug in the feature. **Bumping the version is now a required step when anything in `v3/js/` or `dashboard.css` changes** — one find-and-replace across the three `v3/*.html` files, noted in `v3/README.md` and in a comment above the tags themselves. |
| 2026-09-02 | Reporting (v3) | **A "Start here" tier in the report builder, and plumbing columns hidden while you build.** Curating the rail to 74 commercial objects left a second problem: an analyst was choosing between eight sales rollups that all sound plausible. The answer was already written down — the catalog's own descriptions say which to prefer ("the grain buying decisions are made at. Use this instead of hand-joining", "Use this rather than raw sales_by_day", "FASTEST path for how fast does this sell") — so `report_priority = 1` promotes the 20 they point at. Soft, not another allowlist: the other 54 stay listed and searchable. It orders the shelf, it does not lock a cupboard. **And the build pane was showing `id, company_entity_id, connection_id, row_hash` before it showed `spend`.** Plumbing is now hidden behind a "show all" toggle — exact names plus any uuid foreign key, since the guided builder cannot join; a TEXT id like `account_id` (which ad account) or `media_id` stays, because it means something. The business columns are pre-selected rather than emitting `select *`, because the point of hiding them is that the PREVIEW stops being full of ids. **Ask SILO is untouched by any of this** — it filters the catalog on `is_hidden`, a different flag for a different question, and still sees all 189 objects. Verified against production, not assumed: `payment_requests`, `payroll_register_lines`, `ar_invoices` and `comp_adjustment_requests` are all still visible to it, so a "which open POs came from payment requests" question works exactly as before. |
| 2026-09-02 | Reporting (v3) | **The report builder's source rail is curated to commercial reporting** — it was listing all 189 non-hidden catalog objects, including AR, payroll, comp adjustments, journals, fixed assets, QuickBooks, performance reviews and the chat audit log. That is a workbench, not a database browser. Now 74 objects: sales, Shopify order/session/funnel detail, marketing paid and organic, product, inventory, launches, purchasing and landed cost, returns, locations. **A new `silo_chat_schema_catalog.reportable` flag, not `is_hidden`** — those are different intents. `is_hidden` means "keep it out of Ask SILO's model index", and Ask SILO legitimately needs payroll and comp tables to answer headcount questions; blinding it would break that. **The default is FALSE — an allowlist.** A denylist means the next finance table someone adds appears in the workbench automatically and nobody notices until it is on a dashboard. Defaulting to false means the failure mode is "a useful table is missing", which someone reports, rather than "payroll is browsable", which nobody sees. `refresh_chat_schema_catalog()` only updates relkind/columns on conflict, so the curation survives every refresh. **Curation, not a boundary:** RLS is still the boundary, the SQL tab can still name anything, and someone who types `comp_adjustment_requests` gets exactly the rows their own policies allow — which for most people is none. `verify_v2_schema.sql` asserts no finance or HR object is offered. |
| 2026-09-02 | Dashboards (v3) | **Charts can plot more than one measure, on two axes** — the thing standing between a saved report and "sales vs ad spend vs ROAS on one plot". `visual_config.measures` is an array; the old single `y_field` still works untouched (`points[].value` stays the first measure, so KPI, donut and table are unaffected). A measure gets its own right-hand axis when it **means** something different from the first, or when its typical magnitude is more than 25× away — ROAS averages 3.3 beside $38,000 of sales, and on one axis it is a flat line on the floor. A secondary-axis measure is drawn as a line even in a bar chart, because a ratio rendered as a bar beside dollar bars invites reading them as comparable heights. **Each measure is aggregated by its own semantic, not the widget's** — summing a ratio alongside summing dollars is how a ROAS column becomes 99 instead of 3.3. Tooltips format every series by its own semantic in one popup. **Also fixes the donut**, which was a wrong number: "Top 10 of 12" dropped two slices, and ECharts computes percentages over the slices it is given — so every percentage was inflated, and with 10 slices against an 8-colour palette two pairs shared a colour. A truncated donut now adds an **Other (n)** slice, so the parts sum to the real whole. A bar chart still just shows its top N, because a truncated ranking is honest and a truncated whole is not. Prompted by a live test: after the new training note, Ask SILO returned exactly the flat one-row-per-day query needed — `generate_series` to fill gap days, anchored to `last_complete_day` rather than `current_date`, and a guarded divide. |
| 2026-09-02 | Dashboards (v3) | **Fixed `[object Object]` on any report returning jsonb, and stopped offering to chart one.** A saved "Ad spend vs Online Sales" report rendered three columns as `[object Object]` — its query used `json_agg`/`row_to_json`, so it returns ONE row with three nested objects. Two faults: the table renderer stringified objects (now prints the JSON, truncated at 160 chars with the full value in a tooltip), and nothing recognised that a nested result has no axis to plot. jsonb columns are now profiled as their own kind, excluded from both dimensions and measures, and a jsonb result is recommended as a **table, never a chart**. A chart tile on such a report now names the offending columns and points at the report builder instead of drawing an empty plot. The underlying lesson is about Ask SILO, not the dashboard: that report needed two queries only because the model chose a data-freshness pre-check as query 1 and packed the answer into nested JSON in query 2 — a shape that narrates well and charts not at all. It also aggregates the whole window into a single row, so it has no time axis either. A chartable version is one flat join, verified against live data: day_date / online_sales / ad_spend / roas, one row per day. |
| 2026-09-02 | Reporting (v3) | **New: a report builder** (`/v3/report-builder.html`) — build a saved report from any table or view, no Ask SILO required. Two tabs over one preview: **Build** (pick a source from `silo_chat_schema_catalog`, choose columns or group-and-total, date window, filters, sort, limit) and **SQL** (plain editor, same schema browser). Both compose a single SELECT through `chat_run_readonly_query`, and neither can save without a preview first. Saves as `source = 'manual'`, which the RLS insert policy has allowed for clients since `20260828130000` — no schema change was needed. **The safety rule that matters:** Postgres does not apply RLS to materialized views, so the Build tab force-adds `company_entity_id = active_company_id()` to any matview source and cannot have it removed; the SQL tab can't force anything, so it warns when it sees a matview referenced without a company predicate. Hidden catalog objects (the credential tables) are not offered at all. Identifiers are validated against the catalog's real column list and double-quoted; literals are escaped; 32 unit assertions cover the SQL composition including injection attempts. **Also: the multi-query picker was unusable and is fixed.** Measured against real data — only 5 of 20 saved Ask SILO reports are single-query; the rest ran 2 to 21 — so "pick one of N raw SQL blobs" was the wrong design for the majority. Each query now shows what it reads and its column list, can be **previewed in place** (lazily, one at a time), and can be **refined in the report builder** as editable SQL. A report of more than three queries now says plainly that it was an analysis rather than a dataset, because no single query reproduces a synthesis. |
| 2026-09-02 | Dashboards (v3) | **The seeded "Top Products" report was ranking `x-redo` #1.** The seed excluded Redo's Package Protection line item by title, guessing the title read "Package Protection". Verified against prod the moment the seed landed: it does not — `products_master` genuinely carries `product_title = 'x-redo'` (the SKU is also the title, and `title_source = 'products_master'`, so this is not the view falling back to `sales_by_day.product_name`). The filter matched nothing and x-redo sat above every real product with 8,209 units. Now leads with Leather Baseball, 1,605 units. This is exactly the failure the seed's own comment predicted, and the property that made it acceptable held: a title filter that stops matching puts a visibly wrong row at the *top* of the list rather than failing silently. Both filters are kept — the title match is the fact today, the ilike is insurance if the line item is ever renamed. Shipped as an explicit UPDATE rather than a re-seed, because the seed uses `on conflict (id) do nothing` precisely so a rebuild cannot clobber a correction; `verify_v2_schema.sql` now asserts the filter is present. Found by running each seeded definition through `chat_run_readonly_query` against live data immediately after applying, rather than trusting that it parsed. |
| 2026-09-02 | Marketing | **Ad-platforms sync timeout raised 60 → 240 minutes; a 400-day backfill was being killed at exactly 60 and writing nothing.** The 60 was set when this job was campaign-KPIs-only and finished in under a minute. Ad-level backfill is a different workload: it fetches **day by day** (`chunkDays=1`, forced by Meta's "reduce the amount of data" error on 7-day chunks) and holds every row in memory until a single upsert at the end — so the run died mid-fetch and an hour of API calls was discarded rather than partially saved. GitHub reports that as `cancelled`, not `failed`, which is easy to misread as someone hitting the button. 240 because the killed run was still going **cleanly** at the cut — no rate limiting, no field rejection, purely wall clock. Only manual backfills approach it; the nightly and 2-hourly runs finish in ~5 minutes. `shopify-sync.yml` already carries 330 for the same reason, so this brings the two into line. Worth noting the shape of the miss: the estimate given for the backfill (45–60 min) sat exactly on a ceiling nobody had checked. The fix is one line; the lesson is to read `timeout-minutes` before recommending a long-running dispatch. |
| 2026-09-02 | Accounting | **New: Fixed Assets, a depreciation subledger** (`/v2/fixed-assets.html`, `fixed_assets` table). `docs/ops/quickbooks-integration.md`'s 2026-08-26 scope note listed fixed assets alongside bank reconciliation as permanently out of scope; clarified in the same commit that this was about not duplicating QBO's own ledger, not about refusing a supporting schedule — the same posture `schedule_items` already established for prepaids. An asset register (label, cost, salvage value, useful life in months, in-service date, and the three QBO accounts a depreciation entry touches) plus a computed straight-line schedule (`fixed_asset_depreciation_v`, `fixed_asset_balances_v` for net book value) and a "Build & post depreciation entry" card, mirroring Schedules' amortization posting exactly: one debit (expense)/credit (accumulated depreciation) pair per asset, staged through the existing `je-composer.js`, no edge function changes. **The asset account is never credited** — depreciation posts to a separate accumulated-depreciation account (confirmed against Baseballism's real chart: `Property Plant & Equipment` and `Accumulated Depreciation, PP&E` are both typed `Fixed Asset` in QBO, distinguished only by name, so the UI offers the same account list for both pickers and a person chooses correctly, same as `card_sources`). **A real bug caught by testing before this shipped**: the first cut of `fixed_asset_depreciation_v` shortened the DIVISOR on early disposal, not just the period count — a $24,000/24-month asset disposed after 6 months was recognising the full $24,000 in those 6 months instead of $6,000, force-fully-depreciating any disposed asset and always reporting zero net book value at disposal. Fixed by separating `nominal_months` (the constant divisor) from `periods_to_generate` (shortened by `disposal_date`); re-verified directly against four synthetic assets in prod (full 12-month life, 3-month rounding, disposed-mid-life, and salvage-value-with-disposal) before the UI was built. Scoped to `straight_line` only, matching the prepaid precedent. Verified end-to-end in Chromium against the real file with a stubbed Supabase client (24 assertions): account pickers populate and persist, an unmapped asset refuses and names itself, a mapped asset produces the correct 2-line entry crediting accumulated depreciation specifically (not the asset account), an existing draft reopens rather than duplicating, an already-posted month is refused, asset-creation validation (salvage ≥ cost) is rejected before any insert, and the disposal flow validates its date format before writing. |
| 2026-09-02 | Accounting | **Schedules can now actually post the amortization it computes.** The migration that built `schedule_items`/`schedule_item_amortization_v` said so explicitly: "Nothing here posts to QuickBooks. Generated periods are a proposal a human reads; `quickbooks_journal_postings` exists for when that changes." Nothing had changed it — the page could stamp transactions to an item and show the computed monthly recognition, but there was no path from that number into the books, and (found while building this) no way to even set the recognition-side expense account on an item at all, so every real prepaid item on Baseballism's books (`qbo_expense_account_id` null on both) would have hit the "unmapped" refusal on the very first use. Added an editable expense-account picker per item (a second lean query against `schedule_items` merged into the balances view's rows, rather than widening a view several other callers read), and a new "Amortization entry" card: pick a month, "Build & post" reads `schedule_item_amortization_v` for every active `schedule_type = 'prepaid'` item with a period in that month, refuses with the item's name if any lack an expense account, and otherwise stages one journal entry (one debit/credit pair per item, not netted, so a reviewer sees which item produced which dollar) through the existing `je-composer.js` — no edge function changes, same review-before-post path Accounting Export's button uses. Scoped to `prepaid` only: `deferred_revenue`/`deferred_cost` share the arithmetic but flip which side is debited, and guessing that sign for a type nobody asked to post yet is how you post a backwards entry. Double-posting a month is guarded in the app, not the database — `journal_adjustments.source_context` is deliberately free text, so a repeat "Build & post" for a month that already has a staged entry (`schedules:prepaid:YYYY-MM`) reopens it instead of duplicating it, and a month already posted is refused outright with a pointer to void it first. Verified end-to-end against the real file in Chromium with a stubbed Supabase client (17 assertions): the expense-account picker renders and saves, an unmapped item refuses and names itself, a mapped item produces exactly 2 lines (debit expense / credit prepaid, both for the period amount) with the right memo/month-end entry date/context tag, an existing draft reopens rather than duplicating, and an already-posted month is refused. |
| 2026-09-02 | Accounting | **"Pull accounts" on Integrations started failing with `account_query_failed_504: stream timeout` — confirmed as Intuit's own query engine, not our code, and fixed with a retry.** Two independent failures five hours apart (2026-09-01 20:36 and 2026-09-02 01:49 UTC), each with its own `intuit_tid`, both a bare 5xx from Intuit's `Account` query with no query-side cause: the OAuth grant itself was healthy both times (the access token refreshed successfully immediately before each attempt), and the same query had synced cleanly the day before (`accounts_synced_at` 2026-08-31). `qboQueryAll` (shared by Account/Department/Customer/Vendor pulls in `quickbooks-accounts-sync`) now retries a 5xx response up to twice with backoff (1.5s, 3s) before giving up — a 4xx is never retried, since a bad query or dead token will not fix itself by asking again. Deployed as v10. `fetchLocationTracking`'s own preferences call was left alone — it already fails soft (returns `null`, never blocks the sync), so it was never the thing breaking "Pull accounts." |
| 2026-09-02 | Marketing | **`meta_ad_performance_v` now exposes thruplays, leads and the copy's provenance.** The columns landed on the base tables earlier today, but this view lists its columns explicitly — so the report (which reads the base tables through `wow_creatives`) could see them while ad-hoc queries and Ask SILO reading *through the view* could not. A view that silently exposes less than the table it wraps is how two people run "the same" query and get different answers. Added `thruplays`, `leads` and `creative_body_source`. **Appended rather than slotted in beside `initiate_checkout` where they belong semantically**: `CREATE OR REPLACE VIEW` only permits adding columns at the end, and reordering would force a DROP — this view carries grants to anon/authenticated plus `security_invoker = true`, and losing either to tidy column order is a bad trade. Verified after: view totals match the base table exactly (465,556 thruplays / 17,128 leads), grants and `security_invoker` intact. Also ran `refresh_chat_schema_catalog()` — **which had been missed for every column added today**. The catalogue had none of `shopify_status`, `online_published_at`, `body_source`, `thruplays` or `leads`, and the new sales rollup view was unregistered, so Ask SILO could not see fields it was being asked about. CLAUDE.md requires that refresh after any migration touching public tables; it is now current at 197 relations, with the curated `products_master` description preserved across it. |
| 2026-09-02 | Marketing | **Thruplays and leads per AD, so each objective is judged on the metric it was bought on.** The creative card split ads by objective but could only read Thruplays/Subscribers/Followers on click cost, because `meta_ad_performance_daily` had no such columns and the ad-level fetch deliberately skipped them — its comment said the card was "scoped to purchase campaigns, which are judged on ROAS", which stopped being true when the objective split shipped. What that cost, measured at campaign level for the week to 2026-08-31: **Thruplays $2,142 → 364,629 thruplays at $0.0059 each**; **Followers $694 → 55,851 at $0.0124**; **Subscribers $70,478 → 16,728 leads at $4.21**. Subscribers is the largest Meta line and at $4.21 a lead it is working — but at campaign grain nobody could say *which creative* produced those leads, which is the whole question the card exists to answer. **Leads cost no new API field** — they come out of the `actions` array the ad-level request already fetches. Thruplays add `video_thruplay_watched_actions`, so the ad-level fetch now carries the same drop-and-retry guard campaign level has: Meta rejects an *entire* insights request over one bad field name, and a wrong guess would take down ad-level spend, not just a column. Both columns are **nullable on purpose**, unlike `view_content`/`add_to_cart`/`initiate_checkout` beside them which default to 0: every ad can report those, but a video buy has no leads and a lead buy has no thruplays, and a 0 would rank an ad worst on a metric it was never bought on. Card columns follow: Thruplays and Followers read Thruplays + Cost/thruplay (at 4dp — $0.0059 flattens to $0.01 otherwise), Subscribers reads Leads + Cost/lead. The per-group footnote is now **derived from the data** rather than hardcoded — the old fixed strings said "not synced per ad" and would have kept saying it after the sync started carrying the metric. Verified in a browser in both states: populated shows no caveat, unpopulated says it fills in on the next sync. **Columns are null until the next ad-platforms sync runs.** |
| 2026-09-02 | Marketing | **Ad copy: fetch the other two places Meta keeps it.** The WoW creative table showed "not synced" for most ads and my first diagnosis — Dynamic Creative and `asset_feed_spec` — was **wrong**. Measured over ads that actually spent since 1 June: VIDEO **39 of 39** have `creative.body`, PHOTO **1 of 1**, SHARE only **32 of 271**. 100% coverage on two formats is not what asset-feed fragmentation looks like. Every gap is `object_type = SHARE`: an ad pointing at an **existing Page post** rather than carrying its own creative, so `creative.body` is legitimately empty — the words live on the post, and the sync never asked for it. That is **$1,159,168 of spend** with unreadable copy. The creative fetch now resolves copy in three steps: `creative.body`, then `object_story_spec` (`link_data.message` / `video_data.message` / `photo_data.caption`) for ads built in place, then one extra batched read of the post named by `effective_object_story_id`. The post pass is skipped entirely when nothing needs it, and a failure there is caught and warned rather than thrown — copy is enrichment, and a missing page scope or a deleted post must not cost the creative rows already collected. New `meta_ad_creatives.body_source` records which of the three produced the text (`creative_body` / `object_story_spec` / `page_post`, null when none did). Without it a jump in coverage is unattributable — you could not tell whether the extra request earned its place, and the next person re-guesses exactly as this round did. It is also the signal that the post pass has silently stopped working. **Needs one manual ad-platforms sync run to confirm against the live Meta API.** |
| 2026-09-02 | Marketing | **WoW creative columns now follow the campaign objective — ROAS and CPA appear only under Purchases.** Sammie: *"ROAS is not needed for Thurplays or subscribers of followers as we they are not in a purchasing campaign."* Correct, and this was a self-inflicted regression: the campaign-level tables already split columns by objective (`PAID_COLS`), with a comment explaining that a thruplay buy has no attributed revenue by design and printing `0.00x` against it reads as failure. The new ad-level table shipped with one fixed column set and reintroduced that exact failure a level down. Worse than the `0.00x`: Subscribers ads were showing a **$772 "CPA"**, which is cost per *purchase* on a lead campaign — a real number answering a question nobody asked, which is more misleading than a blank. Columns are now per objective, chosen from what is actually measured at ad level (verified against `meta_ad_performance_daily` for the week to 2026-08-31): **purchase** 1,152 conversions / $100,003 value, so ROAS + CPA are real; **subscribers** 94 conversions but those are purchase-pixel, not leads, and leads are not synced per ad; **thruplay** 0 conversions and thruplay counts not synced per ad; **followers** 0 conversions and follows not synced at all. Everything non-purchase therefore reads on spend, reach and click cost — CPC and CPM derived client-side from fields already in the payload, so no round trip and no migration. Each non-purchase group carries a footnote naming what is missing and why, so an absent column is not mistaken for a dead campaign. Verified in a browser: Purchases shows Spend/ROAS/CPA/Conv/Clicks, Thruplays and Followers show Spend/Impressions/Clicks/CPM/CPC, Subscribers shows Spend/Clicks/CPC/Impressions/CPM. |
| 2026-09-01 | Inventory | **"Is this product live on the website?" is now answerable — it was not, and the Ask SILO log shows what that cost.** Sammie asked it six distinct ways in one afternoon (15 of her 89 questions) while trying to move stale inventory and find pages that are still up but only have XL/2XL left. Ask SILO kept saying no and was **right to**: `is_active` is true on **24,056 of 24,056** rows (a constant, not a signal), `is_discontinued` has never once been set, and `lifecycle_status` only carries `Idea`/`Ready for PO` — the PO pipeline, not the storefront. `created_at` is not a workaround either, which matters because it looks like one: **12,121 rows are stamped 2026-04-28 and 11,514 are stamped 2026-07-20** — 98% of the catalog on two bulk-load dates. It records when SILO first saw the row, so "products created in the last 120 days" returns the July backfill and reads as a brand-new catalog. Shopify has the real answer and **the sync already fetches it**: `runCatalogSync` pulls `products.json` once per status (active/archived/draft) and holds the whole product object at the line where it builds each row — it just never wrote `status` or `published_at` down. Added `shopify_status` and `online_published_at`, written from that object, so no extra API call. **Live on the site = `shopify_status = 'active'` AND `online_published_at is not null`.** Deliberately **sync-owned and separate from `is_active`**, per the catalog sync's own contract that it never touches human-curated fields: Shopify owns these two, a person owns `is_active`/`lifecycle_status`, and neither overwrites the other — backfilling `is_active` would have the nightly sync silently reverse a merchandiser's edit. The `silo_chat_schema_catalog` note for `products_master` now names the fields and explicitly warns off `is_active`, `is_discontinued`, `lifecycle_status` and `created_at`, so the next person does not repeat those six rounds; `verify_v2_schema.sql` fails if that note goes missing. **Columns are null until the next catalog sync runs.** |
| 2026-09-01 | Marketing | **WoW Top Creatives rebuilt to Sammie's spec: every ad that spent, by campaign objective, ten at a time — and Followers is now its own objective.** The card was a flat top-10 by spend across purchase campaigns only. It is now `wow_creatives(report_date, grain, limit)` — ad-level from `meta_ad_performance_daily`, grouped by objective, ordered by spend, showing Ad, Ad set, Copy, Spend, ROAS, **CPA** and the change vs the prior period, with a *Show 10 more* control per objective (verified 10 → 20 → 25, and when the loaded set runs out it says "Showing all 25 loaded of 43 ads that spent" rather than implying 25 is everything). **Followers split out of Subscribers.** `meta_campaign_group()` matched 'follower' inside the subscribers branch, so every follower buy was reported as a subscriber buy — already known to be wrong and worked around rather than fixed, via the `spend_without_leads` exclusion whose own comment says "a followers buy reports `follow`, never `lead`". The new branch sits above subscribers and below thruplay (so a video-views campaign with "followers" in its name still reads as the objective it was bought on) and is word-bounded, so a "Follow Up" campaign is not swept in. Checked against every distinct Meta campaign name in both tables before applying: exactly three move, all genuinely follower buys; everything else keeps its group. At YTD that is **$54,997 of spend** that had been inflating Subscribers and distorting its cost-per-lead. **"Live ads that spent" is `spend > 0`, not `effective_status`.** Status is the ad's state *now* — filtering on it would drop every ad that ran during the window and has since been paused, shrinking last week's report a little more each day. `verify_v2_schema.sql` fails if that filter ever appears. **CPA is spend ÷ conversions and is null, never 0**, when nothing converted: an ad that converted nobody has no cost per acquisition, and 0 would rank it cheapest. **On "best performing copy": it is not in this database, and the card says so rather than showing a blank.** The sync requests `creative{id,thumbnail_url,body,title,object_type}`; Dynamic Creative keeps its variants in `asset_feed_spec` and the winner is only available through a per-asset insights breakdown — neither is fetched, as the sync's own comment admits. Measured on the week to 2026-08-31: 57 ads spent, 57 matched a creative, 57 had a thumbnail, **only 20 had body copy**. So `copy_source` comes back beside the copy and the page prints "not synced" for the rest, because an empty cell reads as "this ad had no copy" when the truth is "we did not fetch it". Closing that gap is a sync change (add `asset_feed_spec` to the creative fetch), not a reporting one. Built as a separate RPC rather than a sixth in-place patch of `wow_paid_media`: different grain, different source table, no shared logic beyond `meta_campaign_group()`, which both call so the objective names match across the two cards. |
| 2026-09-01 | Marketing | **Organic posts in Week over Week are now live instead of retyped, and every "top performer" bucket is verified at all four grains.** The report carried a manual table — twelve blank rows headed "Meta & Instagram organic — last 12 posts", hinted *"Type these in. Fills in automatically once Meta access is granted."* That access was granted: the ad-platforms sync has been filling `instagram_media_insights` since 2026-05-21 (205 posts) and Marketing Overview has rendered it on its Organic tab the whole time. Somebody has been retyping numbers that were already in the database. New `wow_organic_posts(report_date, grain, limit)` — grain-aware through the same `wow_window`, so organic describes the same period as everything beside it — returns period totals (posts / views / reach / engagement) against the prior period and last year, a media-type split, and the top posts. **Ranked by views, and the card says so**: views, reach and engagement disagree about which post won, the same trap already documented on `launch_product_sales_v` where the top product by units was not the top by sell-through and got reported wrong once. **Engagement is likes+comments+shares+saved, and engagement_rate divides by REACH (people), never views (plays, which one person can run up)** — null rather than 0 when reach is 0, because "nobody saw it" and "everyone who saw it ignored it" are different findings. **Coverage is returned, not assumed.** Organic history begins 2026-05-21, so a YTD window asks for January and gets May onward — most of the year missing, sitting next to sales figures that genuinely start Jan 1. `history_starts` comes back on every call and the card marks the period partial whenever the window predates it (verified: the banner fires at YTD and stays hidden at day/week/MTD). At YTD the vs-prior-period change is correctly **null**, not a bogus jump, because the prior window predates coverage entirely. `thumbnail_url` is null for IMAGE and CAROUSEL_ALBUM — Meta only returns one for video (0 of 138 videos null; 35 of 35 images and 32 of 32 carousels null). That is the API's shape, not a sync bug, so the card draws a placeholder rather than a broken image. The manual table is removed from the page; anything previously typed still sits in `wow_report_entries.sections.tables.organic_posts`, unrendered rather than deleted. Organic also joins the CSV export, since a partial export reads as a section with no data rather than one the file forgot. **Bucket audit across day/week/MTD/YTD** (the actual ask, and it found nothing broken): top products 10, categories 12, channels 9–10, creatives 10, Meta objective groups 3–5, Google campaigns 5–8, landing pages 15, discount codes 20, organic posts 2–12, funnel populated. Two buckets first read as empty at every grain — that was the audit query guessing the wrong JSON keys (`rows` instead of `pages`/`codes`), not the data. |
| 2026-09-01 | Marketing | **WoW YTD, final piece: the page fired all seven RPCs at once and the two heavy ones lost the race.** After the rollup landed, the edge log showed the failure had narrowed to exactly one call — `wow_report` 500, every other RPC 200 — with `wow_cohorts` also failing on an earlier attempt. `wow_report` measures **3.1s alone** at YTD against the 8s `authenticated` timeout, so on its own it is fine; fired alongside six others it inflates past the ceiling, because seven concurrent readers of the same large tables contend for CPU and buffers. The page now loads in two batches instead of one `Promise.all`: `wow_report` + `wow_kpi_compare` (~0.8s) first, then the remaining five. Every call in the second batch is sub-second except `wow_cohorts` (~3.7s), which therefore runs essentially alone. Costs about a second of wall clock at YTD and is the difference between a slow report and no report; the headline KPIs also land sooner. Two more index attempts were tried and reverted on measurement, both recorded so they are not repeated: an index on `shopify_orders (company_entity_id, customer_id, shopify_created_at) where cancelled_at is null` — aimed at the returning-customer join, which genuinely does scan all 565,132 orders for want of a `customer_id` index — made `wow_report` **worse and consistently so** (3.5s → 5.9s, 6.3s across repeat runs) because the planner switched to a poorer overall plan, so it was dropped rather than kept on the theory that it ought to help; and an expression index on the `shopify_created_at::date` predicate, which is impossible rather than merely unhelpful — the column is `timestamptz`, so that cast is STABLE, not IMMUTABLE, and cannot be indexed at all. |
| 2026-09-01 | Marketing | **Week over Week YTD still timed out after the CTE fix — the real cause was parallelism, and the fix is a sales rollup.** Narrowing the CTEs halved each call and was not enough, because the page does not make one call: it fires **seven RPCs in parallel**, and the Postgres log for a single YTD load shows seven cancellations inside forty seconds, three of them within 21 milliseconds of each other. Measured serially and warm `wow_report` was 5.0s against the 8s `authenticated` timeout; measured the way the page actually loads, seven concurrent readers of the same 2.1GB table contend and all inflate past the ceiling together. No column-list tuning fixes that — the work itself had to shrink. `sales_by_day` is one row per SKU per location per day (1,139,541 rows), and every figure in the report except Top Products is consumed far coarser. Distinct `(company, location, day, product_type)` is **136,994 rows — 8.3x fewer**; `(company, location, day)` is 12,315 — 92x. So `wow_sales_daily_type_mv` rolls up once nightly at the product_type grain and the two heavy functions read it: a YTD window drops from ~460,000 rows to 15,639, and reading it now measures **0.00s**. **wow_kpi_compare 6.88s → 0.77s (9x). wow_report 8.8–12.5s → 3.09s.** Total DB work per YTD page load fell from ~14s to ~9s with the two heaviest cut hardest; nothing dominates any more (`firsts` 1.24s, top products 0.93s, channels 0.43s). Top Products is deliberately NOT served from the rollup — it needs `product_name`, which only collapses 1.6x, and it only ever reads the current window, so it stays on the base table over a much smaller range. **Correctness was proved, not assumed.** Every measure carried is a SUM, so re-summing partials is exact (nothing averaged, no distinct counts — those would not survive pre-aggregation and none are taken from this); the migration compares rollup against base and raises if any measure differs by a cent. The functions were then diffed key-by-key against a reconstructed pre-rollup copy **in the same transaction**, which mattered: a naive before/after hash showed all four grains CHANGED, but that was a sync landing between snapshots. The controlled diff found exactly one difference — `top_products` at day grain — and comparing products, units and sales row by row returned zero differences: only array order among ties, from `order by units desc` having no tiebreaker. Pre-existing non-determinism, not a regression. `location_tag` is stored already-normalised, keeping the defensive `lower(btrim(...))` behaviour while making the column indexable, which the expression never was. The matview follows the `inventory_on_hand_current_v` layering exactly — no RLS on the matview, no grant to anon/authenticated, tenant filter in a `security_invoker = false` wrapper — and `verify_v2_schema.sql` now fails if the unique index, the invoker setting, the grant posture, the refresh function, or either function's use of the rollup regresses. Refresh is wired into `scripts/shopify-sync.mjs` beside the velocity and inventory refreshes, on a path no sync variant skips. |
| 2026-09-01 | Marketing | **Week over Week YTD failed with "canceling statement due to statement timeout" — fixed, and the reason is worth knowing.** `wow_report` and `wow_kpi_compare` both opened with `sbd as (select s.* from sales_by_day ...)`. `s.*` is ~30 columns of a 1.1M-row / 2.1GB table, and a CTE referenced more than once is materialised into a tuplestore — so every column was copied for every row in the window while the two functions read **eight** of them between them. Invisible at 7 days (a few thousand rows); at YTD the windows are 474 and 600 days — 405,896 and 461,079 rows — and the copying was most of the runtime. Measured on prod, same rows, same plan, only the column list changed: **3.67s → 0.83s (4.4x)**. `authenticated` carries `statement_timeout = 8s`, so `wow_report` at 8.8–12.5s did not merely feel slow, it failed. End to end: report **8.8–12.5s → 5.0s**, kpi_compare **5.6–6.2s → 3.9s**, both under the cap, and every one of the seven RPCs the page fires now clears 8s at YTD. Output verified **byte-identical** (md5 of the full jsonb) at all four grains — only the column list moved. **Why the original testing missed it:** every grain was measured individually on the MCP connection, which runs as `postgres` and has no statement_timeout at all; `set local role authenticated` does *not* apply a role's `rolconfig`, so the ceiling that actually breaks the page was never in play. Timing checks against a role limit have to set the timeout explicitly. Two fixes were tried and rejected on measurement, recorded so nobody repeats them: an expression index on `(company_entity_id, lower(btrim(location_tag)), day_date)` — built, ANALYZEd, and the planner still correctly preferred `sales_by_day_company_day_idx` (forcing anything else measured 15.7s vs 0.97s), so it was dropped rather than left taxing every nightly sync write; and dropping the `lower(btrim(...))` normalisation so the plain index applies — every `location_tag` is normalised today so it would work today, and would silently start dropping rows the first time a sync delivers `'Online'`. `verify_v2_schema.sql` now fails if either CTE returns to `select s.*`, since a migration rebuilding these from the older repo text would reintroduce it and the symptom (only the widest grain, only for real users, never for a superuser) is expensive to rediscover. |
| 2026-09-01 | Marketing | **Week over Week reads at four grains — day, week, month-to-date and year-to-date, each with its own variance against the previous period and last year.** The report was built weekly and the 7-day window was hand-typed into every RPC: eight copies of `p_report_date - 6 / - 13 / - 364`. Adding three more periods that way would have been 32 copies of the arithmetic, so the window is now ONE function (`wow_window(report_date, grain)`) that all eight report RPCs delegate to — a grain cannot mean one thing in the KPI band and another in the funnel below it. Each RPC gained `p_grain text default 'week'`, so every existing call is unchanged; verified by regression, not assumption — week grain returns 259,471 total / 223,783 net for 2026-08-19..25, the exact figures already documented against Shopify's own report. **Month and YTD are to-DATE, not whole periods**: a report dated the 12th compared against a complete prior month reads as a 60% collapse every single time. MTD compares Aug 1–25 against Jul 1–25; the prior-month window is clamped to its own month end so Mar 31 does not run back into a February that has no 31st. **YTD's "previous" is deliberately not last year's YTD** — that is what the LY column already holds, and two columns showing the same window read as corroboration when they are duplication; it is the equal-length window ending Dec 31 instead (on Dec 31 itself the two legitimately coincide, and the period bar prints both windows' dates so the reader sees it). Day and week keep the 364-day last-year offset so weekdays align; month and YTD align on the calendar date. Every "prior week" label on the page now comes from one vocabulary keyed by grain — period bar, funnel and category headers, KPI band delta (WoW/DoD/MoM/vs prev), YoY tooltips, CSV headers and filename — because a month-to-date column headed "vs prior week" tells the reader the number means something it does not. Grain persists in `?grain=` as well as localStorage, so a link someone pastes opens on the period they were reading. Notes are now keyed `(company, report_date, grain)`: without that the daily view would have silently overwritten the week's commentary for the same date. One non-obvious trap found in testing: `wow_window` is a set-returning function, estimated by the planner at 1000 rows by default, and every RPC joins it as `cross join w` — `wow_kpi_compare` went from sub-second to timing out past 60s until it was marked `ROWS 1`. `verify_v2_schema.sql` asserts that, and asserts each RPC actually delegates its window rather than quietly keeping a private 7-day CTE (the pre-existing 364-day check was repointed at `wow_window`, since that literal moved). Verified in a browser across all four grains: correct `p_grain` on all six calls per switch, labels coherent, no page errors. |
| 2026-09-01 | Accounting | **"Build & post journal entry" on Accounting Export** — the earlier "Post adjustment" button opened a blank composer, disconnected from the entry this page already computes. Corrected: a new button extracts the ACTUAL lines (`salesEntryLines()` per location bucket, `feeRowsByStore()` per payout store — the same functions the Sales Journal card and the Journal CSV export already call, not a reimplementation) and stages them through `v2/je-composer.js` in one shot. Verified against a real month pulled from prod (August 2026, Baseballism, all 12 locations): 106 lines, balances exactly ($2,924,220.37 both sides), zero unmapped. Refuses to build rather than posting garbage when a location's revenue/refunds account or the fee/clearing accounts are unmapped, naming which ones. Surfaces one real constraint of the current chart rather than hiding it: `accounts_receivable` maps to a true `Accounts Receivable`-typed account, and QuickBooks requires a customer on every line that hits an AR/AP account — a real month has ~22 such lines (verified: exactly 22 of 106 for August) with no natural single customer for aggregate Shopify revenue. Said up front with the real count, not left as a surprise at the review step; the actual fix (repoint the mapping at a non-AR clearing account) is a chart decision left to whoever owns it, not decided here. 
| 2026-09-01 | Accounting | **je-composer.js stopped rendering every line's account/location/entity as a live `<select>`** — found while building the button above, which stages entries far bigger than anything the composer had been used for. Each line carries ~936 options across three selects (450 accounts, 64 locations, 422 entities); opening a 106-line entry (a real month's Accounting Export build) measured **5,594ms** in Chromium — the identical 5.1s-to-open-a-batch failure Card Coding's table had to fix once already, reproduced in a second place because it shares no code with that fix. Account/location/entity now render as a label button showing the current value, becoming a real `<select>` only on click and collapsing back to a button the moment a value is picked — the table stays light after editing too, not just before. postingType stays a live select (two fixed options, free) and amount/description stay plain inputs. Measured after: **74ms** to open the same 106-line entry, a single cell edit **29ms** flat regardless of row count. Re-ran the existing 30-assertion Chromium behavioral suite against the new interaction model (click-to-edit instead of always-live selects) — all 30 still pass, including the AR-entity requirement, the stale-entity reset when an account changes back to non-AR, and re-staging replacing lines rather than duplicating them |
| 2026-09-01 | Accounting | QuickBooks Reports gets **by-month columns** and a **drill-down**. A By month checkbox adds `summarize_column_by: 'Month'` for the summary-shaped reports (Balance Sheet, P&amp;L, Trial Balance) — auto-disabled for the detail reports where QBO doesn't support it, rather than sending a param that silently does nothing. Clicking an account row no longer jumps straight to the adjustment composer — it now opens that account's transactions for the current period (a `GeneralLedger` fetch reusing the same `flatten()` the main report already uses, so adding this cost no new row-rendering logic), and clicking a Journal Entry row within THAT drills one level further into the entry's own lines. Posting moved into the drill-down's own footer ("Post adjustment for this account"), not lost — just no longer the only thing a click could do. Confirmed the QBO row shape before building anything: pulled a real GeneralLedger run from prod and found Journal Entry rows carry the entry's own id on the Transaction Type cell (`{id: "74905", value: "Journal Entry"}`) — that id is what makes the second drill-down possible. New capability in `quickbooks-report` (deployed v2, deployed source read back to confirm it matches disk): given `journal_entry_id` instead of `report_name`, it fetches ONE JournalEntry by id and returns it directly — same connection/token/permission resolution as a report fetch, deliberately NOT logged to `quickbooks_report_runs` since that table means "a report snapshot," not "someone clicked a row." The JE detail view enriches with SILO's own posting record when one exists — pulled a real stored `readback` from the one entry SILO has posted (the Divvy entry, now voided) to verify the rendering against genuine Intuit response shape (`AccountRef.name`, `Entity.EntityRef.name`, `DepartmentRef.name`) rather than a guessed one. Verified the two new render functions with the actual shipped code (extracted and run in Chromium, not reimplemented) against that real GeneralLedger row and real JE readback: 10/10 — two Journal Entry rows detected by id in order, a non-JE row present but correctly not clickable, debit/credit totals balance, the SILO-posted badge appears only when a matching posting row exists and shows the right source/status, and a null entry renders an empty state rather than throwing |
| 2026-09-01 | Accounting / Ask SILO | **Post adjustment reaches Accounting Export too**, and Insights' replacement got seeded rather than left as a plan. `/v2/accounting-export.html` gets the same composer as QuickBooks Reports — a header button plus a new Adjustments tab listing staged and posted entries (Open / Discard / Void), same `SiloJE.open()` call, same stage-then-review flow, no new backend. Entry date defaults to the LAST DAY of the period month being reviewed rather than today, so an adjustment raised while looking at July's export doesn't silently land in August (`Date.UTC(y, mo, 0)` — verified against Feb/Aug/Dec rather than assumed correct for leap years). Separately, **six saved reports seeded** in `silo_chat_saved_reports` — Ask SILO's "Save report" pin-and-rerun mechanism doing the job the retired Insights digest used to, without a second aggregator page: sales pace vs last year, AR aging, AP overdue, plus two the old engine never had (uncoded card-coding backlog, unapproved journal adjustments). Every query verified through the ACTUAL rerun path (`chat_run_readonly_query`, impersonated as blake@baseballism.com) rather than assumed from a superuser preview — which caught a real near-miss: the stockout-risk query, checked first as a bare superuser query, came back 0 rows and was nearly saved with a false "nothing at risk" answer. That 0 was `active_company_id()` resolving null in an unauthenticated connection — the exact trap `inventory_workboard_v`'s own doc comment warns about — not an empty result; re-verified through a real authenticated session, the true number is 231 SKU/location combinations within 14 days of stockout, including live online sellers, not zero. Rewrote that report's answer with the real figure before it ever became a wrong 'all clear' someone would have trusted |
| 2026-09-01 | Nav / Action Items | **Action Items & Insights retired entirely** rather than extended, after being asked to look into why it felt outdated. Two separate findings, both verified rather than assumed: (1) `compute_silo_insights()` shipped 2026-07-09 and was never touched again — it knows nothing about Card Coding, Journal Adjustments, Comp Requests, Mailroom, Product Concepts, or Redo Returns, everything shipped since. Its six original domains (sales pace vs prior year, inventory stockout/dead stock, purchasing draft/overdue POs, launch readiness/overdue tasks, AR aging, AP overdue/large payment requests) still ran correctly against live, unstaled data when checked — schema intact, no column drift, `ar_invoices` synced within the day — so this wasn't a bug fix, it was a call that the module wasn't worth keeping current. (2) The half that had been silently broken since the feature existed: the nightly AI narrative needs the `ANTHROPIC_API_KEY` GitHub Actions secret, which was never set on this repo. Pulled the actual job log from the run that generated the last real digest (2026-08-31 20:42 UTC) and it says so directly — `ANTHROPIC_API_KEY:` blank in the env dump, `[insights] ANTHROPIC_API_KEY not set — storing findings without a narrative`. Every digest ever generated hit this path; the "Briefing" card had shown the fallback placeholder on every single load since the page existed, never the real thing. Removed: `v2/insights.html`, its nav entry, the Action Items link on `/v2/finance.html`'s Workspace card, the "Generate Action Items & Insights digest" step in `shopify-sync.yml`, `scripts/generate-insights.mjs`, and — on prod, applied same-session per the repo's own drift rule — `compute_silo_insights()` and `silo_insights_digest` (`20260901030000_retire_silo_insights.sql`). The digest table held one upserted row per company with no history, so dropping it loses nothing a nightly job couldn't regenerate if the feature ever came back. `verify_v2_schema.sql` now asserts the RETIREMENT held (flags `UNEXPECTED` if either object reappears) rather than asserting the feature exists. Two now-inaccurate doc claims fixed in the same pass: `docs/ops/org-calendar.md`'s Agenda section described the insights engine in the present tense as still-existing prior art; `docs/ops/silo-chat-setup.md` claimed Ask SILO and the insights narrative "read the same key" for `ANTHROPIC_API_KEY`, which was never true — Ask SILO's is a Supabase edge-function secret, insights read a separate GitHub Actions repo secret, and conflating them is exactly the kind of claim that would have hidden this bug from a future read of that doc |
| 2026-09-01 | Accounting | Journal adjustments got a **review step** before anything reaches QuickBooks. The composer originally posted on a single confirm, which is how the card batches DON'T work — a card batch is coded, reviewed and approved before its JE is built, while a hand-written adjustment had no second look at all, on the one entry type where a person is typing amounts straight into the general ledger. Composing now writes a `draft` and switches to a review screen that **re-reads that draft from the database** and renders it as a journal entry: the point is that what you approve is what `quickbooks-post-journal` will rebuild and send, not what the form happened to hold. Only Approve & post reaches Intuit; Save draft parks it, and the Adjustments card on `/v2/qbo-reports.html` now lists staged entries alongside posted ones with Open / Discard, because a staged draft you cannot find again is an orphan and staging would just be a way to lose work. Reopening refills the form from the stored rows (`SiloJE.open({ adjustmentId })`), and re-staging UPDATES the same adjustment and replaces its lines rather than minting a second row — verified, one adjustment and two lines after an edit-and-restage cycle, not two and four. Approve and post stay separate writes so a failure at Intuit leaves a retryable `approved` entry rather than an untouched draft. Hardened one hazard found in review: entering the review step now disables the post button until `renderReview()` actually succeeds, and a staging or render failure returns to the form — otherwise a failed render left a live Approve & post button over a blank screen, which is approving an entry nobody managed to read. Verified two ways: the staging round-trip against prod as blake@baseballism.com (10/10 — stage, view read-back, re-stage without duplicating, approve, discard cascading its lines, nothing left behind), and the two-step UI driven end to end in Chromium against a stubbed backend (31/31 — including that NOTHING is posted at the staging step, that an AR account demands an entity and reverts cleanly when the account changes back, and that an unbalanced entry cannot reach review) |
| 2026-09-01 | Accounting | **Post adjustment** — a journal entry can now be written from where the problem is noticed. `/v2/qbo-reports.html` gets a Post adjustment button, and any account row in a report (QBO stamps an account id on those rows) opens the composer with that account already on line one; posting re-runs the report, so the adjustment is checked against the same numbers that prompted it. The widget is `v2/je-composer.js`, deliberately page-agnostic (`SiloJE.open({db, companyId, context, prefill, onPosted})`) so Schedules and the prepaid amortisation work can raise the same entry without a second implementation. It validates while composing rather than at post: balance, at least two lines, a memo, and an Entity on every AR/AP line — QuickBooks refuses the WHOLE entry over a missing entity, so catching it in the browser costs nothing while catching it at Intuit costs a claimed posting row and an opaque error. Adjustments **stage in the database first** (`journal_adjustments` + `journal_adjustment_lines`, `20260901000000`), because `quickbooks-post-journal` rebuilds every entry it posts from the database and never takes one from a page — so the composer's job is to write a row, not to hand over a payload. That function now serves both sources through ONE write path (`batch_id` or `adjustment_id`, deployed v4): the guarantees that matter — rebuilt from the database, claimed under the partial unique index BEFORE Intuit is called, read back and compared afterwards — are shared, so a second posting function would have meant a second place to get all of them wrong. The card-batch path is unchanged. **Two gaps closed in the same pass, both found by asking what happens after a post rather than up to one.** (1) `void_card_posting` is hardcoded to `source = 'card_import'`, so adding a second posting source without a matching void would have rebuilt, one surface over, exactly the trap that function exists to undo — an entry deleted in QuickBooks leaves SILO claiming a post, and the double-post index then refuses to ever post that source_ref again. New `void_journal_adjustment` (`20260901010000`), same SECURITY DEFINER shape and same required reason, surfaced as a Void button on an Adjustments card on the reports page so the remedy no longer needs raw SQL. (2) `journal_adjustments`' and `card_import_batches`' write policies guarded the wrong half: `USING` refuses to EDIT a posted row, which reads like the whole rule, but `WITH CHECK` governs the row being WRITTEN and neither constrained the incoming status — so a finance user could move a draft straight to `'posted'` from the browser (USING saw the old status and passed, WITH CHECK never looked) and SILO would then show, and refuse to correct, an entry Intuit never received. That is the precise failure the posting design exists to prevent, reachable without going near the posting path. `20260901020000` adds the status clause to both halves on both tables; verified by impersonating blake@baseballism.com — insert-as-posted and update-to-posted are refused, while draft → categorized → approved and deleting one's own draft still work, so the card flow is unchanged. Nothing client-side ever wrote that status, so the tightening costs nothing today and closes the hole before the adjustments UI made it reachable |
| 2026-08-28 | Ask SILO / Dashboards (v3) | **Save report → add to a dashboard, in one step**, plus a mobile fix. Ask SILO's save dialog now offers a dashboard target (any you can edit, or a new one); saving writes the report, then hands off to `/v3/dashboard.html?id=…&edit=1&add_report=…`, which adds the widget, recommends a visual from the columns the query actually returns, and saves. The report is written BEFORE the hand-off, so any failure past that point degrades to "saved, not added" rather than losing work; `add_report` is stripped from the URL on arrival so a refresh cannot add the same report twice; a report with no stored SQL or an unknown id is refused with a reason instead of becoming a dead tile. It is a redirect rather than v2 building the widget itself on purpose — the dashboard page owns profiling and recommendation and should stay the only place that does, and a v2 page importing `/v3/` code would invert the dependency and leave two recommendation paths to drift apart. **Mobile was broken and is fixed:** the 12-column grid kept its columns at 390px, so a 6-wide tile was half a phone screen — axis labels overlapped the plot and a KPI clipped mid-number. It now collapses to one column below 700px. The subtle half of that fix is in `layout()`, which refuses to serialise a collapsed grid: without it, one save from a phone would overwrite the real 12-column layout with the phone's, for every viewer. Verified: 27 new browser checks covering the whole hop from a restored chat answer through to the saved widget, including the refresh-duplication and phone-layout cases. |
| 2026-08-28 | Dashboards (v3) | **Seeded four central SILO report definitions, and hid the feature from the nav while it is in testing.** Daily Sales, Top Products (30d), Sales by Location (30d) and Open Purchase Orders ship as `source = 'system'` rows — global (`company_entity_id IS NULL`), one definition reused by every tenant, safe because the SQL runs through `chat_run_readonly_query` under each viewer's own RLS. They exist so a dashboard has something to build on before anyone has saved an Ask SILO report, and so each of the four visuals has a natural example; each carries its own `columns_metadata`, so it formats right on first render. **The constraint worth remembering: every definition reads a `security_invoker` view or an RLS-enabled table, never a materialized view.** Postgres does not enforce RLS on matviews — the three sales/inventory matviews all carry `company_entity_id` but none can filter on it by policy, so a global definition querying one would hand every tenant's rows to every tenant. `inventory_workboard_v` is avoided for a duller reason: it already exceeds the 30s timeout. Idempotent via fixed UUIDs and `on conflict (id) do nothing`, not `do update` — `apply_all_post_merge.sql` is re-run for rebuilds and a do-update would silently discard a correction. The nav row in `nav-config.js` is commented out rather than deleted, same stance as returns-overview: the pages work for anyone with the URL and RLS is the boundary either way, so this only controls who discovers it. Verified by building the two source views verbatim from their own migrations and executing all four seeded queries through the real `chat_run_readonly_query` — every column name resolves, Package Protection is correctly excluded from the product ranking, and received POs are correctly excluded from the open list. |
| 2026-08-28 | Dashboards (v3) | **Semantic column typing, aggregation controls, and a visual picker that looks like one.** `silo_chat_saved_reports.columns_metadata` now carries what each result column MEANS (currency / count / percent / date / category), resolved in `v3/js/field-semantics.js` through four layers: widget override → the report's saved metadata → `silo_chat_schema_catalog` → name heuristics. The catalog layer is the one that matters: Postgres already knows `units` is `integer` and `net_sales` is `numeric`, so **an integer column is a count whatever its name says** — which fixes `total_units` formatting as currency at the root rather than by adding another word to a regex. Metadata lives on the REPORT so one correction fixes every widget built on it, and only grounded answers are seeded (writing a name guess into `columns_metadata` would launder a guess into an authoritative record). Semantics also decide the default aggregation: sum for currency and counts, **avg for percent** (40/50/60 averages to 50 and sums to 150). Charts gained a real Aggregation control, and grouping now happens BEFORE sort and limit — top-10-then-sum is a different and wrong answer versus sum-then-top-10 — with the roll-up named in the tile footer since it is otherwise invisible. The visual type in a tile header is now a button with a caret rather than a status pill; switching to KPI shrinks a chart-sized tile to KPI size. **Two real bugs found by testing:** `grid.save()` omits a geometry property matching the item's min/default, so a KPI at `h=2` with `gs-min-h=2` saved with no height and reloaded at the default — quietly breaking reload-identically; geometry is now read from each item's live `gridstackNode`. And `visual_config.value_format` was a second, redundant override path alongside `field_semantics`; removed, one source of truth. Slicers, report parameters, an "Add to dashboard" button inside Ask SILO, and AI-authored config remain deferred. Verified: 67 unit assertions, 61 browser checks, 30 RLS assertions, all verify blocks ok. |
| 2026-08-28 | Reporting architecture | **`silo_chat_saved_reports` became the generic saved-report layer**, so Ask SILO is one authoring surface rather than the only door into dashboards. Added `source` (`ask_silo` / `manual` / `system`) and `description`; made `question`/`answer` nullable (a system report was never a conversation). The table's shape was already generic — title + `queries_run[]` + visibility + company — and `chat_run_readonly_query` was already a generic SECURITY INVOKER read-only runner, so the engine was shared before this; what was missing was a way to say where a report came from. **A `system` definition is global** (`company_entity_id IS NULL`): one "Daily Sales" row reused by every tenant, safe because its SQL executes under the caller's own RLS. NULL company is a privileged state guarded three independent ways — a table CHECK (`company_entity_id is not null or source = 'system'`), an INSERT policy requiring `IS NOT NULL` and `source in ('ask_silo','manual')`, and an UPDATE policy whose USING is false for global rows, so no client (exec/owner included) can edit, delete or promote one. Verified on a real Postgres 16 against a pre-migration copy of the schema: 30 RLS assertions covering cross-tenant visibility, private-report scoping, and every promotion path. One claim corrected while testing: the explicit `company_entity_id IS NOT NULL` in the INSERT policy does **not** fix a live hole — the old `= active_company_id()` already denied a null-company insert, since `NULL = NULL` is `NULL` and RLS treats non-true as failure. It stays because it states the rule rather than leaving it emergent from three-valued logic (any NULL-tolerant rewrite of that comparison would silently remove the protection) and because `verify_v2_schema.sql` can assert a clause it cannot assert an accident. `dashboard_widgets` is deliberately **unchanged** — that was the point: whatever a future report source turns out to be, no widget row migrates. v3's picker now lists every source with a provenance badge and shows `description` where an Ask SILO row shows its question; Ask SILO's own modal filters to `source = 'ask_silo'` so system definitions never appear there as something a user saved. Table rename deferred. |
| 2026-08-28 | Dashboards (v3) | **New `/v3/` dashboard runtime** — saved Ask SILO reports arranged on a drag/resize canvas, stored as configuration rather than as HTML. A dashboard is rows in `dashboards` / `dashboard_widgets`: which saved report feeds a tile, which of that report's queries (`queries_run` is an array — the picker asks rather than silently taking the first), which of five visuals draws it (table / KPI / bar / line / donut), and where it sits on a 12-column GridStack grid. Switching a tile from a table to a bar chart is a one-field update and a re-render — no new page, no LLM call, no deploy. **Deliberately no new `saved_reports` table**: `silo_chat_saved_reports` already stores the question, answer and exact SQL, and Ask SILO's "Refresh data" already re-runs it through `chat_run_readonly_query`; a parallel table would have forked that one artifact and split refresh across two code paths. That RPC is `SECURITY INVOKER`, so a dashboard can never show a viewer data their own RLS would not — and its 500-row cap and 30s timeout surface on the tile rather than being silently absorbed. Adding a widget profiles what the query actually returned and suggests a visual from the shape of the data (deterministic, not an LLM call): a date dimension gets a line, a top-N list gets a **bar not a donut** (few rows is not evidence of a composition — a top-4-products query is a ranking, and drawing it as a donut invites reading the top seller as a share of all sales), and `total_units` formats as a count while `net_sales` formats as currency. Sort/limit apply to the returned rows, not to the report's SQL, and the UI says so. Editing is buffered with client-minted widget ids so Save is one idempotent upsert — a re-save after a failure cannot duplicate tiles. View and edit mode share one render path on purpose, since two paths is the usual way "reload it identically" stops being true. In the nav under Reports, exec-only during soft launch, same posture as Ask SILO. Three `[hidden]` bugs were caught and fixed during the build — `.v3-blank` and `.v3-meta-bar` both set `display`, which beats the UA's `[hidden]` rule, the same trap `beacon.css` already documents for `.bcn-btn`. Filters, an "Add to dashboard" button inside Ask SILO, and AI-authored widget config are deliberately **not** in this pass — see `v3/README.md`. |
| 2026-08-27 | Performance Reviews | Rating questions are **1–4** now, not 1–10, and a rating no longer arrives as a bare number: every scale question renders selectable score buttons with a "Why this rating?" box **beside** them (they wrap to two rows on a narrow screen; a legacy 10-button scale wraps internally rather than squeezing the box off the row), and the two are stored as ONE answer in the existing `review_answers.value` jsonb (`{"score": 3, "comment": "..."}`) rather than a new column — which is why `review-portal`'s edge function, which returns `value` whole, needed no redeploy. The score and the justification are two inputs writing one object, so both handlers spread the existing value; a plain reassignment (the shape the old single-input code used) would have had each wipe the other. **The justification is not optional.** Send is blocked while any rating carries a score with nothing written behind it — a score with no reasoning is the exact thing the field exists to prevent, and a review locks once sent, so Send is the last point the gap can be caught. The offending question is highlighted and scrolled to, and the message names the score given rather than just saying something is missing. Save draft stays unconditional so half-finished work can always be parked, and a **resend** is exempt: those answers are already locked, and a review sent before this rule existed must stay resendable. The rule reaches a legacy 1–10 rating too, and never touches free-text or goals questions — the only enforcement it adds beyond "back up what you scored" is that a question already flagged `required` must actually carry a rating. `scale_1_10` stays in the kind CHECK and in each renderer's `SCALE_MAX` map on purpose — a stored score only means anything next to the kind that produced it, and dropping the kind would silently reprint every historical 7/10 as 7/4; the migration converts existing 1–10 questions to 1–4 only where no answer holds a score above 4. Goals gained a real due date end to end: `employee_goals.target_date` has existed since phase 1 but was only writable on goals created inside the review being edited, so a goal carried forward from a prior cycle could never be given a date or have a slipped one moved — the only way to re-date it was to drop it and retype it as new, losing its history. Prior goals now carry the same date field as new ones, the editor and `/v2/my-review.html` order goals soonest-due-first (undated last, backed by a new `employee_goals_target_date_idx`), the public portal sorts client-side, and a past-due `open`/`carried` goal is flagged overdue on all three. Dates are formatted from their `YYYY-MM-DD` parts, never `new Date(str)`, which reads the string as UTC midnight and prints the day before for a Pacific reader. `20260827200000_review_scale_1_4_and_goal_dates.sql`, applied to prod and verified by round-trip probe. **Template data fixed alongside the code:** "2026 Performance Evaluation" (both the published copy and its draft duplicate) had Quality / Productivity / Attendance / Communication stored as `free_text`, so managers typed a number into a prose box — while the template's own grading legend was already written as a four-point scale ("Does not meet / Fully meets / Exceeds / Far exceeds"). The template was always meant to be graded; the questions were created as the wrong kind. Converted those 8 rows (4 per copy) to `scale_1_4` in place, guarded on having no answers, so the in-flight draft review picked it up without being re-created; `Manager's Comments` stays `free_text` and `Goals` stays `goals`. Worth noting the general gap this exposed: a published template locks its questions (so existing answers stay valid), and the only supported way to change a question's KIND afterward is duplicate-as-draft → edit → publish, which strands reviews already pointing at the old template. Letting the builder edit a published template that has zero answers would close it — not done here, since it loosens a deliberate safety invariant. **Template preview caught up too:** every question kind previews the SHAPE it renders on a real review — the scale previews its buttons, a choice question its options, free text its box — but `goals` was the one exception, described in a sentence ("Previous goals (status updates) + new goals for next period") rather than shown. So the due-date fields an exec is deciding to hand every manager were invisible until an actual review existed. The preview now renders the real shape: a prior-goal row with Due and Status, a new-goal row with Goal and Due, the "+ Add goal" affordance, and a note that goals live on the EMPLOYEE and carry across cycles rather than being stored on the template — which is also why it shows empty fields instead of invented sample goals. The inline styles that had accumulated in the preview moved into `.tp-pv-*` classes alongside the rest. **Found in passing:** prod's kind CHECK already allowed a `rating_scale` value present in no migration, page, or edge function in this repo and used by zero rows — someone widened the constraint straight against prod for a question type that was never built. Preserved, not removed; flagged in CLAUDE.md |
| 2026-08-27 | Marketing | `wow_paid_media_reality(date)` — the honest denominators next to the claimed ones. `wow_paid_media` reports `roas = conversion_value / spend`, but `conversion_value` is what the PLATFORM claims it drove inside its own attribution window; the platforms overlap and none subtract each other. Measured on prod for the 30 days to 2026-08-26, Meta ($1,165,333 claimed) plus Google ($213,658) claimed **93.1% of all online net sales** ($1,481,772). The new RPC re-attributes nothing — guessing would produce a confidently wrong number, worse than a blank — and instead returns what cannot double-count: **nCAC** (spend ÷ new customers), online and blended **MER**, and **claim_ratio** as an explicit diagnostic. New customers come from `shopify_customer_metrics_daily`, never `shopify_orders` (partial backfills — Aug–Oct 2025 and Jan/Mar 2026 absent — would count a returning customer as new); customers are scoped to online domains, since paid spend does not buy a POS sale. `ga4` is excluded from every rollup: it is analytics, not an ad platform, and its 628,511 30-day "conversions" are key events. Also surfaces `platforms_not_synced`, derived from `ad_platform_connections` via a new SECURITY DEFINER helper `ad_platforms_expected()` — never a hardcoded platform list, because a platform nobody connected is not a failed sync and a permanent false alarm is worse than no alarm (TikTok has no connection row, so it is correctly silent). The helper is definer for the same reason `is_employee_manager()` is: `ad_platform_connections` is `is_admin_user()`-gated since its rows carry OAuth tokens, so without it a non-admin would read zero connections and the alarm would be permanently empty for them. Catalog note added to `marketing_kpis_daily` so Ask SILO stops reading `conversion_value` as revenue. RPC only — no page change yet |
| 2026-08-27 | Nav | The sidebar's "Jump to…" box is a real command palette now — it rendered a ⌘K affordance but nothing handled a click on it, and the keybinding called `.focus()` on a bare `<div tabindex="0">`, so it had never done anything. It filters the menu from the same `SiloNav.navSectionsForCompany()` call the sidebar renders from, so profile/department/role/`grantTable` gating applies unchanged and pages deliberately kept out of `nav-config.js` stay unreachable through it. Ranking is label-prefix → label-substring → section hit → subsequence; ↑/↓/Tab move, Enter navigates, Esc and click-outside close. Alongside it, `Reports` (10 flat links, and the section every new BI page was landing in) split into **Sales** (6) and **Marketing** (3, incl. Week over Week). Ask SILO stays in its own `Reports` section on purpose during the soft launch — that section name is absent from `STANDARD_SECTION_ORDER`, so moving it would surface the link to standard-profile execs. `id`s keep their `reports/` prefix: they are the `active` keys the 9 pages pass to `SiloChrome.mount()` |
| 2026-08-26 | Accounting Export | QuickBooks Online integration, phase 1-2 (read-only): connect a QBO company and mirror its chart of accounts, so the Accounting Export mapping binds to real QBO account **ids** instead of hand-typed names. New `quickbooks_connections` / `quickbooks_oauth_states` / `quickbooks_accounts` tables plus `accounting_coa_map.qbo_account_id` (`20260826070000_quickbooks_integration.sql`), a QuickBooks card on `/v2/integrations.html`, and three edge functions (`quickbooks-oauth-start`, `quickbooks-oauth-callback`, `quickbooks-accounts-sync`) — sources only, not deployed. Nothing writes to the general ledger; posting journal entries is a later phase and is designed to require human approval per post. Also fixes the mapping card's save: it silently never persisted, because `loadCoaMap()` rewrote stale stored names in memory on every load (`LEGACY_RENAMES`), so "Apply recommended names" produced no visible change and nobody knew to click Save — and `_co` used the sync `getActiveCompany()`, which returns null in any tab that skipped `login.html`, dropping `company_entity_id` from the upsert. The masking is deleted rather than patched and the stored names promoted to the corrected set (discounts as contra-revenue, sales tax to `Sales Tax Clearing`, Shopify deposits to `Shopify Clearing` instead of generic AR, processing fees out of COGS) — safe to correct in place because nothing has been filed from these exports. See [quickbooks-integration.md](quickbooks-integration.md) |
| 2026-08-24 | Performance Reviews | New Compensation Requests workflow (`/v2/comp-requests.html`, nav `Team → Compensation`) — Team module phase 2, layered on the existing `employees`/`employee_managers` roster rather than a parallel one. A manager submits a raise/bonus/promotion/equity request for one of their reports (draft, freely editable, until submitted); it then routes to finance for review exactly like `payment_requests` does today — new `current_user_can_manage_comp_requests()` mirrors `current_user_can_manage_payment_requests()`'s membership/department gate (`owner_admin`/`admin`, or `finance`/`admin`/`exec` department) but is kept as its own function rather than reused, so the two approval paths (AP vs. comp) can diverge later without coupling them. Once submitted, only finance/exec/owner can move the row (submitted → in_review → needs_info → approved/denied) — matching `payment_requests_internal_update`'s "requester creates, AP owns the rest" boundary; the manager gets their draft-editing rights back only if it's still a draft. New `comp_adjustment_requests` + `comp_adjustment_request_activity` tables (`20260824000000_comp_adjustment_requests.sql`), `comp_adjustment_requests_v` reporting view, and a `comp-request-notify` edge function (source only — not yet deployed) emailing finance on submission and the requesting manager on decision, both via the entity_memberships-then-profiles two-step lookup (not a bare `profiles.department` filter) that fixed the same cross-company leak class in `sample-notify`'s logistics fallback |
| 2026-08-22 | Ask SILO | Product Concepts' phase-1 circuit breaker was hijacking the tester's ordinary data questions: it armed on conceptsEnabled alone (i.e. on EVERY question Blake asked), so any analytical question that legitimately ran 5+ tool rounds -- an overstock analysis, a sales-performance question -- got interrupted mid-investigation by a forced create_product_concept call, stamping junk "TBD Concept - Needs Direction" placeholders into product_concepts (two created live on 2026-08-21, both deleted) and prefixing answers with a confused disclaimer. The breaker now arms only when the conversation actually reads like product drafting (a user message carries concept/draft/collection-style language AND the latest message isn't shaped like an analytical question) -- verified against all nine of this week's real questions: the four analytical ones no longer arm it, the four real drafting asks still do, and "how did the Sonic collab perform?" correctly stays unarmed despite the word collab. A missed arm just means a slow draft goes unhurried -- far cheaper than hijacking a data question. Known remaining gap flagged for the Product Concepts owner: a phase-2 "build out the full plan" request can still arm the breaker and be told to create (not update) a concept -- pre-existing, not addressed here. Deployed as silo-chat v41 |
| 2026-08-21 | Ask SILO | Schema catalog + health scoreboard (the "give it a map, give Blake a number" pass). New `silo_chat_schema_catalog` table: one row per public table/view/matview with column names+types **auto-generated from `pg_catalog`** by `refresh_chat_schema_catalog()` (cannot drift from reality) plus curated `description`/`keywords` seeded verbatim from the prompt's old cheat sheet. The hand-typed schema block in `silo-chat`'s system prompt — root cause of every column-name failure this week (`on_hand`, `launch_window_start`, `po_lines.sku`, `factories.name`), each previously patched with a different band-aid — is **removed from the prompt entirely**; `buildSchemaSection()` now injects a per-question slice at request time (full columns for the ~8 most relevant tables via keyword/name matching with a workhorse-table fallback, a one-line index for the rest, graceful degrade to information_schema guidance if the catalog is unreachable). Credential/internal tables are `is_hidden` from the index (noise reduction; RLS stays the boundary). Re-run the refresh function after schema-changing migrations — `verify_v2_schema.sql` now spot-checks staleness (catalog column count vs live). Also new: `silo_chat_health_v`, the per-day reliability scoreboard (questions, error %, forced finals, avg/p90 rounds, distinct users) so "Ask SILO is broken" claims get settled by a number instead of screenshots. Catalog live in prod: 148 objects mapped, 26 curated, columns verified matching live schema |
| 2026-08-21 | Reports | Product Search felt heavy because it was: every load fetched one raw `sales_by_day_verification_v` row per product/location/day for the whole selected range and re-aggregated it in the browser — 50,502 rows for the default 30-day window, 159,167 for 90d, 291,092 for 180d, re-summed on every filter change. New `product_search_rollup()` RPC (`20260821180000`) does the day-level SUM in Postgres and returns one row per (location, sku, product[, month]) instead of per day — row count is now bounded by catalog size, not date range (90d: 159,167 → 18,102 rows; unaffected by widening the date range much further). Location stays in every fetch so the store-group filter and "Add Store column" toggle remain instant with no refetch; "Add Month column" now triggers its own refetch at month grain (same pattern as the window selector on Top Sellers) since the default fetch no longer carries day-level detail. Also added a "By product type" pivot panel alongside the existing row-level table — same filtered dataset, no extra query — matching the layout of the legacy Power BI report it replaced: click a type to filter Results to it (toggle off on a second click), synced with the existing Product type dropdown |
| 2026-08-21 | Reports / Shopify sync | Kalin caught Top Sellers row #33 ("Bubbles and Doubles Cap - Toddler", never actually launched) showing real sales/on-hand qty — a never-launched Shopify listing shared its SKU with the live "Bubbles and Doubles Cap - Youth" product. Root cause was in the sync, not the report: `runInventorySnapshot()` collapsed on-hand rows and resolved product titles keyed by SKU alone, so two DIFFERENT Shopify variants sharing one SKU got summed together under whichever title happened to be processed last (`scripts/lib/shopify-sync-core.mjs`, mirrored in `supabase/functions/shopify-sync-run`); the sales aggregation had the same shape, merging `sales_by_day` rows by (date, location, SKU) and preferring a SKU-keyed catalog lookup over `li.title` — the real, correct per-order-line title Shopify already gives us on every line item (`shopify_order_lines.title` was unaffected the whole time, which is how the misattribution was diagnosed: zero Toddler-titled order lines ever existed). Fixed both sync copies to key inventory on `inventory_item_id`/`product_id` and sales on the real per-line title instead of SKU alone. Confirmed the underlying data problem is wider than this one product — 467 SKU+location pairs currently resolve to more than one product title simultaneously — so `sales_velocity_by_sku_location_mv` and `inventory_workboard_v`'s join also widened from (location, sku) to (location, sku, product_name) (`20260821170000_sku_collision_velocity_fix.sql`), so two products sharing a SKU get separate velocity numbers instead of one blended row attached to both. Tradeoff: a product renamed within the trailing velocity window will show reduced velocity until the rename ages out of that window — narrow and self-healing, versus the prior silent cross-product blending which never self-healed. Shopify-side catalog cleanup (retiring/renaming colliding SKUs) is still a separate follow-up |
| 2026-08-21 | Ask SILO | Saved reports usability per Blake's first-week feedback: (1) "My reports" vs "Company reports" — new `visibility` column on `silo_chat_saved_reports` (`20260821090000`, applied to prod); the Save button now opens a small dialog asking for a name and where to save, and the Saved reports modal groups the list into the two sections. Private reports are creator-only at the RLS level (hidden even from exec/owner, same author-only stance as `review_private_notes`) — built for analysts/accounting running personal daily reports the company list doesn't need. All 6 pre-existing reports stayed company-visible. (2) Assistant answers and the Saved reports modal widened from 760px/560px to ~1100px — full data tables were clipping into awkward inner scroll. (3) Fixed "~$24K"-style approximations rendering as strikethrough: marked's GFM del rule pairs single tildes across a sentence, and answers never intentionally use strikethrough, so the del tokenizer is disabled in the chat's markdown rendering |
| 2026-08-20 | Ask SILO | Ask SILO could complete an entire analysis and still show the user an error: the uncrustables Black Friday restock question ran 18 clean queries (launch date, daily sell rates, on-hand, Black Friday YoY) and then hit the 12-round tool cap with the finished analysis in hand and no round left to write the answer — the UI got "Couldn't land on an answer" while the audit log held all the data. Three fixes in `silo-chat` (deployed v27): (1) when the round budget runs out, the function now forces one final tool-less turn (`tool_choice: none`) that answers from the data already gathered — an error is now only possible when there's genuinely nothing to say, and these forced answers are flagged `forced final answer at round cap` in `silo_chat_audit_log` so cap saturation stays visible; (2) `MAX_TOOL_ROUNDS` 12 → 20 — with the trigram indexes making name-search queries fast, thorough multi-step questions legitimately use more rounds; (3) the system prompt now carries the real column names the model was burning rounds guessing at — `launch_calendar.launch_date`/`preview_start_date` (it tried `launch_window_start`) and `inventory_workboard_v.total_available_quantity` + velocity columns (it tried `on_hand`/`sell_through_rate`), plus a note that the workboard view is already latest-snapshot-only |
| 2026-08-20 | Ask SILO / Reports | Ask SILO's recurring "queries timed out — the database is likely busy with a background sync" errors (hitting Blake and Loomis) diagnosed from `silo_chat_audit_log`: every failure sat at `tool_rounds = 12` (the cap), always on product-name questions ("uncrustables restock", "sonic launch history"). The model writes `product_name ilike '%term%'`, `sales_by_day` (1.1M rows) had only btree indexes — useless for a leading wildcard — so each attempt seq-scanned into `chat_run_readonly_query`'s 10s statement timeout, and retries burned all 12 rounds. The failures reproduced identically on immediate retry (one question failed 3× in 3 minutes on Aug 17), so the "busy with a background sync, wait a minute" message was wrong and made it feel random. Fixes: trigram GIN indexes on `sales_by_day.product_name`/`sku` (`20260820130000`, pg_trgm), an honest timeout message steering toward narrowing instead of retrying, and a system-prompt rule telling the model to tighten a timed-out query (date-bound big tables, coarser grain) instead of retrying it unchanged. Same root cause behind the BI Product Search slowness Kalin hit doing MLB reporting (SILO "way bigger" than Shopify — it wasn't, the page just hadn't finished loading when he compared): the page fetched EVERY `sales_by_day_verification_v` row in the date range in sequential 10k pages and filtered "mlb" client-side. Search terms now push down as server-side ILIKE (superset of the client filter, so displayed numbers are identical), typing while a big unfiltered load is in flight restarts it narrowed, broadening a term refetches (debounced, with a stale-load token), and the status line shows live row-count progress so a partially loaded table can't be mistaken for final numbers. Full-range unfiltered browsing is still row-by-row — a server-side aggregate RPC for the BI suite is the scoped-out follow-up |
| 2026-08 | Performance Reviews | The "Employee's Comments" concept only ever existed as a manager-authored template question — every question on `review_template_questions`, `free_text` included, renders read-only on both `/v2/my-review.html` and the public `/pages/review.html`; the employee has no input UI for it at all. A manager drafting a review who leaves that question blank (expecting the employee to fill it in later) leaves the employee unable to enter anything — the real, working employee-input field is the acknowledgment step's response textarea (`reviews.employee_response`), which was labeled the throwaway-sounding "Your response (optional)" and had no visible label at all in the manager's completion email. Relabeled that field "Employee's Comments" everywhere it surfaces (both employee views, and a new heading above the quoted text in the `review-finish`/`review-portal` completion emails) so it reads as the one real, intentional place for employee input instead of an optional afterthought — no schema change, `employee_response` already existed. Also closed a second gap found in the same pass: `/v2/review-editor.html` (the manager's view) never showed `employee_response`/signature at all once a review finished — the only way a manager saw it was the one-time email, easy to miss or lose. New "Employee's Comments" card renders there when `status = 'finished'`, mirroring what the employee already sees on their own page. Template questions named "Employee's Comments" or similar are still dead weight for any company using them — that's a per-company `review_templates` content fix via `/v2/review-templates.html`, out of scope here |
| 2026-08 | Purchasing | PO Report redesigned away from dense tables toward a Flexport-style card UI (PO Summary is now a card list with a date timeline; KPI band is individual stat cards; Scope filter is a segmented control; tabs are pill-style) and added a "shipment status map" — logged shipments plotted by factory country (illustrative, no live GPS/carrier-tracking API) colored by status, with a hover tooltip and a banner for shipments whose factory has no country set yet. New `factories.country` column (dropdown constrained to a fixed list of sourcing-country centroids, added to `pages/factories.html`) feeds the map. The Shipments drawer's product picker also became a checkbox list grouped by product title (expand to per-SKU checkboxes) instead of a dropdown-per-item, and shipment status is now a clickable stepper |
| 2026-08 | Purchasing | Incoming shipment tracking added to `/v2/po-report.html`, automating the manual Google Sheets PO tracker Harrison ran by hand (tracking #, ship/ETA/received dates, status, and which products shipped when POs split into multiple deliveries — a "2nd half shipment"). Per-PO "Shipments" drawer (new column in PO Summary) lets any active company member log any number of shipments per PO, each with its own status/tracking/carrier/dates/notes, and assign specific PO lines (products) to a shipment — items on one PO that arrive at different times are logged as separate shipments rather than forcing one ETA per PO. Line Detail tab shows each product's shipment status/tracking inline. Built on `incoming_shipments`, an orphan table already live in prod (applied directly, RLS in place, zero app code touching it, zero rows) — now checked into the repo for the first time, with its write policy widened from creator/admin-only to any active company member per Blake's call, plus new `incoming_shipment_lines` join table for the per-product assignment (migration `20260818210000_incoming_shipment_lines.sql`) |
| 2026-08 | Samples | Sample requests had no way to route to a specific person — `sample-notify` (shipped 2026-08-17) could only broadcast to the whole `logistics` department. New `product_samples.assigned_to` column (mirrors `mail_items.assigned_to`) plus a "Notify" section in the sample drawer (`/v2/products.html`) to pick a warehouse/logistics person; `product_samples_v` view joins assignee/creator name+email. Two new trigger events close the gap Blake asked for directly ("select a user and say sample request from xyz is ready or requested"): `SAMPLE_REQUESTED` fires on insert when the sample is an actual request (assignee set, or `request_source` set — a blank pipeline draft stays silent) and `SAMPLE_WAREHOUSE_READY` fires on the transition into `sample_status = 'warehouse_ready'`, alongside a new `SAMPLE_ASSIGNED` event on reassignment. `sample-notify` now emails the assignee directly when one is set instead of the department broadcast; a manual "Notify now" button in the drawer re-pings on demand (same public, `verify_jwt:false` invoke the DB trigger already uses — no separate JWT-gated function needed, unlike `mail-item-notify`). Same-day follow-up: the assignee is also DMed on Slack, not just emailed — `sample-notify` resolves their Slack account by matching their SILO email via `users.lookupByEmail`, then `conversations.open` + `chat.postMessage` (new `SLACK_BOT_TOKEN` secret, optional/no-op until set, independent of the existing `SLACK_SAMPLES_WEBHOOK_URL` channel post which still fires regardless of assignee) |
| 2026-08 | Samples | Sample notifications were a black box — the DB trigger's `net.http_post` to `sample-notify` is fire-and-forget from Postgres, so a "Saved." toast never actually confirmed anyone was reached. That gap caused a real ~1hr incident the same day: a missing Slack OAuth scope (`users:read.email`) silently dropped every assignee DM, only diagnosable by pulling Supabase edge-function logs by hand. New `sample_notification_log` table (one row per send attempt, written by `sample-notify`'s service-role client) closes it: records which channel(s) delivered and, on failure, why (`missing_scope`, `RESEND_API_KEY not set`, etc). Samples tab gained a "Notification Log" button (`/v2/products.html`) listing recent attempts with a thumbnail of the sample's first uploaded photo where one exists, and `saveSample()` now briefly polls for a new log row after any save that plausibly triggered a notification, following up "Saved." with "Sent to Email – Name" / "Sent to Slack – Name" (or a visible failure) once the async send actually completes — same phrasing on the manual "Notify now" button's response |
| 2026-08 | Calendar | Org Calendar V1 shipped (`/v2/calendar.html`, nav `Planning → Org Calendar`): one company-wide time layer over the tools that already own dates. Hybrid architecture after a full date-source audit (`docs/ops/org-calendar.md`): a new `calendar_events` table holds only manual events (meetings/holidays/deadlines/milestones, visibility company/finance/private), while `calendar_events_v` — a `security_invoker` UNION ALL view — projects system dates in place: launches + campaign sends, open task due dates, active-PO ship/arrival, open AP due dates, paydays, claimed TikTok Live slots (UTC→Pacific), open mail deadlines. Because the view runs as the caller, each branch inherits its source's RLS: members see no POs/AP/payroll they couldn't see in the source tool, private tasks stay private — no calendar-specific ACL to drift. Month + Upcoming views, category/type/search filters, KPI band, event drawer with row-level deep links (`?launch=`, `?po_id=`, `?item=`), manual-event CRUD (creator/admin edit). Known gaps logged for Phase C: request_manager/live-schedule have no row-level deep-link params yet; travel is still Sheets-only; reviews have no cycle dates; AR invoice dues deferred behind a future toggle (row volume) |
| 2026-08 | Login / onboarding | Two new hires in one day (Grant, Brian) got wrecked by the self-signup path, so the login page now closes all three trapdoors: (1) signing up with an email that already has an account no longer shows the lying "Account created. Confirm your email" message — Supabase obfuscates that case (no error, no session, empty `identities`, and no email is ever sent for a confirmed address), which sent Grant into a wait-for-mail-then-invalid-credentials loop; it now says plainly "this email already has an account — sign in or use Forgot password" and prefills the sign-in form. (2) When an invite is for an email with an existing account, the "Create account" button is removed entirely — sign-in is the only path offered, with the invite auto-applying after auth. (3) Founding a brand-new organization now requires ticking an explicit confirmation checkbox ("if you work somewhere that already uses SILO, stop here — ask for an invite link"), so employees can't stumble into it. Brian's stray half-account (auth login with no profile/membership/org) was deleted clean so his invite lands on the fresh "set password & join" path |
| 2026-08 | Backend / roles | The backend's Edit dialog could not actually grant the `executive` role: `admin_update_profile()` only mapped owner/admin and silently coerced everything else — executive included — to profile `user`, then downgraded the person's company membership to `member` as a side effect. Caught live promoting Lopez + Loomis to executive for review-template building: Loomis's membership dropped admin→member, silently costing him profile-name visibility, PO writes, and other membership-admin gates (repaired by hand). RPC now maps the full vocabulary the UI offers (owner/admin/executive/member/viewer/user; executive → profile `executive` + membership `admin`, matching how executives were already configured) and raises on unknown values instead of silently coercing; the never-real `superadmin` option removed from the dropdown. Verified by calling the RPC as owner against both executives: profile `executive` / membership `admin` both times |
| 2026-08 | Payment requests | First real member-tier user (new marketing hire) hit `violates row-level security policy for table "payment_request_files"` submitting a reimbursement — he'd signed up and submitted in the ~10-minute window before his company membership was granted, so `active_company_id()` was NULL: the parent request inserted (that policy had no company check) as an invisible NULL-company ghost, then the attachment rows failed (their policy does check). Six ghost retries cleaned up; his half-deleted account repaired to user/marketing/member and verified working under RLS impersonation — **members do NOT need admin to submit**. Systemic fixes: `payment_requests` insert now requires `company_entity_id = active_company_id()` (fails clearly on the first insert for unactivated accounts, no-op for everyone else), and the request form checks activation up front — disabled submit + "ask an admin to add you, then log out/in" instead of the raw RLS error |
| 2026-08 | Shopify sync | Nightly sync moved from 11:00 UTC to 08:30 UTC. The old anchor ("after the nightly Sheets sync at 10:30") pointed at a pipeline retired in July, and its knock-on effect was that the sales performance "as of" date — which only advances when the end-of-sync comp refresh fires — didn't flip to yesterday until ~6:30–10am PT depending on GitHub cron drift, so morning check-ins (especially Eastern, 3 hours ahead) saw data a day behind. New start is 1:30am PDT / 12:30am PST — the earliest DST-safe slot past Pacific midnight — putting the finished refresh at ~3–5am PT / 6–8am ET. Guard comment added: never schedule before 08:00 UTC or the winter run would start before the Pacific day ends and anchor a partial day |
| 2026-08 | Performance Reviews | Three UI-layer bugs that survived all the RLS fixes because they only reproduce in a browser, not in SQL: (1) `employee_managers` has two FKs to `profiles`, so the roster page's bare `profiles(...)` embed was ambiguous — PostgREST rejected the whole manager-links query, the error was silently swallowed, and the empty map made the roster filter drop every employee for non-exec users ("Employee added" toast, then nothing in the UI, and Start review gated on the empty roster so templates never appeared). Fixed with an explicit FK hint (`profiles!employee_managers_manager_user_id_fkey`) + the error now surfaces. (2) The team-picker embed followed `entity_memberships → profiles`, but that table's `user_id` FK points at `auth.users` — no relationship exists, the embed errored, silent catch, empty Manager/Managers dropdowns. Replaced with two plain queries (memberships then `profiles .in()`). (3) Beacon's `display:flex/inline-flex` on `.bcn-field-group`/`.bcn-btn` overrides the `hidden` attribute (author CSS beats UA CSS), so every conditionally-hidden control rendered anyway — non-exec users saw the empty exec-only Manager picker, the edit-only Managers/Delete controls inside Add, and the exec-only Templates button (which led them to the exec-gated builder page — the "can't see template" reports). Page-wide `[hidden]{display:none!important}`, same idea as beacon.css's existing `.bcn-status[hidden]` patch. This was also the real cause of the earlier "dropdowns empty / dialog looks wrong" reports that were misdiagnosed as stale cache |
| 2026-08 | Performance Reviews | Self-service employee creation was fully broken for every non-exec user (live in prod since the prior migration, caught while building the "add a co-manager at creation" UX, before any real manager other than the account being tested had hit it). Two compounding RLS bugs, same root class as the earlier `employee_managers` recursion fix but a different failure mode (access-denied, not infinite recursion): the roster page's employee insert requested the row back via `.select()`, which needs `INSERT...RETURNING` to pass the SELECT policy on that row — but a brand-new employee has no manager link yet, so the creator was denied visibility of their own just-created row. Fixed by generating the employee id client-side and dropping `.select()` (same CSV-import fix). Even after that, the *next* step — self-linking as manager — independently failed the same way one level deeper (`employee_managers`' own creator-check subquery was itself blocked by the same employees visibility gap); new `is_employee_creator()` `SECURITY DEFINER` helper fixes it. Verified end-to-end under real RLS impersonation: create → self-link → add a second manager → both managers see the employee. Also shipped in the same pass: adding a co-manager no longer requires add → save → reopen Edit — the Add-employee dialog now has an optional "Also share with" field to do it in one step, plus a plain-language hint for non-exec users explaining they'll be set as the manager (previously the manager assignment was silent and invisible, which read as broken even when it wasn't) |
| 2026-08 | Performance Reviews | An employee could only ever have one manager (`employees.manager_user_id`, single column, company-wide unique email index) — no way to represent dual reporting (e.g. an employee needing review from both Loomis and Brett). New `employee_managers` many-to-many join table (backfilled from the existing column) is now the authorization source for roster/review RLS; `employees.manager_user_id` stays but is informational-only (original creator). `reviews_active_insert` also tightened to verify the inserting manager is actually linked to the employee — previously trusted the client with no relationship check. `/v2/reviews.html`: edit-employee dialog gained a Managers list (any co-manager can add another; removing is self-service, removing someone else is exec-only), and starting a review for a shared employee lets exec pick which manager it's assigned to. Caught and fixed same-day before this reached `main`: the first version's RLS did a raw `EXISTS` against `employee_managers` from inside that table's own policy, which re-triggers the policy on every access — `infinite recursion detected in policy for relation "employee_managers"`. New `is_employee_manager()` SECURITY DEFINER helper (same RLS-bypass mechanism as `active_company_id()`) replaces every such subquery across `employee_managers`/`employees`/`employee_goals`/`reviews` |
| 2026-08 | Performance Reviews | Roster/review RLS silently required SILO admin/executive/owner role on top of `employees.manager_user_id = auth.uid()` — so a plain (non-admin) manager couldn't roster or review their own direct reports at all, and the failure mode was a confusing "Save failed." Surfaced by Jon Loomis hitting it on Team → Performance Reviews. `reviews_can_manage()` redefined to true-for-any-active-user (one function, ~15 call sites update automatically); per-row scoping is unchanged — Blake stays company-wide super-admin via `is_exec_or_owner()`, template building stays exec-only. Applied directly to prod, no admin/exec promotion needed for managers going forward |
| 2026-08 | Mailroom | Mail intake/routing had zero notifications — logging or assigning mail never told anyone, unlike POs (Slack) and payment requests (email); staff only found out by opening the queue. New `mail-item-notify` edge function emails the assignee when mail is routed to them (on intake with an assignee set, or on reassignment) and emails the original submitter when their item is marked done, via Resend (same `noreply@silo-baseballism.com` sender as reviews/payment requests). Mailroom has no manage-permission RPC gate, so the function authorizes through `mail_items_v`'s own RLS (same active company) rather than a role check. Added a manual "Notify assignee" button in the drawer for on-demand re-pings, and `?item=<id>` deep-linking on `/v2/mailroom.html` so the email's link opens straight to the item |
| 2026-08 | Receivables | The staleness banner (below) immediately caught a second, worse gap: the AR sync writes as service_role and never stamped `company_entity_id`, so every row inserted after the 2026-06 multi-tenant backfill (23 orders, Jun 15–Jul 21, incl. MLB1492–1503 + a Scheels batch) was invisible to ALL users under RLS while the sync reported success — the workbench had been blind since Jun 12, not Jul 21. Rows backfilled in prod + idempotent migration (20260803160000); the sync now resolves the company entity (`AR_COMPANY_ENTITY_KEY`, default `baseballism`, hard-fails if unresolvable) and stamps every customer/invoice row it upserts; the banner now distinguishes "RLS is hiding rows the sync wrote" (payload newest vs page-visible newest) from "the sheet itself is stale". Also fixed a 1-day timezone shift in the banner/label dates (`new Date('YYYY-MM-DD')` is UTC midnight → prior day in Pacific) |
| 2026-08 | Receivables | AR staleness made loud: the source Google Sheet silently stopped receiving new orders on Jul 21 (its scheduled report kept refreshing — old rows rolled off — but no new orders matched), so the nightly sync stayed green for ~2 weeks while BBISM Receivables drifted stale. The sync now records the newest order date per source in `job_sync_state.payload` and **fails the workflow** when it's older than `AR_STALE_MAX_DAYS` (repo var, default 7 — data still syncs first, the red run is the alarm), and the workbench shows an amber "Stale data" banner with the newest order date plus a "Newest order" chip in the sync label. Fix for the missing orders themselves is upstream in the scheduled report's filters/date range — see when-something-breaks.md |
| 2026-07 | Team | Performance Reviews module shipped end to end (5 phases, PRs #256–#264): exec/owner builds survey-style templates (free text, 1–10, single/multi choice, goals; publish locks questions), managers roster employees (grid + CSV import, edit, delete with cascade warning) and run reviews with private notes (author-only RLS) and goals that persist on the employee across cycles; Send mints a hashed 30-day token and emails the employee via Resend (`noreply@silo-baseballism.com`); SILO-authenticated employees view/sign in-app at `/v2/my-review.html`, associates without SILO logins sign via the public portal `/pages/review.html` (token = the authorization, expired links self-renew rate-limited); signing locks the review immutable + notifies the manager. New `executive` role tier; manager-scoped RLS throughout (managers see only their roster; exec/owner see all); 8 new tables + 3 edge functions (`review-send`, `review-portal`, `review-finish`). Live-tested both paths with Brett incl. a flipped manager/employee test |
| 2026-07 | Accounting | Sales tax income wash added to the journal entry, per Blake's real workbook export: a "Sales Tax Income" credit per location offsets the existing "Sales Tax Expense" debit so the pass-through nets to zero on the P&L instead of showing as a bare expense. Two corrections after comparing against Blake's real export line-by-line: (1) no separate revenue debit is needed — `accounting_sales_buckets().gross` is already tax-exclusive (price × qty, no tax), so debiting revenue by tax again was double-subtracting it and understating recognized revenue; (2) AR/Clearing must include tax (`gross − discounts − refunds + shipping + taxes`, i.e. the buckets' own `total` field) since that's the actual cash Shopify collects and remits — it was previously tax-exclusive and understated by the tax amount, which is why per-location Clearing looked off from the real book. New `tax_income_all` COA map key (seeded "Sales Tax Income", pairing the existing "Sales Tax Expense" rename). Playwright-verified against real June figures (Atlanta, St. Louis, Online): each location ties out individually, AR/Clearing matches the real export to within data-snapshot-timing rounding, and the whole-journal balance chip stays green |
| 2026-07 | Insights | Shipped Action Items & Insights (`/v2/insights.html`) — a deterministic rules engine (`compute_silo_insights()`) scans sales pace vs prior year, inventory stockout/dead-stock risk, purchasing (draft/overdue POs), launch readiness/overdue tasks, AR aging, and AP overdue/large payment requests, then an AI step (nightly, `scripts/generate-insights.mjs`) synthesizes a short briefing from ONLY those findings — no reconciliation/tie-out framing, no invented facts. AR/AP findings hidden for non-finance departments, same rule as the Accounting nav section. Requires a new `ANTHROPIC_API_KEY` GitHub secret (falls back to findings-only, no narrative, if unset) |
| 2026-07 | Reports | Top Sellers leaderboard now ranks by product title, not by SKU — a product with 5 sizes used to claim 5 leaderboard slots (e.g. "Bubbles and Doubles Shorts" occupied 3 of the top 12 as separate Youth/S/M/L rows). Qty, avg/day, and on-hand are summed across every contributing SKU and location; the Location column becomes a count ("2 locations") since the By-location card already has that breakdown, and the subtitle shows SKU count instead of a single variant. CSV export matches. Playwright-verified: 7 raw SKU/location rows for 3 distinct products collapse to exactly 3 leaderboard rows, ranked correctly by summed qty |
| 2026-07 | Reports | Top Sellers scroll-box fix from the last PR made things *worse* — the leaderboard rendered at 0px tall (one row barely visible) instead of the intended scrollable panel. Root cause: `.bcn-card-body` lost its `overflow:visible` override, so it (and the new inner scroll div) became flex items with `overflow:auto` nested 3 levels inside flex columns — the flexbox spec treats a scrollable flex item's automatic minimum size as 0, so under space pressure it collapsed instead of scrolling. Fixed with `overflow:visible` restored on the card body, explicit `min-height` on the scroll boxes as a hard floor, and `flex-shrink:0` on the cards so they claim their intended size and the page scrolls a little instead of squeezing tables to nothing. Playwright-verified this time by measuring the actual rendered pixel height of the scroll box (not just page height, which is what let the first broken version slip through) |
| 2026-07 | Reports | Top Sellers UI fixed — the 50-row leaderboard, 25-row type table, and uncapped location list all rendered inline with no scroll container, pushing the page past ~4000px and forcing zoom-out just to see it. Each table now scrolls in its own bounded panel with a sticky header (same `.bcn-matrix-scroll` pattern used elsewhere); page fits the viewport, no data or query changes |
| 2026-07 | Access | Department filtering follow-up from Brett's live test: the sidebar filtered correctly, but Home's hardcoded tool cards leaked — Payables/Receivables cards, Request Manager, and Payroll BI links now carry `data-departments="exec,finance"` and a card filter using the same session-cached department. Also found the legacy AP pages (`accountspayable.html`, `ap-report.html`) had NO auth at all (Google-Sheets-backed, public URL) — both now load config + supabase + dept-guard |
| 2026-07 | Access | Department-level nav filtering shipped (layer 1 of the department plan): finance-sensitive links (Accounting section, Request Manager, Payroll BI) show only for `exec`/`finance` departments; employee-facing forms (Payment Request, Travel) and Reports/Planning stay visible to everyone. Chrome resolves the user's department from profiles (session-cached, cleared on the login page), and a new `dept-guard.js` soft-redirects non-finance deep links on the six finance pages. UX only — layer 3 adds department-aware RLS on the underlying tables |
| 2026-07 | Slack | Draft POs no longer post to Slack at all — the INSERT notification fires only for POs created in a non-Draft status; a draft that later gets sent is announced by the status-transition trigger. The daily summary still counts drafts (aggregate, not per-PO noise) |
| 2026-07 | Slack | PO notifications no longer claim every new PO was "Sent to Factory" — the INSERT trigger fires for drafts too (20 of 73 POs are Drafts). The notify-slack edge function header is now status-aware ("New Draft PO Created" / "New PO Sent to Factory" / "New PO Created — {status}"), a new `trg_slack_po_sent` trigger posts "PO Sent to Factory" at the moment a PO's status actually transitions to Sent to Factory, and the daily summary splits new POs into sent vs draft and stops counting Cancelled/Received/Draft POs as "arriving in the next 7 days" |
| 2026-07 | Shopify sync | First nightly with the payouts step failed: `sync_jobs.job_type` has a CHECK constraint and `payouts_sync` wasn't in the allowed list, so the job insert threw for every connection — which also aborted each connection's inventory snapshot (payouts step runs first). Sales synced fine. Constraint extended (20260709020000); verification dispatch confirmed payouts + inventory jobs green |
| 2026-07 | Accounting | Accounting Export shipped (`/v2/accounting-export.html`): pick a month → journal-ready entries replacing the manual Google Sheets process. Sales journal per location (revenue/freight/refunds/discounts/tax pair/AR, balanced, from the penny-tied sales buckets), processing-fee entry per store, and a deposit register for bank rec — all exportable as CSV. Powered by a new nightly `payouts_sync` job (Shopify Payments payouts per store, `shopify_payouts` table, YTD backfilled for all 20 stores: 1,476 payouts since Jan 1) and an `accounting_coa_map` table seeded with the sheet's account names, editable in the UI. `accounting_sales_buckets()` RPC aggregates server-side under RLS |
| 2026-07 | Multi-tenant | Full audit of every public table/view lacking `company_entity_id` (40 objects): 39 are correct by design — the `entity_*` registry + `activity_events` + `files` scope by membership, `profiles` is per-user, `job_sync_state` is deny-all to clients, and all 31 flagged views are security_invoker over scoped tables or DEFINER MV readers filtering `active_company_id()`. One real gap: `launch_task_templates` had `true` policies (any company could read/write any company's templates) — empty table, so latent not leaked; company-scoped with the standard column + stamp trigger + active-company policies (20260709000000) and added to the verify script |
| 2026-07 | Shopify sync | Shipping discounts captured: `total_shipping_price_set` is the PRE-discount charge, so orders with a 100%-off shipping code (the $9.99 "Baseballism Fast Pass 🔥" free-shipping promo) booked $9.99 in SILO while Shopify reported $0 — no refund object exists, so it survived incremental re-syncs (7/6 +$29.97, 7/7 +$19.98, always $9.99 multiples). Shipping now sums `shipping_lines[].discounted_price_set` (fallback to the old field when shipping_lines is absent); refunded shipping is unchanged (still negative rows on the refund date). Verified against the Admin API: 5 discounted orders matched both days' deltas exactly and the recomputed totals tie to Shopify to the penny. Re-run history sync for days since the promo started to restate |
| 2026-07 | Cleanup | Dead "Executive" links removed: `executive.html` doesn't exist in the repo at all — Home's Overview card link, backend.html's "Owner cockpit" button, and its access-denied redirect all pointed at 404s. Overview card removed from Home (no remaining links); backend's denied users now land on Home |
| 2026-07 | Employee Hub | Retired entirely (redirect stub → Home): resurrecting it this morning made clear it's a pre-SILO iframe launcher with its own non-Beacon shell, and everything unique in it already lives on Home's Requests card. Home's Overview link and the profile default-page option removed; anyone whose default page or bookmark points at it lands on Home |
| 2026-07 | Multi-tenant | MV reader views flipped to definer + inventory MV grants closed: security_invoker on the filtered `_v` views required users to hold SELECT on the locked MVs (workbench/planning broke with "permission denied"); definer views read the MV with owner rights while `active_company_id()` still scopes rows. Also found via `pg_class.relacl` (information_schema is blind to matview grants) that `inventory_on_hand_current_mv` still granted anon+authenticated FULL privileges — a direct PostgREST cross-company read path — now service_role-only. Verified by impersonating an authenticated session: workboard/rollup/velocity return active-company rows only, direct MV access denied |
| 2026-07 | Multi-tenant | MV isolation sweep completed: `sales_velocity_by_sku_location_mv` (inventory workboard sell-through + planning demand math) blended all companies by (location, sku) — rebuilt per-company, service_role-only, with the `_v` view filtering `active_company_id()` so the workboard join is same-company by construction; refresh RPC hardened (concurrent w/ plain fallback, 300s timeout). Orphaned `sales_sku_location_rollup_mv` (no dependents, no company column) access revoked pending deletion. All four MVs now scoped or locked |
| 2026-07 | Multi-tenant | Planning Scenarios cross-company leak fixed: `sales_monthly_product_type_rollup_mv` aggregated ALL companies' sales into one blended rollup with no company column and was directly readable by authenticated users (MVs bypass RLS) — test-co saw Baseballism's baseline numbers. Rebuilt per-company, MV access revoked to service_role-only, page repointed at the security-invoker view filtered by `active_company_id()` (inventory-MV pattern), and a `refresh_sales_monthly_rollup_mv()` RPC now runs in the nightly Shopify sync (the MV previously had no refresh path at all) |
| 2026-07 | Multi-tenant | Company switcher shipped (roadmap item, urgent now that test-co exists): the sidebar company name links to `/v2/company-picker.html`, which now self-loads memberships on direct visits (previously bounced to login) and honors a sanitized `?next=` — so switching returns you to the page you were on. Login also threads `?next=` through the picker for multi-company users, whose deep links previously died at the pick step |
| 2026-07 | Sync | Legacy Sheets/Better Reports nightly sync retired: coverage verified first (every sales location has shopify_api rows in the last 21 days; Shopify's latest snapshots cover all 22 inventory locations incl. the 11 BR ones) — Shopify API is now the sole source of truth for sales + inventory. The inventory current-MV refresh moved from the Sheets script into shopify-sync.mjs. Manual dispatch kept for emergencies; BR history stays in `sales_by_day` for pre-API reporting |
| 2026-07 | Multi-tenant | `inventory_on_hand` isolation finished (last holdout): the Sheets nightly sync never stamped `company_entity_id` on inventory rows (~816k legacy NULLs — also invisible in the workboard, whose view filters by active company), and the old `inventory_on_hand_admin_all` policy had no company predicate, letting any company's admin read/write every row. Sync script now stamps Baseballism; NULLs backfilled via a temporary pg_cron chunk job (25k/min — single-statement updates exceeded API timeouts against 12 indexes); policy replaced with company-bound admin writes; MV refreshed so legacy rows reappear |
| 2026-07 | Multi-tenant | `product_tags` company-scoped like the rest: `company_entity_id` added + backfilled (443 rows → Baseballism), insert stamp trigger attached, legacy authenticated-read/admin-write policies replaced with `active_company_id()` isolation (writes stay admin-only). Closes the future-company exposure where the Products Catalog tab would have shown Baseballism tags to other companies' users. Catalog query re-scoped client-side too |
| 2026-07 | Products | Hotfix: Catalog tab queried `product_tags` with a `company_entity_id` filter, but that table never got the multi-tenant column (backfill deferred) — the 400 rejected the boot `Promise.all` and hung the whole page on "Loading…". Filter removed, and catalog loading is now isolated so a catalog failure can't take down Pipeline/Samples. Bonus: the type dropdowns selected `product_type` from a table whose column is `type` — silently empty since the Tracker shipped; fixed |
| 2026-07 | Employee Hub | Nav button removed (duplicative: 1 of its 7 tools was already in the nav, the rest are external form bookmarks). Its unique links — Receipt Upload, Travel Request form, Cash Reconciliation, Logistics form, Requests Hub grid — moved to Home's Requests card, and Home's Overview card links to the page itself. Start is now just Home · My Profile |
| 2026-07 | Employee Hub | Page was dead for everyone — the main `<script>` had no closing tag, so the browser never executed a line of the app and every visitor (signed in or not) saw only the default sign-in card. Restored the closer; also: no-session now redirects to login (with `?next=`) instead of stranding users on the card, unexpected boot errors surface a message, and `login.html` finally honors `?next=` (same-origin paths only) so sign-in returns you to the page you came from instead of always dumping to finance.html |
| 2026-07 | Nav | Start section simplified: "SILO home" removed (legacy standalone landing card — a dead end for logged-in users), "Finance department" renamed **Home** (it's the actual post-login destination for every department per login.html routing). Start is now Home / My Profile / Employee Hub |
| 2026-07 | Nav | Sidebar accordion: exactly one section open at a time — the active page's section on load, click to switch (the old behavior accumulated every opened section in localStorage forever, so the menu only ever grew). Baseballism nav consolidated 10 → 7 sections: Payables + Receivables + Requests merged into **Accounting** (7 links), single-item Overview folded into Start as "Employee Hub". Fixed standard-profile bug where the Settings section (Integrations) was silently dropped because it wasn't in STANDARD_SECTION_ORDER |
| 2026-07 | Tasks | Task Manager upgraded for the marketing team: `launch_tasks.launch_id` no longer required (DB NOT NULL dropped + UI), enabling evergreen to-dos; per-person prioritized lists (All Tasks / My List / any teammate) with manual up/down ordering (`sort_order`) and an Auto-arrange action (priority → due date → quickest first); estimated time (`estimated_minutes`) shown in the list and used by auto-arrange; private tasks (`is_private` + RLS: visible to assignee + creator only); launch cells link to the launch drawer, the launch drawer's Tasks tab links back to the filtered Task Manager, and `?view=/?launch=/?assignee=` deep links |
| 2026-07 | Products | Product Hub + Product Tracker consolidated into `/v2/products.html` (Pipeline / Samples / Catalog tabs). Kills two competing editors over `product_tracker` — Hub's editor was corrupting data on every save (`on_hand` text coerced to `0`, youth/cap sizes wiped by its 8-size grid, `factory_id` nulled by a `factories.name` vs `factory_name` query bug). Catalog tab rebuilds the legacy `/pages/product-manager.html` tags editor in the Beacon shell. New `product_samples.tracker_id` links samples directly to pipeline items (backfilled); deep links (`?item=`, `?sample=`, `?tab=`, launch-calendar `?launch=`) make every cross-reference clickable in both directions. Old URLs redirect; nav shows one "Products" entry |
| 2026-07 | Shopify sync | Cancelled orders included: the sync skipped `cancelled_at` orders on the wrong assumption that Shopify sales reports exclude them — Shopify keeps the sale and books the cancellation refund as a Return on its processed date. Confirmed against a raw 6/7 orders export: 177 cancelled TikTok-Shop "Seller discount" orders = $24,310 gross / $22,976 discounts, matching the day's report gap to the dollar; same mechanism drove the ~1%/month gross drizzle and most of the ~$94k Returns gap Jan–Jun. Test orders remain excluded. Requires a history re-import to restate |
| 2026-07 | Shopify sync | Shipping tax captured: `taxes` only summed `line_items[].tax_lines`, but tax charged on shipping lives on `shipping_lines[].tax_lines` — Shopify's Taxes column includes it, so SILO ran ~5%/month low (~$30.6k over Jan–Jun online). Now allocated per line alongside the shipping charge; requires a history re-import to restate. Part of the PBI-variance reconciliation — returns valuation (~$94k) and promo-day free-gift lines (~$84k) still under investigation, see bugs.md |
| 2026-07 | Sales verification | Comp summary now anchors to the last **complete** business day (Pacific) instead of `max(day_date)` — the incremental Shopify sync always lands a few hours of the in-progress day, so the Day comp was showing a partial CY day against a full PY day (e.g. $3.4k vs $106k on 7/7) and the partial day leaked into MTD/YTD while PY windows covered full days; KPI labels on the BI overview/dashboard now name the anchored day instead of saying "Today" |
| 2026-07 | Sales verification | `refresh_sales_verification_store_comp_summary()` gained tax-inclusive Total Sales columns (matches the legacy PBI dashboard's Day/MTD/YTD figures, which were never net-of-tax) and an explicit `statement_timeout = 120s` — the full recompute over `sales_by_day_verification_v` (1.1M+ rows) had no override and was silently timing out under the nightly sync's `service_role` PostgREST call (same class of bug as the `purge_better_reports_overlap` fix below), requiring manual refreshes to see current data |
| 2026-07 | Shopify sync | Refund date parity with Shopify POS reports / Better Reports: refunds now book as negative rows on the refund's `processed_at` date (per-SKU + `[Shipping]` + `[Refund discrepancy]` rows, `total_orders = 0`, additive-only) instead of restating the original order day — requires one full history re-import per store |
| 2026-07 | Shopify sync | BR-vs-API variance: incremental sync fetches by `updated_at` and rebuilds affected days (captures refunds/edits/cancellations on older orders); gift cards, cancelled and test orders excluded (Shopify report parity); history chunk boundary dates no longer overwritten with partial aggregates; `row_hash` includes `shop_domain` |
| 2026-07 | Shopify sync | `purge_better_reports_overlap` rewritten as indexed semi-join (old self-join timed out on ~1M rows, leaving BR + shopify_api rows double-counted for recent days); duplicate active connections (chicago, dsg) deactivated; `default_location_code` set for main + wholesale shops so unfulfilled orders aren't dropped |
| 2026-06 | Multi-tenant | **Phase 1 complete**: company context layer, RLS active-company isolation, view security_invoker — see multi-tenant section in roadmap.md |
| 2026-06 | Multi-tenant | `profiles.active_company_id` column + `active_company_id()` / `set_active_company()` RPC functions — DB-level per-session company isolation |
| 2026-06 | Multi-tenant | `security_invoker = true` on all 30+ views in public schema so RLS applies through views, not just on base tables |
| 2026-06 | Multi-tenant | `company_entity_id` column backfilled on 40+ operational tables (excluding `inventory_on_hand` and `sales_by_day` — deferred) |
| 2026-06 | Multi-tenant | Company picker page (`/v2/company-picker.html`) for users with multiple company memberships |
| 2026-06 | Multi-tenant | `_co` + `company_entity_id` JS query filters added to profile.html work queue, tasks.html, projections.html, launch-calendar.html, po-builder.html, purchase_request.html, request_manager.html, po-costing.html, product-manager.html, product-samples.html, factories.html |
| 2026-06 | Multi-tenant | Insert hardening: `stamp_company_entity_id` trigger on all company-scoped tables (excl. inventory/sales bulk) + `withCompany()` / `withCompanyRows()` in `pages/config.js` |
| 2026-06 | Shopify | Phase 2a: `access_scopes.json` on connection test; `scopes_granted` / `scopes_missing` on `shopify_connections`; UI scope warnings |
| 2026-06 | Shopify | Hotfix: `access_scopes.json` must use `/admin/oauth/` (unversioned); versioned path returned 404 and false missing-scope warnings |
| 2026-06 | Shopify | Revert PR #166 scope regression: restore client-side retest persist, clear stale `meta.scopes_fetch_error`, keep `/admin/oauth/access_scopes.json` |
| 2026-06 | Shopify | Phase 2b: `shopify-sync.mjs` orchestrator, `shopify-sync.yml` workflow, `source=shopify_api` writes with `company_entity_id` |
| 2026-06 | Shopify | UI-initiated windowed sales backfill (90/365/730d) + inventory sync via `shopify-sync-run` edge function; `sync_enabled` toggle on Integrations |
| 2026-06 | Sales verification | `sales_by_day` + `sales_verification_store_comp_summary` scoped by `company_entity_id`; refresh RPC per company; Sheets sync stamps Baseballism id on insert |
| 2026-06 | Sales verification | Server-side `sales_verification_filtered_summary` RPC replaces client row-chunk scans (fixes timeouts on large histories) |
| 2026-06 | Shopify sync | Sales row math: refunds from `order.refunds`, net/total sales include shipping, duties, additional fees, and taxes (Shopify POS parity) |
| 2026-06 | Shopify / Integrations | Location mapper UI: link Shopify locations to SILO `locations` (create or map existing); `list_shopify_locations` edge action |
| 2026-06 | Integrations | Fix location create duplicate PK: `next_location_id()` RPC (client max(id) was RLS-scoped per company) |
| 2026-06 | Shopify sync | Sales rows link to SILO mapped locations when Shopify omits order location_id; fresh history import purges stale shopify_api sales |
| 2026-06 | Auth | Login resolves company via `entity_memberships`, auto-sets single company or routes to picker for multi-company users |
|------|------|----------------|
| 2026-06 | Supabase schema | Verified all tables, RLS, and policies healthy across PO builder, costing, profiles, and launch workbench |
| 2026-06 | Supabase schema | Created missing `po_builder_can_write()` function (was in migration but hadn't applied) |
| 2026-06 | RLS | Confirmed all 13 tables have RLS enabled; PO builder had pre-existing dashboard policies — all access working |
| 2026-06 | Roles | All 7 profiles are `admin` — full write access to PO builder and costing confirmed |
| 2026-06 | Launch comments | `author_name`, `author_email`, `user_id` columns confirmed present on `launch_comments` |
| 2026-05 | Planning Scenarios v2 | Restored v1 revenue/mix/ASP logic; filter scope parity — see [planning-scenarios-filter-scope.md](../planning-scenarios-filter-scope.md) |
| 2026-05 | Planning Scenarios v2 | Rebuilt page from v1 layout + projection seed + Beacon theme |
| 2026-05 | PO module | SQL migrations for builder, costing, profile RLS — [supabase/README.md](../../supabase/README.md) |
