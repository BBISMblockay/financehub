{
  "name": "silo-v2-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Tests for the /v2/ inventory page. Own package scope because the repo root is type:module and these suites are CommonJS.",
  "type": "commonjs",
  "scripts": {
    "test": "node run.js",
    "test:unit": "node run.js --unit",
    "test:browser": "node run.js --browser"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
