.claude/
data/legacy-payment-requests*.csv
data/legacy-payment-requests*.tsv
node_modules/
package-lock.json
supabase/.temp/

# v3 test dependencies (installed with `cd v3/tests && npm install`)
v3/tests/node_modules
v3/tests/package-lock.json
v3/tests/*.png

# v2 test dependencies (installed with `cd v2/tests && npm install`)
v2/tests/node_modules
v2/tests/package-lock.json
v2/tests/*.png

# Local screenshot scratch (PR screenshots are attached, not committed)
.screenshots/
